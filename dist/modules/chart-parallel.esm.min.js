/*!
 * Lattice Grid 1.34.0, chart-parallel module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-parallel/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"bindParallel",{enumerable:true,get:function(){return bindParallel;}});
Object.defineProperty(__exports,"drawParallel",{enumerable:true,get:function(){return drawParallel;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
function bindParallel(grid,spec){
const columns=(Array.isArray(spec.columns)?spec.columns:[]);
const rows=[];
const groupSet=[];
const n=grid.rows.count();
for(let i=0;i<n;i++){
const row=grid.rows.get(i);
if(!row||row.group)continue;
const record={values:{},group:spec.colourBy?grid.rows.text(row.key,spec.colourBy):null};
let any=false;
for(const col of columns){
const v=Number(grid.rows.value(row.key,col));
record.values[col]=Number.isFinite(v)?v:null;
if(record.values[col]!==null)any=true;
}
if(record.group!==null&&!groupSet.includes(record.group))groupSet.push(record.group);
if(any)rows.push(record);
}
return{rows,columns,groups:groupSet,empty:rows.length===0||columns.length<2};
}
function drawParallel(ctx){
const{
plot,bound,scheme,fontSize,groups,helpers,
}=ctx;
const{
extent,linearScale,Pool,attrs,path,round,setText,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const{rows,columns}=bound;
const axisX=columns.map((_,i)=>(columns.length===1
?(plot.left+plot.right)/2
:plot.left+(plot.right-plot.left)*(i/(columns.length-1))));
const scales={};
for(const col of columns){
const dom=extent(rows.map((r)=>r.values[col]).filter((v)=>v!==null))||{min:0,max:1};
scales[col]=linearScale(dom,[plot.bottom,plot.top]);
}
const axisPool=new Pool(groups.axis);
const labelPool=new Pool(groups.overlay);
columns.forEach((col,i)=>{
attrs(axisPool.next('line',`${NS}__pc-axis`),{
x1:round(axisX[i]),y1:round(plot.top),x2:round(axisX[i]),y2:round(plot.bottom),
stroke:'currentColor','stroke-opacity':0.3,
});
const label=labelPool.next('text',`${NS}__pc-label`);
setText(label,String(col));
attrs(label,{
x:round(axisX[i]),y:round(plot.top-4),
'text-anchor':i===0?'start':i===columns.length-1?'end':'middle',
'font-size':fontSize,
});
});
const marks=new Pool(groups.marks);
for(const r of rows){
const parts=[];
columns.forEach((col,i)=>{
const v=r.values[col];
if(v===null){return;}
parts.push(parts.length?'L':'M',round(axisX[i]),round(scales[col].of(v)));
});
if(parts.length<3)continue;
const colour=r.group!==null?sch.series(Math.max(0,bound.groups.indexOf(r.group))):sch.series(0);
attrs(marks.next('path',`${NS}__pc-line ${NS}__mark`),{
d:path(parts),stroke:colour,'stroke-width':1,'stroke-opacity':0.4,fill:'none',
});
}
axisPool.finish();
labelPool.finish();
marks.finish();
return{rows:rows.length,dimensions:columns.length,legend:[]};
}
registerChartType('parallel',{draw:drawParallel,bind:bindParallel,freeform:true,labelled:false});
const __default=drawParallel;
});
const __entry=__req("packages/modules/chart-parallel/index.js");
export const
bindParallel=__entry.bindParallel,
drawParallel=__entry.drawParallel;
export default __entry.default;