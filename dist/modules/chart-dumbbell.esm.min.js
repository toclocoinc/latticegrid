/*!
 * Lattice Grid 1.34.1, chart-dumbbell module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-dumbbell/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"bindDumbbell",{enumerable:true,get:function(){return bindDumbbell;}});
Object.defineProperty(__exports,"drawDumbbell",{enumerable:true,get:function(){return drawDumbbell;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
function bindDumbbell(grid,spec){
const rows=[];
const n=grid.rows.count();
for(let i=0;i<n;i++){
const row=grid.rows.get(i);
if(!row||row.group)continue;
const start=Number(grid.rows.value(row.key,spec.start));
const end=Number(grid.rows.value(row.key,spec.end));
if(!Number.isFinite(start)||!Number.isFinite(end))continue;
rows.push({label:spec.x?grid.rows.text(row.key,spec.x):String(rows.length),start,end});
}
return{rows,empty:rows.length===0};
}
function drawDumbbell(ctx){
const{
plot,bound,scheme,fontSize,groups,grid,helpers,
}=ctx;
const{
measureDomain,linearScale,bandScale,linearTicks,Pool,attrs,path,round,setText,
formatters,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const startColour=sch.series(0);
const endColour=sch.series(1);
const{rows}=bound;
const values=[];
for(const r of rows)values.push(r.start,r.end);
const xDomain=measureDomain(values,{zero:false});
const xScale=linearScale(xDomain,[plot.left,plot.right]);
const yScale=bandScale(rows.map((r)=>r.label),[plot.top,plot.bottom],0.4);
const gridPool=new Pool(groups.grid);
const axisPool=new Pool(groups.axis);
const fmt=formatters(grid,{kind:'linear',step:(xDomain.max-xDomain.min)/5||1});
for(const t of linearTicks(xDomain,5)){
const x=xScale.of(t);
attrs(gridPool.next('line',`${NS}__gridline`),{
x1:round(x),y1:round(plot.top),x2:round(x),y2:round(plot.bottom),stroke:'currentColor','stroke-opacity':0.1,
});
const tl=axisPool.next('text',`${NS}__tick`);
setText(tl,fmt.tick(t));
attrs(tl,{x:round(x),y:round(plot.bottom+fontSize+4),'text-anchor':'middle','font-size':fontSize});
}
const marks=new Pool(groups.marks);
const overlay=new Pool(groups.overlay);
const bw=yScale.bandwidth;
for(const r of rows){
const cy=yScale.of(r.label)+bw/2;
attrs(marks.next('path',`${NS}__dumbbell-bar ${NS}__mark`),{
d:path(['M',round(xScale.of(r.start)),round(cy),'L',round(xScale.of(r.end)),round(cy)]),
stroke:'currentColor','stroke-opacity':0.35,'stroke-width':2,fill:'none',
});
attrs(marks.next('circle',`${NS}__dumbbell-start ${NS}__mark`),{
cx:round(xScale.of(r.start)),cy:round(cy),r:4,fill:startColour,
});
attrs(marks.next('circle',`${NS}__dumbbell-end ${NS}__mark`),{
cx:round(xScale.of(r.end)),cy:round(cy),r:4,fill:endColour,
});
const label=overlay.next('text',`${NS}__dumbbell-label`);
setText(label,String(r.label));
attrs(label,{x:round(plot.left-4),y:round(cy),'text-anchor':'end','font-size':fontSize});
}
gridPool.finish();
axisPool.finish();
marks.finish();
overlay.finish();
return{rows:rows.length,legend:[]};
}
registerChartType('dumbbell',{draw:drawDumbbell,bind:bindDumbbell,freeform:true,labelled:false});
const __default=drawDumbbell;
});
const __entry=__req("packages/modules/chart-dumbbell/index.js");
export const
bindDumbbell=__entry.bindDumbbell,
drawDumbbell=__entry.drawDumbbell;
export default __entry.default;