/*!
 * Lattice Grid 1.34.1, chart-diverging module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-diverging/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"drawDiverging",{enumerable:true,get:function(){return drawDiverging;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
function drawDiverging(ctx){
const{
plot,bound,scheme,fontSize,groups,grid,helpers,
}=ctx;
const{
measureDomain,linearScale,bandScale,linearTicks,Pool,attrs,round,setText,
formatters,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const pos=sch.series(0);
const neg=sch.series(1);
const series=bound.series&&bound.series[0];
const points=(series&&series.points)||[];
if(!points.length)return{empty:true,legend:[]};
const values=points.map((p)=>p.y).filter((v)=>v!==null);
const reach=Math.max(1,...values.map((v)=>Math.abs(v)));
const xDomain=measureDomain([-reach,reach],{zero:true});
const xScale=linearScale(xDomain,[plot.left,plot.right]);
const yScale=bandScale(points.map((p)=>p.label),[plot.top,plot.bottom],0.3);
const zero=xScale.of(0);
const gridPool=new Pool(groups.grid);
const axisPool=new Pool(groups.axis);
const fmt=formatters(grid,{kind:'linear',step:reach/3||1});
for(const t of linearTicks(xDomain,5)){
const x=xScale.of(t);
attrs(gridPool.next('line',`${NS}__gridline`),{
x1:round(x),y1:round(plot.top),x2:round(x),y2:round(plot.bottom),
stroke:'currentColor','stroke-opacity':t===0?0.35:0.1,
});
const tl=axisPool.next('text',`${NS}__tick`);
setText(tl,fmt.tick(t));
attrs(tl,{x:round(x),y:round(plot.bottom+fontSize+4),'text-anchor':'middle','font-size':fontSize});
}
const marks=new Pool(groups.marks);
const overlay=new Pool(groups.overlay);
const bw=yScale.bandwidth;
for(const p of points){
if(p.y===null)continue;
const x=xScale.of(p.y);
const top=yScale.of(p.label);
attrs(marks.next('rect',`${NS}__diverging-bar ${NS}__mark`),{
x:round(Math.min(zero,x)),y:round(top),
width:round(Math.abs(x-zero)),height:round(bw),
fill:p.y>=0?pos:neg,'data-value':p.y,
});
const label=overlay.next('text',`${NS}__diverging-label`);
setText(label,String(p.label));
attrs(label,{
x:round(p.y>=0?zero-4:zero+4),y:round(top+bw/2),
'text-anchor':p.y>=0?'end':'start','font-size':fontSize,
});
}
gridPool.finish();
axisPool.finish();
marks.finish();
overlay.finish();
return{bars:points.length,legend:[]};
}
registerChartType('diverging',{draw:drawDiverging,freeform:true,labelled:false});
const __default=drawDiverging;
});
const __entry=__req("packages/modules/chart-diverging/index.js");
export const
drawDiverging=__entry.drawDiverging;
export default __entry.default;