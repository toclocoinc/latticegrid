/*!
 * Lattice Grid 1.33.0, chart-ridgeline module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-ridgeline/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"drawRidgeline",{enumerable:true,get:function(){return drawRidgeline;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
const STEPS=64;
const DEFAULT_OVERLAP=1.7;
function drawRidgeline(ctx){
const{
plot,bound,scheme,fontSize,groups,grid,spec,helpers,
}=ctx;
const{
valuesByCategory,density,measureDomain,linearScale,linearTicks,
Pool,attrs,path,round,setText,formatters,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const grouped=valuesByCategory(bound);
const categories=[...grouped.keys()];
if(!categories.length)return{empty:true,legend:[]};
const all=[];
for(const values of grouped.values())for(const n of values)all.push(n);
const domain=measureDomain(all,{zero:false});
const xScale=linearScale(domain,[plot.left,plot.right]);
const curves=categories.map((cat)=>density(grouped.get(cat),domain,STEPS));
let maxDensity=0;
for(const curve of curves)for(const p of curve)if(p.density>maxDensity)maxDensity=p.density;
maxDensity=maxDensity||1;
const rows=categories.length;
const rowStep=(plot.bottom-plot.top)/rows;
const overlap=spec&&Number.isFinite(spec.overlap)?spec.overlap:DEFAULT_OVERLAP;
const ridgeHeight=rowStep*overlap;
const gridPool=new Pool(groups.grid);
const axisPool=new Pool(groups.axis);
const fmt=formatters(grid,{kind:'linear',step:(domain.max-domain.min)/5||1});
for(const tick of linearTicks(domain,5)){
const x=xScale.of(tick);
attrs(gridPool.next('line',`${NS}__gridline`),{
x1:round(x),y1:round(plot.top),x2:round(x),y2:round(plot.bottom),
stroke:'currentColor','stroke-opacity':0.12,
});
const label=axisPool.next('text',`${NS}__tick`);
setText(label,fmt.tick(tick));
attrs(label,{
x:round(x),y:round(plot.bottom+fontSize+4),
'text-anchor':'middle','font-size':fontSize,
});
}
const marks=new Pool(groups.marks);
const overlayPool=new Pool(groups.overlay);
categories.forEach((cat,i)=>{
const baseline=plot.top+rowStep*(i+1);
const curve=curves[i];
const colour=sch.series(i);
const parts=['M',round(xScale.of(curve[0].at)),round(baseline)];
for(const p of curve){
parts.push('L',round(xScale.of(p.at)),round(baseline-(p.density/maxDensity)*ridgeHeight));
}
parts.push('L',round(xScale.of(curve[curve.length-1].at)),round(baseline),'Z');
attrs(marks.next('path',`${NS}__ridge ${NS}__mark`),{
d:path(parts),fill:colour,'fill-opacity':0.7,stroke:colour,'data-point':i,
});
attrs(marks.next('path',`${NS}__ridge-base`),{
d:path(['M',round(plot.left),round(baseline),'L',round(plot.right),round(baseline)]),
stroke:colour,'stroke-opacity':0.4,fill:'none',
});
const label=overlayPool.next('text',`${NS}__ridge-label`);
setText(label,String(cat));
attrs(label,{
x:round(plot.left-4),y:round(baseline-2),
'text-anchor':'end','font-size':fontSize,
});
});
gridPool.finish();
axisPool.finish();
marks.finish();
overlayPool.finish();
return{xScale,categories,plot,legend:[]};
}
registerChartType('ridgeline',{draw:drawRidgeline,labelled:false});
const __default=drawRidgeline;
});
const __entry=__req("packages/modules/chart-ridgeline/index.js");
export const
drawRidgeline=__entry.drawRidgeline;
export default __entry.default;