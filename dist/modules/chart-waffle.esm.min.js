/*!
 * Lattice Grid 1.34.0, chart-waffle module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-waffle/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"drawWaffle",{enumerable:true,get:function(){return drawWaffle;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
const DEFAULT_CELLS=100;
function allocate(values,cells){
const total=values.reduce((t,v)=>t+v,0)||1;
const exact=values.map((v)=>(v/total)*cells);
const floors=exact.map((e)=>Math.floor(e));
let used=floors.reduce((t,v)=>t+v,0);
const order=exact
.map((e,i)=>({i,frac:e-Math.floor(e)}))
.sort((a,b)=>b.frac-a.frac);
let k=0;
while(used<cells&&k<order.length){floors[order[k].i]+=1;used+=1;k+=1;}
return floors;
}
function drawWaffle(ctx){
const{
plot,bound,scheme,groups,spec,helpers,
}=ctx;
const{
Pool,attrs,round,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const series=bound.series&&bound.series[0];
const points=((series&&series.points)||[]).filter((p)=>p.y!==null&&p.y>0);
if(!points.length)return{empty:true,legend:[]};
const cells=spec&&spec.cells>0?Math.floor(spec.cells):DEFAULT_CELLS;
const cols=spec&&spec.columns>0?Math.floor(spec.columns):Math.ceil(Math.sqrt(cells));
const rows=Math.ceil(cells/cols);
const counts=allocate(points.map((p)=>p.y),cells);
const size=Math.max(2,Math.floor(Math.min(
(plot.right-plot.left)/cols,
(plot.bottom-plot.top)/rows,
)));
const gap=Math.max(1,Math.round(size*0.12));
const step=size+gap;
const cellCat=[];
counts.forEach((n,cat)=>{for(let i=0;i<n;i++)cellCat.push(cat);});
const marks=new Pool(groups.marks);
cellCat.forEach((cat,idx)=>{
const col=idx%cols;
const rowFromBottom=Math.floor(idx/cols);
const x=plot.left+col*step;
const y=plot.top+(rows-1-rowFromBottom)*step;
attrs(marks.next('rect',`${NS}__waffle-cell ${NS}__mark`),{
x:round(x),y:round(y),width:size,height:size,rx:1,
fill:sch.series(cat),'data-cat':cat,
});
});
marks.finish();
const legend=points.map((p,i)=>({key:p.label,label:`${p.label} (${counts[i]})`,index:i}));
return{cells:cellCat.length,counts,legend};
}
registerChartType('waffle',{draw:drawWaffle,freeform:true,labelled:false});
const __default=drawWaffle;
});
const __entry=__req("packages/modules/chart-waffle/index.js");
export const
drawWaffle=__entry.drawWaffle;
export default __entry.default;