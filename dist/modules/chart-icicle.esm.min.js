/*!
 * Lattice Grid 1.33.0, chart-icicle module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-icicle/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"drawIcicle",{enumerable:true,get:function(){return drawIcicle;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
function depthOf(node){
if(!node.children||!node.children.length)return 0;
let max=0;
for(const child of node.children)max=Math.max(max,depthOf(child));
return max+1;
}
function drawIcicle(ctx){
const{
plot,bound,scheme,fontSize,groups,helpers,
}=ctx;
const{
Pool,attrs,round,setText,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const root=bound.root;
if(!root||!root.children||!root.children.length)return{empty:true,legend:[]};
const levels=Math.max(1,depthOf(root));
const rowH=(plot.bottom-plot.top)/levels;
const marks=new Pool(groups.marks);
const overlay=new Pool(groups.overlay);
let drawn=0;
const layout=(node,x0,x1,depth,topIndex)=>{
const children=node.children||[];
const total=children.reduce((t,c)=>t+(c.total||0),0)||1;
let cursor=x0;
children.forEach((child,i)=>{
const w=((child.total||0)/total)*(x1-x0);
const left=cursor;
const right=cursor+w;
cursor=right;
const colourIndex=depth===1?i:topIndex;
const y=plot.top+(depth-1)*rowH;
attrs(marks.next('rect',`${NS}__icicle-cell ${NS}__mark`),{
x:round(left),y:round(y),width:round(Math.max(0,right-left-1)),height:round(Math.max(0,rowH-1)),
fill:sch.series(colourIndex),'fill-opacity':0.85,stroke:'currentColor','stroke-opacity':0.15,
'data-depth':depth,
});
drawn+=1;
if(w>fontSize*3){
const label=overlay.next('text',`${NS}__icicle-label`);
setText(label,String(child.label));
attrs(label,{x:round(left+3),y:round(y+fontSize),'font-size':fontSize});
}
if(child.children&&child.children.length)layout(child,left,right,depth+1,colourIndex);
});
};
layout(root,plot.left,plot.right,1,0);
marks.finish();
overlay.finish();
return{cells:drawn,levels,legend:[]};
}
registerChartType('icicle',{draw:drawIcicle,hierarchical:true,freeform:true,labelled:false});
const __default=drawIcicle;
});
const __entry=__req("packages/modules/chart-icicle/index.js");
export const
drawIcicle=__entry.drawIcicle;
export default __entry.default;