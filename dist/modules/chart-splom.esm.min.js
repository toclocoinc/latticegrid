/*!
 * Lattice Grid 1.34.0, chart-splom module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-splom/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"bindSplom",{enumerable:true,get:function(){return bindSplom;}});
Object.defineProperty(__exports,"drawSplom",{enumerable:true,get:function(){return drawSplom;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
const MAX_COLUMNS=6;
function bindSplom(grid,spec){
const columns=(Array.isArray(spec.columns)?spec.columns:[]).slice(0,MAX_COLUMNS);
const rows=[];
const n=grid.rows.count();
for(let i=0;i<n;i++){
const row=grid.rows.get(i);
if(!row||row.group)continue;
const record={__key:row.key};
let any=false;
for(const col of columns){
const value=Number(grid.rows.value(row.key,col));
record[col]=Number.isFinite(value)?value:null;
if(record[col]!==null)any=true;
}
if(any)rows.push(record);
}
return{rows,columns,empty:rows.length===0||columns.length<2};
}
function drawSplom(ctx){
const{
plot,bound,scheme,fontSize,groups,helpers,
}=ctx;
const{
extent,linearScale,Pool,attrs,round,setText,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const{rows,columns}=bound;
const size=columns.length;
const colour=sch.series(0);
const domains={};
for(const col of columns){
domains[col]=extent(rows.map((r)=>r[col]).filter((v)=>v!==null))||{min:0,max:1};
}
const cellW=(plot.right-plot.left)/size;
const cellH=(plot.bottom-plot.top)/size;
const pad=Math.max(4,Math.round(Math.min(cellW,cellH)*0.08));
const radius=Math.max(1,Math.min(2.5,cellW/60));
const framePool=new Pool(groups.grid);
const dotPool=new Pool(groups.marks);
const labelPool=new Pool(groups.overlay);
for(let r=0;r<size;r++){
for(let c=0;c<size;c++){
const left=plot.left+c*cellW;
const top=plot.top+r*cellH;
attrs(framePool.next('rect',`${NS}__splom-cell`),{
x:round(left+pad),y:round(top+pad),
width:round(cellW-pad*2),height:round(cellH-pad*2),
fill:'none',stroke:'currentColor','stroke-opacity':0.15,
});
if(r===c){
const label=labelPool.next('text',`${NS}__splom-diag`);
setText(label,String(columns[r]));
attrs(label,{
x:round(left+cellW/2),y:round(top+cellH/2),
'text-anchor':'middle','font-size':fontSize,
});
continue;
}
const xCol=columns[c];
const yCol=columns[r];
const xScale=linearScale(domains[xCol],[left+pad,left+cellW-pad]);
const yScale=linearScale(domains[yCol],[top+cellH-pad,top+pad]);
for(const row of rows){
if(row[xCol]===null||row[yCol]===null)continue;
attrs(dotPool.next('circle',`${NS}__splom-dot ${NS}__mark`),{
cx:round(xScale.of(row[xCol])),cy:round(yScale.of(row[yCol])),
r:radius,fill:colour,'fill-opacity':0.5,
});
}
}
}
framePool.finish();
dotPool.finish();
labelPool.finish();
return{columns,cells:size*size,legend:[]};
}
registerChartType('splom',{draw:drawSplom,bind:bindSplom,freeform:true,labelled:false});
const __default=drawSplom;
});
const __entry=__req("packages/modules/chart-splom/index.js");
export const
bindSplom=__entry.bindSplom,
drawSplom=__entry.drawSplom;
export default __entry.default;