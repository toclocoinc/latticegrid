/*!
 * Lattice Grid 1.33.0, chart-slope module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-slope/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"drawSlope",{enumerable:true,get:function(){return drawSlope;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
function drawSlope(ctx){
const{
plot,bound,scheme,fontSize,groups,helpers,
}=ctx;
const{
measureDomain,linearScale,bandScale,Pool,attrs,path,round,setText,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const labels=bound.labels||[];
if(labels.length<2)return{empty:true,legend:[]};
const values=[];
for(const s of bound.series)for(const p of s.points)if(p.y!==null)values.push(p.y);
const yDomain=measureDomain(values,{zero:false});
const yScale=linearScale(yDomain,[plot.bottom,plot.top]);
const xScale=bandScale(labels,[plot.left,plot.right],0.5);
const colX=labels.map((l)=>xScale.of(l)+xScale.bandwidth/2);
const axisPool=new Pool(groups.axis);
labels.forEach((label,i)=>{
const t=axisPool.next('text',`${NS}__tick`);
setText(t,String(label));
attrs(t,{x:round(colX[i]),y:round(plot.bottom+fontSize+4),'text-anchor':'middle','font-size':fontSize});
});
const marks=new Pool(groups.marks);
const overlay=new Pool(groups.overlay);
bound.series.forEach((s,i)=>{
const colour=sch.series(i);
const parts=[];
s.points.forEach((p,j)=>{
if(p.y===null)return;
parts.push(parts.length?'L':'M',round(colX[j]),round(yScale.of(p.y)));
});
if(parts.length>=3){
attrs(marks.next('path',`${NS}__slope-line ${NS}__mark`),{
d:path(parts),stroke:colour,'stroke-width':2,fill:'none','data-series':i,
});
}
s.points.forEach((p,j)=>{
if(p.y===null)return;
attrs(marks.next('circle',`${NS}__slope-dot ${NS}__mark`),{
cx:round(colX[j]),cy:round(yScale.of(p.y)),r:3,fill:colour,
});
});
const last=[...s.points].reverse().find((p)=>p.y!==null);
if(last){
const t=overlay.next('text',`${NS}__slope-label`);
setText(t,String(s.label||s.key));
attrs(t,{x:round(plot.right+2),y:round(yScale.of(last.y)),'font-size':fontSize});
}
});
axisPool.finish();
marks.finish();
overlay.finish();
return{series:bound.series.length,legend:[]};
}
registerChartType('slope',{draw:drawSlope,labelled:false});
const __default=drawSlope;
});
const __entry=__req("packages/modules/chart-slope/index.js");
export const
drawSlope=__entry.drawSlope;
export default __entry.default;