/*!
 * Lattice Grid 1.66.0, kpi module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
__def("packages/core/src/internal/util.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"VERSION",{enumerable:true,get:function(){return VERSION;}});
Object.defineProperty(__exports,"reportedWarnings",{enumerable:true,get:function(){return reportedWarnings;}});
Object.defineProperty(__exports,"warnOnce",{enumerable:true,get:function(){return warnOnce;}});
Object.defineProperty(__exports,"infoOnce",{enumerable:true,get:function(){return infoOnce;}});
Object.defineProperty(__exports,"resetWarnings",{enumerable:true,get:function(){return resetWarnings;}});
Object.defineProperty(__exports,"fail",{enumerable:true,get:function(){return fail;}});
Object.defineProperty(__exports,"invariant",{enumerable:true,get:function(){return invariant;}});
Object.defineProperty(__exports,"DEV",{enumerable:true,get:function(){return DEV;}});
Object.defineProperty(__exports,"isObject",{enumerable:true,get:function(){return isObject;}});
Object.defineProperty(__exports,"isFunction",{enumerable:true,get:function(){return isFunction;}});
Object.defineProperty(__exports,"isNil",{enumerable:true,get:function(){return isNil;}});
Object.defineProperty(__exports,"isBlank",{enumerable:true,get:function(){return isBlank;}});
Object.defineProperty(__exports,"isCtor",{enumerable:true,get:function(){return isCtor;}});
Object.defineProperty(__exports,"pathGetter",{enumerable:true,get:function(){return pathGetter;}});
Object.defineProperty(__exports,"pathSetter",{enumerable:true,get:function(){return pathSetter;}});
Object.defineProperty(__exports,"getPath",{enumerable:true,get:function(){return getPath;}});
Object.defineProperty(__exports,"setPath",{enumerable:true,get:function(){return setPath;}});
Object.defineProperty(__exports,"humanise",{enumerable:true,get:function(){return humanise;}});
Object.defineProperty(__exports,"escapeHtml",{enumerable:true,get:function(){return escapeHtml;}});
Object.defineProperty(__exports,"titleCase",{enumerable:true,get:function(){return titleCase;}});
Object.defineProperty(__exports,"expand",{enumerable:true,get:function(){return expand;}});
Object.defineProperty(__exports,"toArray",{enumerable:true,get:function(){return toArray;}});
Object.defineProperty(__exports,"merge",{enumerable:true,get:function(){return merge;}});
Object.defineProperty(__exports,"mergeRow",{enumerable:true,get:function(){return mergeRow;}});
Object.defineProperty(__exports,"Lru",{enumerable:true,get:function(){return Lru;}});
Object.defineProperty(__exports,"collator",{enumerable:true,get:function(){return collator;}});
Object.defineProperty(__exports,"defaultCompare",{enumerable:true,get:function(){return defaultCompare;}});
Object.defineProperty(__exports,"now",{enumerable:true,get:function(){return now;}});
Object.defineProperty(__exports,"nextFrame",{enumerable:true,get:function(){return nextFrame;}});
Object.defineProperty(__exports,"cancelFrame",{enumerable:true,get:function(){return cancelFrame;}});
Object.defineProperty(__exports,"frameBatched",{enumerable:true,get:function(){return frameBatched;}});
Object.defineProperty(__exports,"settleDebounce",{enumerable:true,get:function(){return settleDebounce;}});
Object.defineProperty(__exports,"whenIdle",{enumerable:true,get:function(){return whenIdle;}});
Object.defineProperty(__exports,"uid",{enumerable:true,get:function(){return uid;}});
const STAMPED_VERSION="1.66.0";
async function resolveVersion(){
if(STAMPED_VERSION!=='0.0.0-source')return STAMPED_VERSION;
return STAMPED_VERSION;
}
const VERSION="1.66.0";
const warned=new Set();
const WARNED_LIMIT=2000;
function rememberWarned(key){
warned.add(key);
if(warned.size>WARNED_LIMIT){
const oldest=warned.values().next().value;
if(oldest!==undefined)warned.delete(oldest);
}
}
const reported=[];
const REPORT_LIMIT=500;
function record(key,level,message){
reported.push({
key,
level,
message:message.map((m)=>(typeof m==='string'?m:safeString(m))).join(' '),
at:Date.now(),
});
if(reported.length>REPORT_LIMIT)reported.shift();
}
function safeString(value){
if(value instanceof Error)return value.message;
try{return JSON.stringify(value);}catch{return String(value);}
}
function reportedWarnings(){return reported.map((r)=>({...r}));}
function warnOnce(key,...message){
if(warned.has(key))return;
rememberWarned(key);
record(key,'warn',message);
console.warn('[lattice]',...message);
}
function infoOnce(key,...message){
if(warned.has(key))return;
rememberWarned(key);
record(key,'info',message);
console.info('[lattice]',...message);
}
function resetWarnings(){
warned.clear();
reported.length=0;
}
function fail(message,extra){
const err=new Error(`[lattice] ${message}`);
if(extra!==undefined)err.cause=extra;
throw err;
}
function invariant(condition,message){
if(!condition)fail(message);
}
const DEV=(()=>{
try{
return!(typeof process!=='undefined'&&process.env
&&process.env.NODE_ENV==='production');
}catch{
return true;
}
})();
function isObject(v){
return v!==null&&typeof v==='object'&&!Array.isArray(v);
}
function isFunction(v){
return typeof v==='function';
}
function isNil(v){
return v===null||v===undefined;
}
function isBlank(v){
return v===null||v===undefined||v==='';
}
function isCtor(v){
if(typeof v!=='function')return false;
if(/^class[\s{]/.test(Function.prototype.toString.call(v)))return true;
return!!(v.prototype&&Object.getOwnPropertyNames(v.prototype).length>1);
}
const pathCache=new Map();
function pathGetter(path){
let fn=pathCache.get(path);
if(fn)return fn;
if(!path.includes('.')){
fn=(o)=>(o==null?undefined:o[path]);
}else{
const parts=path.split('.');
const n=parts.length;
fn=(o)=>{
let cur=o;
for(let i=0;i<n;i++){
if(cur==null)return undefined;
cur=cur[parts[i]];
}
return cur;
};
}
pathCache.set(path,fn);
return fn;
}
const setterCache=new Map();
function pathSetter(path){
let fn=setterCache.get(path);
if(fn)return fn;
if(!path.includes('.')){
fn=(o,v)=>{if(o!=null)o[path]=v;};
}else{
const parts=path.split('.');
const last=parts.length-1;
fn=(o,v)=>{
let cur=o;
for(let i=0;i<last;i++){
if(cur==null)return;
const k=parts[i];
if(cur[k]==null)cur[k]={};
cur=cur[k];
}
if(cur!=null)cur[parts[last]]=v;
};
}
setterCache.set(path,fn);
return fn;
}
function getPath(obj,path){
return pathGetter(path)(obj);
}
function setPath(obj,path,value){
pathSetter(path)(obj,value);
}
function humanise(field){
if(!field)return'';
const leaf=field.includes('.')?field.slice(field.lastIndexOf('.')+1):field;
return leaf
.replace(/[_-]+/g,' ')
.replace(/([a-z0-9])([A-Z])/g,'$1 $2')
.replace(/([A-Z]+)([A-Z][a-z])/g,'$1 $2')
.replace(/\s+/g,' ')
.trim()
.replace(/^./,(c)=>c.toUpperCase());
}
const ESCAPES={'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'};
function escapeHtml(s){
const str=s==null?'':String(s);
return/[&<>"']/.test(str)?str.replace(/[&<>"']/g,(c)=>ESCAPES[c]):str;
}
function titleCase(s){
return String(s).replace(/\w\S*/g,(t)=>t[0].toUpperCase()+t.slice(1).toLowerCase());
}
function expand(value,key,whenTrue){
if(value===undefined)return undefined;
if(value===true)return{enabled:true,...whenTrue};
if(value===false)return{enabled:false};
if(isObject(value))return value;
return{[key]:value,enabled:true};
}
function toArray(v){
if(v===undefined||v===null)return[];
return Array.isArray(v)?v:[v];
}
const MERGE_FORBIDDEN_KEYS=Object.freeze(new Set(['__proto__','constructor','prototype']));
function merge(a,b){
if(!isObject(a))return isObject(b)?{...b}:b;
if(!isObject(b))return b===undefined?a:b;
const out={...a};
for(const k of Object.keys(b)){
if(MERGE_FORBIDDEN_KEYS.has(k))continue;
const bv=b[k];
if(bv===undefined)continue;
out[k]=isObject(bv)&&isObject(out[k])?merge(out[k],bv):bv;
}
return out;
}
function mergeRow(previous,patch){
if(!isObject(previous)||!isObject(patch)||previous===patch)return patch;
const out=Object.create(Object.getPrototypeOf(previous));
Object.assign(out,previous,patch);
return out;
}
class Lru{
#max;
#map=new Map();
#onEvict;
constructor(max=256,onEvict=null){
this.#max=max;
this.#onEvict=onEvict;
}
get size(){
return this.#map.size;
}
get max(){
return this.#max;
}
set max(v){
this.#max=v;
this.#trim();
}
has(k){
return this.#map.has(k);
}
get(k){
const m=this.#map;
if(!m.has(k))return undefined;
const v=m.get(k);
m.delete(k);
m.set(k,v);
return v;
}
peek(k){
return this.#map.get(k);
}
set(k,v){
const m=this.#map;
if(m.has(k))m.delete(k);
m.set(k,v);
this.#trim();
return v;
}
delete(k){
const v=this.#map.get(k);
if(this.#map.delete(k)&&this.#onEvict)this.#onEvict(v,k);
return v;
}
clear(){
if(this.#onEvict)for(const[k,v]of this.#map)this.#onEvict(v,k);
this.#map.clear();
}
keys(){
return this.#map.keys();
}
values(){
return this.#map.values();
}
#trim(){
const m=this.#map;
while(m.size>this.#max){
const oldest=m.keys().next().value;
const v=m.get(oldest);
m.delete(oldest);
if(this.#onEvict)this.#onEvict(v,oldest);
}
}
}
const collators=new Map();
function collator(locale,opts){
const key=`${locale||''}|${opts?JSON.stringify(opts):''}`;
let c=collators.get(key);
if(!c){
c=new Intl.Collator(locale||undefined,{
numeric:true,sensitivity:'variant',...opts,
});
collators.set(key,c);
}
return c;
}
function defaultCompare(a,b){
if(a===b)return 0;
if(a===null||a===undefined)return 1;
if(b===null||b===undefined)return-1;
if(typeof a==='number'&&typeof b==='number'){
if(Number.isNaN(a))return Number.isNaN(b)?0:1;
if(Number.isNaN(b))return-1;
return a<b?-1:a>b?1:0;
}
const sa=String(a);
const sb=String(b);
return sa<sb?-1:sa>sb?1:0;
}
function now(){
return typeof performance!=='undefined'&&performance.now
?performance.now()
:Date.now();
}
const hasRaf=typeof requestAnimationFrame==='function';
function nextFrame(fn){
if(hasRaf)return requestAnimationFrame(fn);
return setTimeout(()=>fn(now()),16);
}
function cancelFrame(handle){
if(handle==null)return;
if(hasRaf)cancelAnimationFrame(handle);
else clearTimeout(handle);
}
function frameBatched(fn){
let handle=null;
let lastArgs=null;
const run=()=>{
handle=null;
const a=lastArgs;
lastArgs=null;
fn(...(a||[]));
};
const wrapped=(...args)=>{
lastArgs=args;
if(handle===null)handle=nextFrame(run);
};
wrapped.cancel=()=>{
cancelFrame(handle);
handle=null;
lastArgs=null;
};
wrapped.flush=()=>{
if(handle!==null){
cancelFrame(handle);
run();
}
};
return wrapped;
}
function settleDebounce(fn,waitMs){
let timer=null;
let held=null;
const trailing=()=>{
timer=null;
if(held===null)return;
const args=held;
held=null;
fn(...args);
arm();
};
const arm=()=>{
timer=setTimeout(trailing,waitMs);
if(typeof timer?.unref==='function')timer.unref();
};
const wrapped=(...args)=>{
if(timer===null){
fn(...args);
arm();
}else{
held=args;
clearTimeout(timer);
arm();
}
};
wrapped.flush=()=>{
if(timer!==null)clearTimeout(timer);
timer=null;
if(held===null)return;
const args=held;
held=null;
fn(...args);
};
wrapped.cancel=()=>{
if(timer!==null)clearTimeout(timer);
timer=null;
held=null;
};
wrapped.pending=()=>timer!==null||held!==null;
return wrapped;
}
function whenIdle(fn,timeout=50){
if(typeof requestIdleCallback==='function'){
return requestIdleCallback(fn,{timeout});
}
return setTimeout(()=>fn({timeRemaining:()=>0,didTimeout:true}),1);
}
let idSeq=0;
function uid(prefix='l'){
return`${prefix}${(++idSeq).toString(36)}`;
}
});
__def("packages/modules/kpi/aggregate.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"AGGREGATIONS",{enumerable:true,get:function(){return AGGREGATIONS;}});
Object.defineProperty(__exports,"makeAccumulator",{enumerable:true,get:function(){return makeAccumulator;}});
Object.defineProperty(__exports,"accessorFor",{enumerable:true,get:function(){return accessorFor;}});
Object.defineProperty(__exports,"bandFor",{enumerable:true,get:function(){return bandFor;}});
Object.defineProperty(__exports,"UNKNOWN_STATUS",{enumerable:true,get:function(){return UNKNOWN_STATUS;}});
Object.defineProperty(__exports,"statusFor",{enumerable:true,get:function(){return statusFor;}});
Object.defineProperty(__exports,"barBounds",{enumerable:true,get:function(){return barBounds;}});
Object.defineProperty(__exports,"barPercent",{enumerable:true,get:function(){return barPercent;}});
Object.defineProperty(__exports,"formatValue",{enumerable:true,get:function(){return formatValue;}});
const __m0=__req("packages/core/src/internal/util.js");
const isNil=__m0["isNil"];
const isFunction=__m0["isFunction"];
const pathGetter=__m0["pathGetter"];
const AGGREGATIONS=Object.freeze([
'sum','avg','min','max','count','countDistinct','custom',
]);
function toNumber(v){
if(isNil(v)||v==='')return null;
const n=typeof v==='number'?v:Number(v);
return Number.isFinite(n)?n:null;
}
function makeAccumulator(kind){
switch(kind){
case'count':{
let n=0;
return{
add(){n+=1;},
remove(){n-=1;},
value(){return n;},
};
}
case'sum':{
let s=0;
let n=0;
return{
add(v){const x=toNumber(v);if(x!==null){s+=x;n+=1;}},
remove(v){const x=toNumber(v);if(x!==null){s-=x;n-=1;}},
value(){return n>0?s:0;},
};
}
case'avg':{
let s=0;
let n=0;
return{
add(v){const x=toNumber(v);if(x!==null){s+=x;n+=1;}},
remove(v){const x=toNumber(v);if(x!==null){s-=x;n-=1;}},
value(){return n>0?s/n:null;},
};
}
case'countDistinct':{
const counts=new Map();
return{
add(v){if(isNil(v))return;counts.set(v,(counts.get(v)||0)+1);},
remove(v){
if(isNil(v))return;
const c=counts.get(v);
if(!c)return;
if(c===1)counts.delete(v);else counts.set(v,c-1);
},
value(){return counts.size;},
};
}
case'min':
case'max':
return extremaAccumulator(kind==='max');
default:
return null;
}
}
function extremaAccumulator(isMax){
const counts=new Map();
let cached=null;
let dirty=false;
const better=(a,b)=>(isMax?a>b:a<b);
const rescan=()=>{
let best=null;
for(const k of counts.keys())if(best===null||better(k,best))best=k;
return best;
};
return{
add(v){
const x=toNumber(v);
if(x===null)return;
counts.set(x,(counts.get(x)||0)+1);
if(!dirty&&(cached===null||better(x,cached)))cached=x;
},
remove(v){
const x=toNumber(v);
if(x===null)return;
const c=counts.get(x);
if(!c)return;
if(c===1){
counts.delete(x);
if(x===cached)dirty=true;
}else{
counts.set(x,c-1);
}
},
value(){
if(counts.size===0)return null;
if(dirty){cached=rescan();dirty=false;}
return cached;
},
};
}
function accessorFor(field){
if(isFunction(field))return field;
if(typeof field==='string'&&field)return pathGetter(field);
return()=>undefined;
}
function bandFor(value,spec){
if(value===null||value===undefined||Number.isNaN(value))return null;
if(Array.isArray(spec.bands)){
for(const b of spec.bands){
const lo=isNil(b.min)?-Infinity:b.min;
const hi=isNil(b.max)?Infinity:b.max;
if(value>=lo&&value<hi)return b.status??null;
}
return null;
}
const t=spec.thresholds;
if(!t||isNil(t.warn)||isNil(t.critical))return null;
const lowerIsBetter=t.direction==='lowerIsBetter';
if(lowerIsBetter){
if(value<=t.warn)return'good';
if(value<=t.critical)return'warn';
return'critical';
}
if(value>=t.warn)return'good';
if(value>=t.critical)return'warn';
return'critical';
}
const UNKNOWN_STATUS='unknown';
function statusFor(value,spec,hasData){
if(!hasData)return UNKNOWN_STATUS;
return bandFor(value,spec);
}
function barBounds(spec){
if(!spec)return null;
const points=[];
let open=false;
const point=(v)=>{
if(isNil(v))return false;
const n=Number(v);
if(!Number.isFinite(n))return false;
points.push(n);
return true;
};
if(Array.isArray(spec.bands)&&spec.bands.length){
for(const b of spec.bands||[]){
if(!point(b&&b.min))open=true;
if(!point(b&&b.max))open=true;
}
}else if(spec.thresholds&&!isNil(spec.thresholds.warn)&&!isNil(spec.thresholds.critical)){
point(spec.thresholds.warn);
point(spec.thresholds.critical);
open=true;
}else{
open=true;
}
point(spec.target);
if(open)points.push(0);
if(!points.length)return null;
const lo=Math.min(...points);
const hi=Math.max(...points);
if(!(hi>lo))return null;
return{lo,hi};
}
function barPercent(value,bounds){
if(!bounds)return null;
if(isNil(value))return null;
const n=Number(value);
if(!Number.isFinite(n))return null;
const span=bounds.hi-bounds.lo;
if(!span)return null;
return Math.max(0,Math.min(1,(n-bounds.lo)/span))*100;
}
function formatValue(value,format,nullText='—'){
if(value===null||value===undefined||Number.isNaN(value))return nullText;
const spec=typeof format==='string'?{type:format}:(format||{});
const type=spec.type||'number';
const locale=spec.locale||undefined;
const opts={};
if(type==='currency'){opts.style='currency';opts.currency=spec.currency||'USD';}
else if(type==='percent')opts.style='percent';
else if(type==='compact')opts.notation='compact';
if(!isNil(spec.decimals)){opts.minimumFractionDigits=spec.decimals;opts.maximumFractionDigits=spec.decimals;}
try{
return new Intl.NumberFormat(locale,opts).format(value);
}catch{
return String(value);
}
}
});
__def("packages/core/src/source/tree.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"SYNTHETIC",{enumerable:true,get:function(){return SYNTHETIC;}});
Object.defineProperty(__exports,"buildFromPath",{enumerable:true,get:function(){return buildFromPath;}});
Object.defineProperty(__exports,"buildFromParentKey",{enumerable:true,get:function(){return buildFromParentKey;}});
Object.defineProperty(__exports,"assignLevels",{enumerable:true,get:function(){return assignLevels;}});
Object.defineProperty(__exports,"resolveTree",{enumerable:true,get:function(){return resolveTree;}});
Object.defineProperty(__exports,"buildTree",{enumerable:true,get:function(){return buildTree;}});
Object.defineProperty(__exports,"indexTree",{enumerable:true,get:function(){return indexTree;}});
const __m0=__req("packages/core/src/internal/util.js");
const isFunction=__m0["isFunction"];
const warnOnce=__m0["warnOnce"];
const SEP='\u001f';
const SYNTHETIC='\u0000tree:';
function node(physical,key){
return{physical,key,level:0,children:[],value:undefined};
}
function buildFromPath(order,ctx){
const roots=[];
const nodes=new Map();
for(let i=0;i<order.length;i++){
const physical=order[i];
const raw=ctx.pathOf(physical);
const path=Array.isArray(raw)?raw.map((p)=>String(p)):[];
if(!path.length){
const own=node(physical,ctx.keyOf(physical));
roots.push(own);
continue;
}
let parent=null;
for(let level=0;level<path.length;level++){
const at=path.slice(0,level+1).join(SEP);
const leaf=level===path.length-1;
let found=nodes.get(at);
if(found&&leaf&&found.physical===-1){
found.physical=physical;
found.key=ctx.keyOf(physical);
}
if(!found){
found=node(leaf?physical:-1,leaf?ctx.keyOf(physical):SYNTHETIC+at);
found.level=level;
found.value=path[level];
nodes.set(at,found);
if(parent)parent.children.push(found);else roots.push(found);
}
parent=found;
}
}
return{roots,nodes};
}
function buildFromParentKey(order,ctx){
const byKey=new Map();
const pending=[];
for(let i=0;i<order.length;i++){
const physical=order[i];
const key=ctx.keyOf(physical);
const own=node(physical,key);
byKey.set(key,own);
const parent=ctx.parentOf(physical);
if(parent!==null&&parent!==undefined&&parent!==''){
pending.push({node:own,parent:String(parent)});
}
}
let orphanBucket=null;
const placed=new Map();
for(const{node:child,parent}of pending){
const found=byKey.get(parent);
if(found){placed.set(child.key,found);continue;}
if(ctx.orphans&&ctx.orphans!=='root'){
if(!orphanBucket){
orphanBucket=node(-1,`${SYNTHETIC}orphans`);
orphanBucket.value=ctx.orphans;
}
placed.set(child.key,orphanBucket);
}
}
const roots=[];
for(const own of byKey.values()){
const parent=placed.get(own.key);
if(parent)parent.children.push(own);
else roots.push(own);
}
if(orphanBucket&&orphanBucket.children.length)roots.push(orphanBucket);
return{roots:breakCycles(roots,byKey,ctx.orphans,orphanBucket),nodes:byKey};
}
function breakCycles(roots,byKey,orphans,bucket){
const reachable=new Set();
const stack=[...roots];
while(stack.length){
const current=stack.pop();
if(reachable.has(current.key))continue;
reachable.add(current.key);
for(const child of current.children)stack.push(child);
}
const stranded=[];
for(const own of byKey.values()){
if(!reachable.has(own.key))stranded.push(own);
}
if(!stranded.length)return roots;
const named=!!orphans&&orphans!=='root';
warnOnce(
'tree.cycle',
`tree.parentKey produced ${stranded.length} row(s) inside a parent cycle; `
+(named?`they are shown under "${orphans}".`:'they are shown at the root.'),
);
for(const own of stranded)own.children=own.children.filter((c)=>reachable.has(c.key));
if(!named)return[...roots,...stranded];
let holder=bucket;
const out=[...roots];
if(!holder){
holder=node(-1,`${SYNTHETIC}orphans`);
holder.value=orphans;
out.push(holder);
}
holder.children.push(...stranded);
return out;
}
function assignLevels(roots){
let deepest=0;
const walk=(list,level)=>{
for(const own of list){
own.level=level;
if(level>deepest)deepest=level;
if(own.children.length)walk(own.children,level+1);
}
};
walk(roots,0);
return deepest;
}
function resolveTree(config,rows){
if(!config||typeof config!=='object')return null;
const path=config.path;
const parentKey=config.parentKey;
const declaresChildren=isFunction(config.hasChildren)
?(physical)=>!!config.hasChildren(rows.dataAt(physical))
:null;
if(isFunction(path)){
return{
mode:'path',
pathOf:(physical)=>path(rows.dataAt(physical)),
keyOf:rows.keyOf,
declaresChildren,
};
}
if(parentKey!==null&&parentKey!==undefined){
const read=isFunction(parentKey)
?parentKey
:(data)=>(data==null?undefined:data[parentKey]);
return{
mode:'parentKey',
parentOf:(physical)=>read(rows.dataAt(physical)),
keyOf:rows.keyOf,
orphans:config.orphans||'root',
declaresChildren,
};
}
warnOnce(
'tree.shape',
'tree is configured with neither `path` nor `parentKey`; rows stay flat.',
);
return null;
}
function buildTree(order,tree){
const built=tree.mode==='path'
?buildFromPath(order,tree)
:buildFromParentKey(order,tree);
const depth=assignLevels(built.roots);
return{...built,depth};
}
function indexTree(roots,declaresChildren){
const meta=new Map();
const walk=(list)=>{
for(let i=0;i<list.length;i++){
const own=list[i];
if(own.physical>=0){
const declared=declaresChildren?declaresChildren(own.physical):false;
const lazy=declared&&own.children.length===0;
meta.set(own.physical,{
level:own.level,
posinset:i+1,
setsize:list.length,
hasChildren:own.children.length>0||declared,
lazy,
});
}
if(own.children.length)walk(own.children);
}
};
walk(roots);
return meta;
}
});
__def("packages/modules/kpi/tree.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"DEFAULT_SEPARATOR",{enumerable:true,get:function(){return DEFAULT_SEPARATOR;}});
Object.defineProperty(__exports,"worseOf",{enumerable:true,get:function(){return worseOf;}});
Object.defineProperty(__exports,"resolveHierarchy",{enumerable:true,get:function(){return resolveHierarchy;}});
Object.defineProperty(__exports,"decorateNodes",{enumerable:true,get:function(){return decorateNodes;}});
Object.defineProperty(__exports,"allNodes",{enumerable:true,get:function(){return allNodes;}});
Object.defineProperty(__exports,"visibleNodes",{enumerable:true,get:function(){return visibleNodes;}});
Object.defineProperty(__exports,"branchKeys",{enumerable:true,get:function(){return branchKeys;}});
const __m0=__req("packages/core/src/internal/util.js");
const isFunction=__m0["isFunction"];
const isObject=__m0["isObject"];
const warnOnce=__m0["warnOnce"];
const __m1=__req("packages/core/src/source/tree.js");
const resolveTree=__m1["resolveTree"];
const buildTree=__m1["buildTree"];
const DEFAULT_SEPARATOR='.';
const SEVERITY=Object.freeze({good:1,warn:2,critical:3});
const BY_RANK=Object.freeze([null,'good','warn','critical']);
function worseOf(a,b){
const rank=Math.max(SEVERITY[a]||0,SEVERITY[b]||0);
return BY_RANK[rank];
}
function resolveHierarchy(config,tiles){
if(config===false||!tiles.length)return null;
const cfg=isObject(config)?config:{};
const order=tiles.map((_,i)=>i);
const keyOf=(physical)=>tiles[physical].id;
if(isFunction(cfg.path)||(cfg.parentKey!==null&&cfg.parentKey!==undefined)){
const resolved=resolveTree(cfg,{dataAt:(physical)=>tiles[physical].spec,keyOf});
if(!resolved)return null;
return placed({...buildTree(order,resolved),mode:resolved.mode},tiles);
}
const sep=typeof cfg.separator==='string'&&cfg.separator?cfg.separator:DEFAULT_SEPARATOR;
if(!tiles.some((t)=>t.id.includes(sep)))return null;
const resolved=resolveTree(
{path:(id)=>String(id).split(sep)},
{dataAt:(physical)=>tiles[physical].id,keyOf},
);
return placed({...buildTree(order,resolved),mode:'id'},tiles);
}
function placed(built,tiles){
const seen=new Set();
const taken=new Set(tiles.map((t)=>t.id));
const walk=(list,prefix)=>{
for(const node of list){
const segment=node.physical>=0
?node.key
:(node.value===null||node.value===undefined?'':String(node.value));
const path=`${prefix}/${segment}`;
if(node.physical>=0)seen.add(node.physical);
else if(!taken.has(path)){node.key=path;taken.add(path);}
walk(node.children,path);
}
};
walk(built.roots,'');
const lost=tiles.filter((_,i)=>!seen.has(i)).map((t)=>t.id);
if(lost.length){
warnOnce('kpi:tree:unplaced',
`${lost.length} KPI tile(s) are not on the tree and will not render: ${lost.join(', ')}. `
+'Two tiles cannot share one path; a path names the tile\'s own place, last segment included.');
}
return built;
}
function labelFor(tile,segment){
if(tile&&tile.spec&&tile.spec.label!==null&&tile.spec.label!==undefined){
return String(tile.spec.label);
}
if(segment!==null&&segment!==undefined)return String(segment);
return tile?tile.label:'';
}
function decorateNodes(hierarchy,tiles,models,expanded){
const build=(raw,level,at,of)=>{
const tile=raw.physical>=0?tiles[raw.physical]:null;
const model=tile?models.get(tile.id):null;
const children=raw.children.map((child,i)=>build(child,level+1,i,raw.children.length));
const own=model?model.status:null;
let rollup=SEVERITY[own]?own:null;
let unknown=own==='unknown'?1:0;
let items=model?1:0;
for(const child of children){
rollup=worseOf(rollup,child.rollup);
unknown+=child.unknown;
items+=child.items;
}
return{
key:raw.key,
id:model?model.id:null,
label:labelFor(tile,raw.value),
level,
posinset:at+1,
setsize:of,
hasChildren:children.length>0,
expanded:children.length>0&&expanded.has(raw.key),
children,
tile:model,
value:model?model.value:null,
formatted:model?model.formatted:null,
status:own??null,
rollup,
unknown,
items,
};
};
return hierarchy.roots.map((raw,i)=>build(raw,0,i,hierarchy.roots.length));
}
function allNodes(roots){
const out=[];
const walk=(list)=>{
for(const node of list){out.push(node);walk(node.children);}
};
walk(roots);
return out;
}
function visibleNodes(roots){
const out=[];
const walk=(list)=>{
for(const node of list){
out.push(node);
if(node.expanded)walk(node.children);
}
};
walk(roots);
return out;
}
function branchKeys(roots){
const out=[];
const walk=(list)=>{
for(const node of list){
if(node.children.length){out.push(node.key);walk(node.children);}
}
};
walk(roots);
return out;
}
});
__def("packages/modules/kpi/messages.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"FALLBACK_EN",{enumerable:true,get:function(){return FALLBACK_EN;}});
Object.defineProperty(__exports,"resolveMessages",{enumerable:true,get:function(){return resolveMessages;}});
Object.defineProperty(__exports,"fallbackT",{enumerable:true,get:function(){return fallbackT;}});
const __m0=__req("packages/core/src/internal/util.js");
const isFunction=__m0["isFunction"];
const isObject=__m0["isObject"];
const FALLBACK_EN=Object.freeze({
'kpi.status.good':'good',
'kpi.status.warn':'warn',
'kpi.status.critical':'critical',
'kpi.status.unknown':'No data',
'kpi.node.item':'{count} item',
'kpi.node.items':'{count} items',
'kpi.node.worst':'worst status {status}',
'kpi.node.unknown':'{count} unknown',
});
function format(template,params){
if(!params)return template;
return template.replace(/\{(\w+)\}/g,(whole,name)=>(name in params?String(params[name]):whole));
}
function resolveMessages(cfg){
const host=(isObject(cfg.messages)&&isFunction(cfg.messages.t)&&cfg.messages)
||(isObject(cfg.grid)&&isObject(cfg.grid.messages)&&isFunction(cfg.grid.messages.t)&&cfg.grid.messages)
||null;
return(key,params)=>{
if(host){
let text;
try{text=host.t(key,params);}catch{text=null;}
if(typeof text==='string'&&text&&text!==key)return text;
}
return format(FALLBACK_EN[key]||key,params);
};
}
const fallbackT=resolveMessages({});
});
__def("packages/modules/kpi/view.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"NS",{enumerable:true,get:function(){return NS;}});
Object.defineProperty(__exports,"EMPTY_TEXT",{enumerable:true,get:function(){return EMPTY_TEXT;}});
Object.defineProperty(__exports,"Emitter",{enumerable:true,get:function(){return Emitter;}});
Object.defineProperty(__exports,"injectStyles",{enumerable:true,get:function(){return injectStyles;}});
Object.defineProperty(__exports,"sparkPoints",{enumerable:true,get:function(){return sparkPoints;}});
Object.defineProperty(__exports,"tileMinFor",{enumerable:true,get:function(){return tileMinFor;}});
Object.defineProperty(__exports,"renderKPI",{enumerable:true,get:function(){return renderKPI;}});
const __m0=__req("packages/modules/kpi/aggregate.js");
const UNKNOWN_STATUS=__m0["UNKNOWN_STATUS"];
const __m1=__req("packages/modules/kpi/tree.js");
const visibleNodes=__m1["visibleNodes"];
const __m2=__req("packages/modules/kpi/messages.js");
const fallbackT=__m2["fallbackT"];
const NS='lat-kpi';
const EMPTY_TEXT=fallbackT('kpi.status.unknown');
const fallbackSay=fallbackT;
const MARKS=Object.freeze({
good:'\u25cf',warn:'\u25b2',critical:'\u25a0',unknown:'\u25cb',
});
function statusWord(say,status){
return status?say(`kpi.status.${status}`):'';
}
class Emitter{
constructor(){this.map=new Map();}
on(name,fn){
if(!this.map.has(name))this.map.set(name,new Set());
this.map.get(name).add(fn);
return()=>this.off(name,fn);
}
off(name,fn){this.map.get(name)?.delete(fn);}
emit(name,payload){for(const fn of this.map.get(name)||[])fn(payload);}
}
function injectStyles(doc){
if(!doc||(doc.getElementById&&doc.getElementById(`${NS}-styles`)))return;
const style=doc.createElement('style');
style.id=`${NS}-styles`;
style.setAttribute('id',`${NS}-styles`);
style.textContent=`
.${NS}{display:grid;gap:12px;grid-template-columns:repeat(auto-fit,minmax(var(--${NS}-tile-min,160px),1fr));font-family:var(--${NS}-font-family,var(--lattice-font-family,var(--lat-chrome-font-family,system-ui,-apple-system,"Segoe UI",Roboto,Helvetica,Arial,sans-serif)));font-size:var(--${NS}-font-size,var(--lattice-font-size,var(--lat-chrome-font-size,13px)));line-height:var(--${NS}-line-height,var(--lattice-line-height,var(--lat-chrome-line-height,1.4)));--${NS}-meter-w:var(--${NS}-meter-width,calc(34px * var(--lat-chrome-scale,1)))}
.${NS}__tile{border:1px solid var(--${NS}-border,#d7dbe0);border-radius:8px;padding:12px 14px;background:var(--${NS}-bg,#fff);display:flex;flex-direction:column;gap:4px;margin:0;min-width:0}
.${NS}__tile:focus-visible{outline:2px solid var(--${NS}-focus,#2563eb);outline-offset:2px}
.${NS}__label{font-size:.8em;color:var(--${NS}-muted,#586069);order:2;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;min-width:0}
.${NS}__value{font-size:1.6em;font-weight:600;line-height:1.1;order:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;min-width:0}
.${NS}__value.${NS}__clock{display:flex;flex-direction:column;gap:2px;white-space:normal}
.${NS}__clock-date{font-size:.55em;font-weight:400;color:var(--${NS}-muted,#586069);order:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.${NS}__clock-time{font-size:1em;font-weight:600;line-height:1.1;order:2;font-variant-numeric:tabular-nums;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.${NS}__delta{font-size:.8em;order:3}
.${NS}__delta--up{color:var(--${NS}-good,#1a7f37)}
.${NS}__delta--down{color:var(--${NS}-critical,#cf222e)}
.${NS}__tile--good{border-left:4px solid var(--${NS}-good,#1a7f37)}
.${NS}__tile--warn{border-left:4px solid var(--${NS}-warn,#bf8700)}
.${NS}__tile--critical{border-left:4px solid var(--${NS}-critical,#cf222e)}
.${NS}__tile--unknown{border-left:4px dashed var(--${NS}-unknown,#6e7781)}
.${NS}__tile--unknown .${NS}__value{color:var(--${NS}-muted,#586069)}
.${NS}__empty{font-size:.8em;font-style:italic;color:var(--${NS}-muted,#586069);order:3}
.${NS}--tree{display:block}
.${NS}__node{display:flex;align-items:center;gap:6px;padding:0 8px;border-radius:4px;min-height:var(--${NS}-row-height,var(--lat-chrome-row-height,22px))}
.${NS}__node:focus-visible{outline:2px solid var(--${NS}-focus,#2563eb);outline-offset:-2px}
.${NS}__node--branch{cursor:pointer;user-select:none;-webkit-user-select:none}
.${NS}__node--branch:hover{background:var(--${NS}-hover,#f2f4f7)}
.${NS}__node--branch:hover .${NS}__twisty--branch{border-color:var(--${NS}-muted,#586069);color:var(--${NS}-fg,#14171a)}
.${NS}__twisty{flex:none;box-sizing:border-box;display:inline-flex;align-items:center;justify-content:center;width:1.15em;height:1.15em;font-size:.8em;line-height:1;color:var(--${NS}-muted,#586069)}
.${NS}__twisty--branch{border:1px solid var(--${NS}-border,#d7dbe0);border-radius:2px;background:var(--${NS}-bg,#fff);font-weight:600}
.${NS}__mark{flex:none;font-size:.8em;line-height:1;width:1em;text-align:center}
.${NS}__mark--good{color:var(--${NS}-good,#1a7f37)}
.${NS}__mark--warn{color:var(--${NS}-warn,#bf8700)}
.${NS}__mark--critical{color:var(--${NS}-critical,#cf222e)}
.${NS}__mark--unknown{color:var(--${NS}-unknown,#6e7781)}
.${NS}__name{flex:1 1 auto;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.${NS}__reading{flex:none;font-weight:600;font-variant-numeric:tabular-nums}
.${NS}__note{flex:none;font-size:.8em;font-style:italic;color:var(--${NS}-muted,#586069)}
.${NS}__meter{flex:none;width:var(--${NS}-meter-w);height:var(--${NS}-meter-height,calc(10px * var(--lat-chrome-scale,1)));border-radius:2px;background:var(--${NS}-meter-track,#e6e9ed);overflow:hidden}
.${NS}__meter--nodata{background:none;border:1px dashed var(--${NS}-unknown,#6e7781);box-sizing:border-box}
.${NS}__fill{display:block;height:100%;background:var(--${NS}-meter-fill,#7d8590)}
.${NS}__fill--good{background:var(--${NS}-good,#1a7f37)}
.${NS}__fill--warn{background:var(--${NS}-warn,#bf8700)}
.${NS}__fill--critical{background:var(--${NS}-critical,#cf222e)}
.${NS}__gap{flex:none;width:var(--${NS}-meter-w)}
.${NS}__spark{order:4;width:100%;height:28px;overflow:visible}
.${NS}__spark polyline{fill:none;stroke:var(--${NS}-spark,#2563eb);stroke-width:1.5;transition:all .25s ease}
@media (prefers-reduced-motion: reduce){.${NS}__spark polyline{transition:none}}
`;
(doc.head||doc.documentElement).appendChild(style);
}
function sparkPoints(series,w=100,h=28){
const ys=series.filter((y)=>typeof y==='number'&&Number.isFinite(y));
if(ys.length<2)return'';
const min=Math.min(...ys);
const max=Math.max(...ys);
const span=max-min||1;
const step=w/(ys.length-1);
return ys.map((y,i)=>`${(i*step).toFixed(2)},${(h-((y-min)/span)*h).toFixed(2)}`).join(' ');
}
const STATUS_CLASSES=Object.freeze(['good','warn','critical','unknown']);
const TILE_GAP_PX=12;
const TILE_FLOOR_PX=96;
function tileMinFor(n){
const cols=Math.max(1,Math.trunc(n)||1);
const gaps=(cols-1)*TILE_GAP_PX;
return`max(${TILE_FLOOR_PX}px,calc((100% - ${gaps}px)/${cols}))`;
}
function setAttr(node,name,value){
if(node.getAttribute(name)!==value)node.setAttribute(name,value);
}
function setText(node,value){
if(node.textContent!==value)node.textContent=value;
}
function childByClass(parent,cls){
for(const c of parent.childNodes){
if(c&&c.classList&&c.classList.contains(cls))return c;
}
return null;
}
function reconcileChildren(parent,wanted){
const keep=new Set(wanted);
for(const child of[...parent.childNodes]){
if(!keep.has(child))parent.removeChild(child);
}
for(let i=0;i<wanted.length;i++){
if(parent.childNodes[i]!==wanted[i])parent.insertBefore(wanted[i],parent.childNodes[i]||null);
}
}
function renderKPI(kpi){
const{el,doc}=kpi;
if(!el||!doc)return;
injectStyles(doc);
el.classList.add(NS);
if(kpi.tree){renderTree(kpi);return;}
el.setAttribute('role','group');
if(kpi.config.ariaLabel)el.setAttribute('aria-label',kpi.config.ariaLabel);
if(kpi.config.columns)el.style.setProperty(`--${NS}-tile-min`,tileMinFor(kpi.config.columns));
const byId=new Map();
for(const child of el.childNodes){
const id=child&&child.getAttribute?child.getAttribute('data-tile'):null;
if(id!==null&&!byId.has(id))byId.set(id,child);
}
const wanted=kpi.model.tiles.map((t)=>{
const found=byId.get(String(t.id));
if(found){patchTile(doc,found,t,kpi.t);return found;}
return tileNode(doc,t,kpi.t);
});
reconcileChildren(el,wanted);
if(!kpi.wired)wire(kpi);
}
function tileNode(doc,t,say=fallbackSay){
const fig=doc.createElement('figure');
fig.classList.add(`${NS}__tile`);
fig.setAttribute('tabindex','0');
fig.setAttribute('role','group');
fig.setAttribute('data-tile',String(t.id));
patchTile(doc,fig,t,say);
return fig;
}
function valueNode(doc,fig,t){
const value=childByClass(fig,`${NS}__value`)||div(doc,`${NS}__value`);
setText(value,String(t.formatted));
return value;
}
function clockNode(doc,fig,t){
const wrap=childByClass(fig,`${NS}__value`)||div(doc,`${NS}__value`);
wrap.classList.add(`${NS}__clock`);
const lines=[];
if(t.clock&&t.clock.date){
const dateEl=childByClass(wrap,`${NS}__clock-date`)||div(doc,`${NS}__clock-date`);
setText(dateEl,t.clock.date);
lines.push(dateEl);
}
const timeEl=childByClass(wrap,`${NS}__clock-time`)||div(doc,`${NS}__clock-time`);
setText(timeEl,t.clock?t.clock.time:String(t.formatted));
lines.push(timeEl);
reconcileChildren(wrap,lines);
return wrap;
}
function patchTile(doc,fig,t,say=fallbackSay){
for(const s of STATUS_CLASSES){
if(s!==t.status)fig.classList.remove(`${NS}__tile--${s}`);
}
if(t.status)fig.classList.add(`${NS}__tile--${t.status}`);
const spoken=statusWord(say,t.status);
setAttr(fig,'aria-label',`${t.label}: ${t.formatted}${spoken?`, ${spoken}`:''}`);
const parts=[];
const value=t.kind==='clock'?clockNode(doc,fig,t):valueNode(doc,fig,t);
parts.push(value);
let cap=childByClass(fig,`${NS}__label`);
if(!cap){
cap=doc.createElement('figcaption');
cap.classList.add(`${NS}__label`);
}
setText(cap,String(t.label));
parts.push(cap);
if(t.status===UNKNOWN_STATUS){
const empty=childByClass(fig,`${NS}__empty`)||div(doc,`${NS}__empty`);
setText(empty,say('kpi.status.unknown'));
parts.push(empty);
}
if(t.delta!==null&&t.delta!==undefined)parts.push(deltaNode(doc,fig,t));
const spark=sparkNode(doc,fig,t);
if(spark)parts.push(spark);
reconcileChildren(fig,parts);
}
function div(doc,cls){
const node=doc.createElement('div');
node.classList.add(cls);
return node;
}
function deltaNode(doc,fig,t){
const dir=t.delta>0?'up':(t.delta<0?'down':'flat');
const arrow=t.delta>0?'▲':(t.delta<0?'▼':'■');
const pct=t.deltaPercent===null||t.deltaPercent===undefined
?'':` (${(t.deltaPercent*100).toFixed(1)}%)`;
const node=childByClass(fig,`${NS}__delta`)||div(doc,`${NS}__delta`);
for(const d of['up','down','flat']){
if(d!==dir)node.classList.remove(`${NS}__delta--${d}`);
}
node.classList.add(`${NS}__delta--${dir}`);
setText(node,`${arrow} ${t.deltaFormatted??t.delta}${pct}`);
return node;
}
function sparkNode(doc,fig,t){
if(!t.sparkline||t.sparkline.length<2)return null;
const pts=sparkPoints(t.sparkline);
if(!pts)return null;
let svg=childByClass(fig,`${NS}__spark`);
let poly;
if(svg){
poly=svg.childNodes[0];
}else{
svg=doc.createElementNS('http://www.w3.org/2000/svg','svg');
svg.classList.add(`${NS}__spark`);
svg.setAttribute('viewBox','0 0 100 28');
svg.setAttribute('preserveAspectRatio','none');
svg.setAttribute('aria-hidden','true');
poly=doc.createElementNS('http://www.w3.org/2000/svg','polyline');
svg.appendChild(poly);
}
setAttr(poly,'points',pts);
return svg;
}
function meterNode(doc,row,node){
const bar=node.tile?node.tile.bar:null;
if(node.hasChildren||!bar)return null;
const track=childByClass(row,`${NS}__meter`)||span(doc,`${NS}__meter`);
track.setAttribute('aria-hidden','true');
const nothing=bar.percent===null||bar.percent===undefined;
track.classList.toggle(`${NS}__meter--nodata`,nothing);
if(nothing){
reconcileChildren(track,[]);
return track;
}
const fill=childByClass(track,`${NS}__fill`)||span(doc,`${NS}__fill`);
for(const state of STATUS_CLASSES){
if(state!==node.status)fill.classList.remove(`${NS}__fill--${state}`);
}
if(node.status&&node.status!==UNKNOWN_STATUS)fill.classList.add(`${NS}__fill--${node.status}`);
const width=`${Math.round(bar.percent*100)/100}%`;
if(fill.style&&fill.style.setProperty&&fill.style.getPropertyValue('width')!==width){
fill.style.setProperty('width',width);
}
reconcileChildren(track,[fill]);
return track;
}
function spacerNode(doc,row){
const gap=childByClass(row,`${NS}__gap`)||span(doc,`${NS}__gap`);
gap.setAttribute('aria-hidden','true');
return gap;
}
function renderTree(kpi){
const{el,doc}=kpi;
el.classList.add(`${NS}--tree`);
el.setAttribute('role','tree');
if(kpi.config.ariaLabel)el.setAttribute('aria-label',kpi.config.ariaLabel);
const byKey=new Map();
for(const child of el.childNodes){
const key=child&&child.getAttribute?child.getAttribute('data-node'):null;
if(key!==null&&!byKey.has(key))byKey.set(key,child);
}
const nodes=visibleNodes(kpi.model.nodes||[]);
if(!nodes.some((n)=>n.key===kpi.activeNode))kpi.activeNode=nodes.length?nodes[0].key:null;
const wanted=nodes.map((node)=>{
const found=byKey.get(node.key);
if(found){patchNode(doc,found,node,kpi);return found;}
return nodeElement(doc,node,kpi);
});
reconcileChildren(el,wanted);
if(!kpi.wired)wireTree(kpi);
}
function nodeElement(doc,node,kpi){
const row=doc.createElement('div');
row.classList.add(`${NS}__node`);
row.setAttribute('role','treeitem');
row.setAttribute('data-node',node.key);
patchNode(doc,row,node,kpi);
return row;
}
function patchNode(doc,row,node,kpi){
const say=kpi.t||fallbackSay;
const shown=node.hasChildren?node.rollup:node.status;
for(const state of STATUS_CLASSES){
if(state!==shown)row.classList.remove(`${NS}__node--${state}`);
}
if(shown)row.classList.add(`${NS}__node--${shown}`);
row.classList.toggle(`${NS}__node--branch`,node.hasChildren);
setAttr(row,'aria-level',String(node.level+1));
setAttr(row,'aria-posinset',String(node.posinset));
setAttr(row,'aria-setsize',String(node.setsize));
setAttr(row,'tabindex',node.key===kpi.activeNode?'0':'-1');
if(node.hasChildren)setAttr(row,'aria-expanded',node.expanded?'true':'false');
else if(row.removeAttribute)row.removeAttribute('aria-expanded');
if(row.style&&row.style.setProperty){
row.style.setProperty('padding-left',
`calc(8px + ${node.level} * var(--${NS}-indent, calc(var(--lat-chrome-space,4px) * 4)))`);
}
setAttr(row,'aria-label',nodeName(say,node));
const parts=[];
const twisty=childByClass(row,`${NS}__twisty`)||span(doc,`${NS}__twisty`);
twisty.setAttribute('aria-hidden','true');
twisty.classList.toggle(`${NS}__twisty--branch`,node.hasChildren);
setText(twisty,node.hasChildren?(node.expanded?'−':'+'):'');
parts.push(twisty);
const state=shown||(node.status===UNKNOWN_STATUS?UNKNOWN_STATUS:null);
const mark=childByClass(row,`${NS}__mark`)||span(doc,`${NS}__mark`);
for(const other of STATUS_CLASSES){
if(other!==state)mark.classList.remove(`${NS}__mark--${other}`);
}
if(state)mark.classList.add(`${NS}__mark--${state}`);
mark.setAttribute('aria-hidden','true');
setText(mark,state?(MARKS[state]||''):'');
parts.push(mark);
const name=childByClass(row,`${NS}__name`)||span(doc,`${NS}__name`);
setText(name,node.label);
parts.push(name);
if(node.tile){
const reading=childByClass(row,`${NS}__reading`)||span(doc,`${NS}__reading`);
setText(reading,String(node.formatted));
parts.push(reading);
}
const note=noteFor(say,node);
if(note){
const el=childByClass(row,`${NS}__note`)||span(doc,`${NS}__note`);
setText(el,note);
parts.push(el);
}
parts.push(meterNode(doc,row,node)||spacerNode(doc,row));
reconcileChildren(row,parts);
}
function noteFor(say,node){
if(!node.unknown)return'';
if(!node.hasChildren)return say('kpi.status.unknown');
return say('kpi.node.unknown',{count:node.unknown});
}
function nodeName(say,node){
if(!node.hasChildren){
const spoken=statusWord(say,node.status);
const value=node.tile?`: ${node.formatted}`:'';
return`${node.label}${value}${spoken?`, ${spoken}`:''}`;
}
const parts=[node.tile?`${node.label}: ${node.formatted}`:node.label];
parts.push(say(node.items===1?'kpi.node.item':'kpi.node.items',{count:node.items}));
if(node.rollup)parts.push(say('kpi.node.worst',{status:statusWord(say,node.rollup)}));
if(node.unknown)parts.push(say('kpi.node.unknown',{count:node.unknown}));
return parts.join(', ');
}
function span(doc,cls){
const node=doc.createElement('span');
node.classList.add(cls);
return node;
}
function nodeFor(kpi,target){
const el=target&&target.closest?target.closest(`.${NS}__node`):null;
return el?(kpi.node(el.getAttribute('data-node'))||null):null;
}
function focusNode(kpi,node){
if(!node)return;
kpi.activeNode=node.key;
for(const child of kpi.el.childNodes){
if(!child||!child.getAttribute)continue;
const is=child.getAttribute('data-node')===node.key;
setAttr(child,'tabindex',is?'0':'-1');
if(is&&child.focus)child.focus();
}
}
function treeKeydown(kpi,e){
const node=nodeFor(kpi,e.target);
if(!node)return;
const visible=kpi.visibleNodes();
const at=visible.findIndex((n)=>n.key===node.key);
const go=(next)=>{
if(!next)return;
if(e.preventDefault)e.preventDefault();
focusNode(kpi,next);
};
switch(e.key){
case'ArrowDown':go(visible[at+1]);return;
case'ArrowUp':go(visible[at-1]);return;
case'Home':go(visible[0]);return;
case'End':go(visible[visible.length-1]);return;
case'ArrowRight':
if(node.hasChildren&&!node.expanded){
if(e.preventDefault)e.preventDefault();
kpi.expand(node.key);
focusNode(kpi,kpi.node(node.key));
}else if(node.hasChildren){
go(node.children[0]);
}
return;
case'ArrowLeft':
if(node.hasChildren&&node.expanded){
if(e.preventDefault)e.preventDefault();
kpi.collapse(node.key);
focusNode(kpi,kpi.node(node.key));
}else{
go(parentOf(kpi.nodes(),node.key));
}
return;
case'Enter':
case' ':
if(e.preventDefault)e.preventDefault();
activateNode(kpi,node,e);
return;
default:
}
}
function parentOf(roots,key){
const walk=(list,parent)=>{
for(const node of list){
if(node.key===key)return parent;
const hit=walk(node.children,node);
if(hit)return hit;
}
return undefined;
};
return walk(roots,undefined);
}
function activateNode(kpi,node,e){
if(node.tile)kpi.fire('tile:click',{tile:node.tile,id:node.tile.id,node,originalEvent:e});
if(node.hasChildren){kpi.toggle(node.key);focusNode(kpi,kpi.node(node.key));}
}
function wireTree(kpi){
const{el}=kpi;
el.addEventListener('click',(e)=>{
const node=nodeFor(kpi,e.target);
if(node){focusNode(kpi,node);activateNode(kpi,node,e);}
});
el.addEventListener('dblclick',(e)=>{
const node=nodeFor(kpi,e.target);
if(node&&node.tile)kpi.fire('tile:dblclick',{tile:node.tile,id:node.tile.id,node,originalEvent:e});
});
el.addEventListener('contextmenu',(e)=>{
const node=nodeFor(kpi,e.target);
if(node&&node.tile)kpi.fire('tile:contextmenu',{tile:node.tile,id:node.tile.id,node,originalEvent:e});
});
el.addEventListener('keydown',(e)=>treeKeydown(kpi,e));
kpi.wired=true;
}
function wire(kpi){
const{el}=kpi;
const tileFor=(target)=>{
const node=target&&target.closest?target.closest(`.${NS}__tile`):null;
if(!node)return null;
return kpi.tile(node.getAttribute('data-tile'));
};
el.addEventListener('click',(e)=>{const t=tileFor(e.target);if(t)kpi.fire('tile:click',{tile:t,id:t.id,originalEvent:e});});
el.addEventListener('dblclick',(e)=>{const t=tileFor(e.target);if(t)kpi.fire('tile:dblclick',{tile:t,id:t.id,originalEvent:e});});
el.addEventListener('contextmenu',(e)=>{const t=tileFor(e.target);if(t)kpi.fire('tile:contextmenu',{tile:t,id:t.id,originalEvent:e});});
el.addEventListener('keydown',(e)=>{
if(e.key!=='Enter'&&e.key!==' ')return;
const t=tileFor(e.target);
if(t){if(e.preventDefault)e.preventDefault();kpi.fire('tile:click',{tile:t,id:t.id,originalEvent:e});}
});
kpi.wired=true;
}
});
__def("packages/modules/shared/chrome.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"CHROME_TOKENS",{enumerable:true,get:function(){return CHROME_TOKENS;}});
Object.defineProperty(__exports,"findGridRoot",{enumerable:true,get:function(){return findGridRoot;}});
Object.defineProperty(__exports,"syncGridChrome",{enumerable:true,get:function(){return syncGridChrome;}});
Object.defineProperty(__exports,"adoptGridChrome",{enumerable:true,get:function(){return adoptGridChrome;}});
const CHROME_TOKENS=Object.freeze({
'--lattice-font-family':'--lat-chrome-font-family',
'--lattice-font-size':'--lat-chrome-font-size',
'--lattice-line-height':'--lat-chrome-line-height',
'--lattice-header-height':'--lat-chrome-header-height',
'--lattice-row-height':'--lat-chrome-row-height',
'--lattice-space':'--lat-chrome-space',
'--lattice-scale':'--lat-chrome-scale',
});
function findGridRoot(grid){
const host=grid&&grid.element;
if(!host||typeof host!=='object')return null;
if(host.classList&&host.classList.contains('lat-grid-root'))return(host);
if(typeof host.querySelector!=='function')return null;
return host.querySelector('.lat-grid-root');
}
function syncGridChrome(el,grid){
if(!el||!el.style||typeof el.style.setProperty!=='function')return false;
const root=findGridRoot(grid);
const view=root&&root.ownerDocument&&root.ownerDocument.defaultView;
if(!root||!view||typeof view.getComputedStyle!=='function')return false;
const computed=view.getComputedStyle(root);
if(!computed||typeof computed.getPropertyValue!=='function')return false;
for(const[token,published]of Object.entries(CHROME_TOKENS)){
const value=String(computed.getPropertyValue(token)||'').trim();
if(value){
if(el.style.getPropertyValue(published)!==value)el.style.setProperty(published,value);
}else if(typeof el.style.removeProperty==='function'){
el.style.removeProperty(published);
}
}
return true;
}
function adoptGridChrome(el,grid){
const refresh=()=>syncGridChrome(el,grid);
refresh();
let off=null;
if(grid&&typeof grid.on==='function'){
const bound=grid.on('config:changed',refresh);
off=typeof bound==='function'?bound:null;
}
return{
refresh,
release(){
if(off){try{off();}catch{}off=null;}
if(el&&el.style&&typeof el.style.removeProperty==='function'){
for(const published of Object.values(CHROME_TOKENS))el.style.removeProperty(published);
}
},
};
}
});
__def("packages/core/src/source/rows.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"DEFAULT_ROW_HEIGHT",{enumerable:true,get:function(){return DEFAULT_ROW_HEIGHT;}});
Object.defineProperty(__exports,"KEY_SEP",{enumerable:true,get:function(){return KEY_SEP;}});
Object.defineProperty(__exports,"createRowFactory",{enumerable:true,get:function(){return createRowFactory;}});
Object.defineProperty(__exports,"RowFactory",{enumerable:true,get:function(){return RowFactory;}});
Object.defineProperty(__exports,"pinnedRow",{enumerable:true,get:function(){return pinnedRow;}});
Object.defineProperty(__exports,"lazyRowView",{enumerable:true,get:function(){return lazyRowView;}});
Object.defineProperty(__exports,"groupPathElement",{enumerable:true,get:function(){return groupPathElement;}});
Object.defineProperty(__exports,"groupKey",{enumerable:true,get:function(){return groupKey;}});
Object.defineProperty(__exports,"groupPrefixKeys",{enumerable:true,get:function(){return groupPrefixKeys;}});
const __m0=__req("packages/core/src/internal/util.js");
const warnOnce=__m0["warnOnce"];
const isFunction=__m0["isFunction"];
const DEFAULT_ROW_HEIGHT=32;
const KEY_SEP='\u0000';
function createRowFactory(ctx){
return new RowFactory(ctx);
}
class RowFactory{
#rowKey;
#height;
constructor(ctx){
this.#rowKey=isFunction(ctx&&ctx.rowKey)?ctx.rowKey:null;
const configured=ctx&&ctx.config?ctx.config.rowHeight:undefined;
this.#height=typeof configured==='number'||isFunction(configured)
?(configured)
:DEFAULT_ROW_HEIGHT;
}
get keyed(){
return this.#rowKey!==null;
}
keyOf(data,index){
if(this.#rowKey){
const key=this.#rowKey(data);
if(key!==undefined&&key!==null)return String(key);
}else{
warnOnce(
'source.rowKey.missing',
'no config.rowKey; falling back to index identity.',
'Row changes, streaming dedupe, selection persistence and remote reload are disabled.',
);
}
return`#${index}`;
}
heightOf(row){
return isFunction(this.#height)
?Number((this.#height)(row))||DEFAULT_ROW_HEIGHT
:(this.#height);
}
leaf(data,physical,level=0,parent=null){
const row={
key:this.keyOf(data,physical),
data,
level,
parent,
group:false,
expanded:false,
leafCount:1,
height:0,
index:null,
selected:false,
physical,
};
row.height=this.heightOf(row);
return row;
}
group(spec){
const row={
key:groupKey(spec.keyPath||spec.path),
data:null,
level:spec.level,
parent:spec.parent||null,
children:[],
group:true,
expanded:spec.expanded!==false,
leafCount:spec.leafCount||0,
height:0,
index:null,
selected:false,
physical:null,
groupColumn:spec.column,
groupValue:spec.value,
groupPath:spec.path,
hasChildren:spec.hasChildren!==false,
};
if(spec.totals)row.totals=spec.totals;
row.height=this.heightOf(row);
return row;
}
footer(groupRow){
const row={
key:`${groupRow.key}${KEY_SEP}footer`,
data:null,
level:groupRow.level,
parent:groupRow,
group:true,
footer:true,
expanded:false,
leafCount:groupRow.leafCount||0,
height:0,
index:null,
selected:false,
physical:null,
groupColumn:groupRow.groupColumn,
groupValue:groupRow.groupValue,
groupPath:groupRow.groupPath,
hasChildren:false,
totals:groupRow.totals,
};
row.height=this.heightOf(row);
return row;
}
detail(master,height){
const row={
key:`${master.key}${KEY_SEP}detail`,
data:master.data,
level:master.level+1,
parent:master,
group:false,
detail:true,
expanded:false,
leafCount:0,
height,
index:null,
selected:false,
physical:null,
hasChildren:false,
masterKey:master.key,
};
return row;
}
grandTotal(totals,leafCount){
const row={
key:`${KEY_SEP}grandTotal`,
data:null,
level:0,
parent:null,
group:true,
grandTotal:true,
expanded:false,
leafCount,
height:0,
index:null,
selected:false,
physical:null,
groupPath:[],
hasChildren:false,
totals,
};
row.height=this.heightOf(row);
return row;
}
skeleton(index,level=0){
const row={
key:`${KEY_SEP}pending${KEY_SEP}${index}`,
data:null,
level,
parent:null,
group:false,
expanded:false,
leafCount:1,
height:0,
index,
selected:false,
physical:null,
};
row.height=this.heightOf(row);
return row;
}
}
function pinnedRow(data,index,edge,height){
const row={
key:`${KEY_SEP}pinned${KEY_SEP}${edge}${KEY_SEP}${index}`,
data,
level:0,
parent:null,
group:false,
expanded:false,
leafCount:0,
height,
index:null,
selected:false,
physical:null,
hasChildren:false,
pinned:edge,
};
return row;
}
function lazyRowView(size,read){
return((new Proxy([],{
get(target,prop,receiver){
if(prop==='length')return size();
if(typeof prop==='string'){
const index=Number(prop);
if(Number.isInteger(index)&&index>=0)return read(index);
}
return Reflect.get(target,prop,receiver);
},
has(target,prop){
if(typeof prop==='string'){
const index=Number(prop);
if(Number.isInteger(index)&&index>=0)return index<size();
}
return Reflect.has(target,prop);
},
})));
}
function groupPathElement(value){
return value===null||value===undefined?'':String(value);
}
const NULL_ELEMENT='\u0001';
function keyElement(value){
if(value===null||value===undefined)return NULL_ELEMENT;
const text=String(value);
return text.startsWith(NULL_ELEMENT)?NULL_ELEMENT+text:text;
}
function groupKey(path){
const list=path||[];
let key='g';
for(let i=0;i<list.length;i++)key+=KEY_SEP+keyElement(list[i]);
return key;
}
function groupPrefixKeys(path){
const keys=new Array(path.length);
const acc=[];
for(let level=0;level<path.length;level++){
acc.push(path[level]);
keys[level]=groupKey(acc);
}
return keys;
}
});
__def("packages/core/src/source/handles.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"HandleProvider",{enumerable:true,get:function(){return HandleProvider;}});
Object.defineProperty(__exports,"createHandleProvider",{enumerable:true,get:function(){return createHandleProvider;}});
const __m0=__req("packages/core/src/internal/util.js");
const isFunction=__m0["isFunction"];
const warnOnce=__m0["warnOnce"];
class HandleProvider{
#ctx;
#readRows;
#dropped;
#cache=new Map();
#version=0;
constructor(ctx,readRows,dropped){
this.#ctx=ctx;
this.#readRows=readRows;
this.#dropped=isFunction(dropped)?dropped:()=>false;
}
get store(){
const store=(this.#ctx).store;
return store&&isFunction(store.column)?(store):null;
}
invalidate(){
this.#version++;
}
handle(colId){
const store=this.store;
if(store){
const stored=store.column(colId);
if(stored)return stored;
}
const cached=this.#cache.get(colId);
if(cached&&cached.builtAt===this.#version)return cached.handle;
const column=this.#column(colId);
if(!column)return undefined;
if(store&&this.#dropped()){
warnOnce(
`source.dropped.${colId}`,
`column "${colId}" cannot be served under ingest.dropSourceRows: it is an `
+'impure computed column (a shadow or a positional/rank column) that is '
+'never stored, so it can only be read from the caller\'s row objects — '
+'which dropSourceRows has released. Do not sort, filter, group or total '
+'on such a column with dropSourceRows enabled.',
);
return undefined;
}
const handle=this.#buildObjectHandle(column);
this.#cache.set(colId,{handle,builtAt:this.#version});
return handle;
}
#column(colId){
const columns=(this.#ctx).columns;
return columns&&isFunction(columns.get)?columns.get(colId):undefined;
}
#buildObjectHandle(column){
const rows=this.#readRows()||[];
const n=rows.length;
const values=new Array(n);
const read=isFunction(column.getValue)
?(data)=>column.getValue(data)
:(data)=>(data==null?undefined:(data)[column.field||column.id]);
for(let i=0;i<n;i++)values[i]=rows[i]===undefined?undefined:read(rows[i]);
const version=this.#version;
return{
id:column.id,
kind:'object',
nullable:false,
values,
presence:null,
dict:null,
offsets:null,
get(physical){return values[physical];},
version,
};
}
}
function createHandleProvider(ctx,readRows,dropped){
return new HandleProvider(ctx,readRows,dropped);
}
});
__def("packages/core/src/store/bitset.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"Bitset",{enumerable:true,get:function(){return Bitset;}});
const WORD_BITS=8;
class Bitset{
static#POP=new Uint8Array(256);
static{
for(let i=1;i<256;i++)Bitset.#POP[i]=Bitset.#POP[i>>1]+(i&1);
}
#words;
#bits;
constructor(bits=0){
const n=Math.max(0,bits|0);
this.#bits=n;
this.#words=new Uint8Array(Math.ceil(n/WORD_BITS));
}
get size(){return this.#bits;}
get words(){return this.#words;}
get bytes(){return this.#words?this.#words.byteLength:0;}
get(i){
if(i<0||i>=this.#bits)return 0;
return(this.#words[i>>3]>>(i&7))&1;
}
set(i){
if(i>=0&&i<this.#bits)this.#words[i>>3]|=1<<(i&7);
return this;
}
clear(i){
if(i>=0&&i<this.#bits)this.#words[i>>3]&=~(1<<(i&7));
return this;
}
assign(i,bit){return bit?this.set(i):this.clear(i);}
fill(bit=false){
this.#words.fill(bit?0xff:0);
if(bit)this.#maskTail();
return this;
}
grow(bits){
const n=Math.max(0,bits|0);
if(n<=this.#bits)return this;
const need=Math.ceil(n/WORD_BITS);
if(need>this.#words.length){
const next=new Uint8Array(need);
next.set(this.#words);
this.#words=next;
}
this.#bits=n;
return this;
}
count(){
const w=this.#words;
const pop=Bitset.#POP;
let total=0;
for(let i=0;i<w.length;i++)total+=pop[w[i]];
return total;
}
and(other){
const b=other instanceof Bitset?other.words:other;
const w=this.#words;
const shared=Math.min(w.length,b.length);
for(let i=0;i<shared;i++)w[i]&=b[i];
for(let i=shared;i<w.length;i++)w[i]=0;
return this;
}
or(other){
const b=other instanceof Bitset?other.words:other;
const w=this.#words;
const shared=Math.min(w.length,b.length);
for(let i=0;i<shared;i++)w[i]|=b[i];
return this;
}
not(){
const w=this.#words;
for(let i=0;i<w.length;i++)w[i]=~w[i]&0xff;
this.#maskTail();
return this;
}
clone(){
const out=new Bitset(this.#bits);
out.words.set(this.#words.subarray(0,out.words.length));
return out;
}
release(){
this.#words=new Uint8Array(0);
this.#bits=0;
}
#maskTail(){
const used=this.#bits&7;
if(used===0)return;
const last=(this.#bits>>3);
if(last<this.#words.length)this.#words[last]&=(1<<used)-1;
}
static from(bools){
const arr=Array.isArray(bools)?bools:Array.from(bools);
const out=new Bitset(arr.length);
for(let i=0;i<arr.length;i++)if(arr[i])out.set(i);
return out;
}
}
});
__def("packages/core/src/store/dictionary.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"Dictionary",{enumerable:true,get:function(){return Dictionary;}});
const __m0=__req("packages/core/src/internal/util.js");
const collator=__m0["collator"];
const defaultCompare=__m0["defaultCompare"];
class Dictionary{
#values;
#codes=new Map();
#version=0;
#ranks=null;
#ranksVersion=-1;
#ranksLocale='\u0000';
constructor(values=[]){
this.#values=[];
for(let i=0;i<values.length;i++){
const v=values[i];
if(this.#codes.has(v))continue;
this.#codes.set(v,this.#values.length);
this.#values.push(v);
}
}
get size(){return this.#values.length;}
get version(){return this.#version;}
get bytes(){
let total=this.#values.length*8;
for(let i=0;i<this.#values.length;i++){
const v=this.#values[i];
if(typeof v==='string')total+=v.length*2;
total+=16;
}
return total;
}
codeOf(value){
const existing=this.#codes.get(value);
if(existing!==undefined)return existing;
const code=this.#values.length;
this.#values.push(value);
this.#codes.set(value,code);
this.#version++;
return code;
}
lookup(value){
const code=this.#codes.get(value);
return code===undefined?-1:code;
}
has(value){return this.#codes.has(value);}
valueOf(code){return this.#values[code];}
values(){return this.#values;}
ranks(locale){
const key=locale||'';
if(this.#ranks&&this.#ranksVersion===this.#version&&this.#ranksLocale===key){
return this.#ranks;
}
const n=this.#values.length;
const order=new Array(n);
for(let i=0;i<n;i++)order[i]=i;
const cmp=collator(locale).compare;
const values=this.#values;
order.sort((a,b)=>this.#compare(values[a],values[b],cmp));
const ranks=new Uint32Array(n);
for(let rank=0;rank<n;rank++)ranks[order[rank]]=rank;
this.#ranks=ranks;
this.#ranksVersion=this.#version;
this.#ranksLocale=key;
return ranks;
}
#compare(a,b,compare){
if(typeof a==='string'&&typeof b==='string')return compare(a,b);
return defaultCompare(a,b);
}
}
});
__def("packages/core/src/store/multivalue.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"MultiValue",{enumerable:true,get:function(){return MultiValue;}});
class MultiValue{
#values;
#offsets;
#rows=0;
#fill=0;
constructor(capacity={}){
const rows=Math.max(1,capacity.rows??16);
const values=Math.max(1,capacity.values??rows);
this.#values=new Int32Array(values);
this.#offsets=new Uint32Array(rows+1);
}
get values(){return this.#values;}
get offsets(){return this.#offsets;}
get rows(){return this.#rows;}
get length(){return this.#fill;}
get bytes(){return this.#values.byteLength+this.#offsets.byteLength;}
count(r){
if(r<0||r>=this.#rows)return 0;
return this.#offsets[r+1]-this.#offsets[r];
}
at(r){
if(r<0||r>=this.#rows)return this.#values.subarray(0,0);
return this.#values.subarray(this.#offsets[r],this.#offsets[r+1]);
}
has(r,code){
if(r<0||r>=this.#rows)return false;
const v=this.#values;
const end=this.#offsets[r+1];
for(let i=this.#offsets[r];i<end;i++)if(v[i]===code)return true;
return false;
}
hasAny(r,codes){
for(let i=0;i<codes.length;i++)if(this.has(r,codes[i]))return true;
return false;
}
hasAll(r,codes){
for(let i=0;i<codes.length;i++)if(!this.has(r,codes[i]))return false;
return true;
}
hasNone(r,codes){return!this.hasAny(r,codes);}
push(codes){
const r=this.#rows;
const n=codes.length;
this.#ensureRows(r+1);
this.#ensureValues(this.#fill+n);
const start=this.#fill;
for(let i=0;i<n;i++)this.#values[start+i]=codes[i]|0;
this.#fill+=n;
this.#rows=r+1;
this.#offsets[r]=start;
this.#offsets[r+1]=this.#fill;
return r;
}
write(r,codes){
if(r===this.#rows){this.push(codes);return;}
if(r<0||r>this.#rows)return;
const start=this.#offsets[r];
const end=this.#offsets[r+1];
const n=codes.length;
if(end-start===n){
for(let i=0;i<n;i++)this.#values[start+i]=codes[i]|0;
return;
}
this.#rebuild(r,codes);
}
compact(remap,liveCount,dead){
const oldValues=this.#values;
const oldOffsets=this.#offsets;
const oldRows=this.#rows;
const values=new Int32Array(Math.max(1,this.#fill));
const offsets=new Uint32Array(liveCount+1);
let w=0;
for(let p=0;p<oldRows;p++){
if(remap[p]===dead)continue;
const start=oldOffsets[p];
const end=oldOffsets[p+1];
offsets[remap[p]]=w;
for(let i=start;i<end;i++)values[w++]=oldValues[i];
offsets[remap[p]+1]=w;
}
this.#values=values;
this.#offsets=offsets;
this.#rows=liveCount;
this.#fill=w;
}
release(){
this.#values=new Int32Array(0);
this.#offsets=new Uint32Array(1);
this.#rows=0;
this.#fill=0;
}
#ensureRows(rows){
if(rows+1<=this.#offsets.length)return;
let cap=this.#offsets.length-1;
while(cap<rows)cap=cap*2||16;
const next=new Uint32Array(cap+1);
next.set(this.#offsets);
this.#offsets=next;
}
#ensureValues(n){
if(n<=this.#values.length)return;
let cap=this.#values.length;
while(cap<n)cap=cap*2||16;
const next=new Int32Array(cap);
next.set(this.#values);
this.#values=next;
}
#rebuild(r,codes){
const oldValues=this.#values;
const oldOffsets=this.#offsets;
const rows=this.#rows;
const delta=codes.length-(oldOffsets[r+1]-oldOffsets[r]);
const values=new Int32Array(Math.max(1,this.#fill+delta));
const offsets=new Uint32Array(oldOffsets.length);
let w=0;
for(let p=0;p<rows;p++){
offsets[p]=w;
if(p===r){
for(let i=0;i<codes.length;i++)values[w++]=codes[i]|0;
}else{
for(let i=oldOffsets[p];i<oldOffsets[p+1];i++)values[w++]=oldValues[i];
}
offsets[p+1]=w;
}
this.#values=values;
this.#offsets=offsets;
this.#fill=w;
}
}
});
__def("packages/core/src/compute/handle.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"identity",{enumerable:true,get:function(){return identity;}});
Object.defineProperty(__exports,"rowCount",{enumerable:true,get:function(){return rowCount;}});
Object.defineProperty(__exports,"bitReader",{enumerable:true,get:function(){return bitReader;}});
Object.defineProperty(__exports,"presenceReader",{enumerable:true,get:function(){return presenceReader;}});
Object.defineProperty(__exports,"dictSize",{enumerable:true,get:function(){return dictSize;}});
Object.defineProperty(__exports,"dictValue",{enumerable:true,get:function(){return dictValue;}});
Object.defineProperty(__exports,"multiValue",{enumerable:true,get:function(){return multiValue;}});
Object.defineProperty(__exports,"valueReader",{enumerable:true,get:function(){return valueReader;}});
Object.defineProperty(__exports,"numericTotalOrder",{enumerable:true,get:function(){return numericTotalOrder;}});
Object.defineProperty(__exports,"valueComparator",{enumerable:true,get:function(){return valueComparator;}});
Object.defineProperty(__exports,"dictRanks",{enumerable:true,get:function(){return dictRanks;}});
Object.defineProperty(__exports,"isMissing",{enumerable:true,get:function(){return isMissing;}});
const __m0=__req("packages/core/src/internal/util.js");
const collator=__m0["collator"];
const defaultCompare=__m0["defaultCompare"];
const warnOnce=__m0["warnOnce"];
function identity(n){
const out=new Uint32Array(n);
for(let i=0;i<n;i++)out[i]=i;
return out;
}
function rowCount(handle,opts){
if(opts&&typeof opts.count==='number')return opts.count;
if(!handle)return 0;
if(typeof handle.count==='number')return handle.count;
if(typeof handle.length==='number')return handle.length;
const values=handle.values;
if(handle.kind==='multi'&&handle.offsets)return Math.max(0,handle.offsets.length-1);
if(!values)return handle.presence&&typeof handle.presence.size==='number'?handle.presence.size:0;
if(handle.kind==='bitset'){
if(typeof values.size==='number')return values.size;
if(handle.presence&&typeof handle.presence.size==='number')return handle.presence.size;
warnOnce(`count:${handle.id}`,`column "${handle.id}" is bitset-backed with no declared row count; assuming ${values.length*8}`);
return values.length*8;
}
return values.length;
}
const bitOrders=new WeakMap();
function bitOrderOf(bitset){
const ctor=bitset.constructor;
if(!ctor)return'unknown';
const cached=bitOrders.get(ctor);
if(cached)return cached;
let order='unknown';
try{
let probe=null;
if(typeof ctor.from==='function')probe=ctor.from([false,true]);
else{
probe=new ctor(8);
probe.set(1);
}
const words=probe&&probe.words;
if(words&&words.length){
if(words[0]===0x02)order='lsb';
else if(words[0]===0x40)order='msb';
}
}catch{
order='unknown';
}
bitOrders.set(ctor,order);
return order;
}
function bitReader(bits){
if(!bits)return()=>0;
const raw=bits instanceof Uint8Array?bits:bits.words;
if(raw instanceof Uint8Array){
const order=bits instanceof Uint8Array?'lsb':bitOrderOf(bits);
if(order==='lsb')return(i)=>(raw[i>>>3]>>>(i&7))&1;
if(order==='msb')return(i)=>(raw[i>>>3]>>>(7-(i&7)))&1;
}
if(typeof bits.get==='function')return(i)=>(bits.get(i)?1:0);
return()=>0;
}
function presenceReader(handle){
if(!handle||!handle.presence)return null;
return bitReader(handle.presence);
}
function dictSize(dict){
if(!dict)return 0;
if(typeof dict.size==='number')return dict.size;
if(typeof dict.values==='function')return dict.values().length;
return 0;
}
function dictValue(dict,code){
if(!dict)return null;
if(typeof dict.valueOf==='function')return dict.valueOf(code);
if(typeof dict.values==='function')return dict.values()[code];
return null;
}
function multiValue(handle,i){
const offsets=handle.offsets;
const values=handle.values;
if(!offsets||!values)return[];
const from=offsets[i];
const to=offsets[i+1];
if(!(to>from))return[];
const dict=handle.dict;
const out=new Array(to-from);
for(let k=from;k<to;k++)out[k-from]=dict?dictValue(dict,values[k]):values[k];
return out;
}
function valueReader(handle){
if(!handle)return()=>undefined;
const values=handle.values;
const present=presenceReader(handle);
const kind=handle.kind;
if(kind==='dictionary'){
const dict=handle.dict;
if(present)return(i)=>(present(i)?dictValue(dict,values[i]):null);
return(i)=>dictValue(dict,values[i]);
}
if(kind==='bitset'){
const bit=bitReader(values);
if(present)return(i)=>(present(i)?bit(i)===1:null);
return(i)=>bit(i)===1;
}
if(kind==='multi'){
if(present)return(i)=>(present(i)?multiValue(handle,i):null);
return(i)=>multiValue(handle,i);
}
if(!values&&typeof handle.get==='function'){
const get=handle.get.bind(handle);
return(i)=>{
const v=get(i);
return v===undefined?null:v;
};
}
if(present){
return(i)=>{
if(!present(i))return null;
const v=values[i];
return v===undefined?null:v;
};
}
return(i)=>{
const v=values[i];
return v===undefined?null:v;
};
}
function numericTotalOrder(a,b){
if(a<b)return-1;
if(a>b)return 1;
if(a===b){
const na=Object.is(a,-0);
const nb=Object.is(b,-0);
if(na===nb)return 0;
return na?-1:1;
}
const an=Number.isNaN(a);
const bn=Number.isNaN(b);
if(an&&bn)return 0;
return an?1:-1;
}
function valueComparator(locale){
const coll=collator(locale);
return(a,b)=>{
if(a===b)return 0;
const ta=typeof a;
const tb=typeof b;
if(ta==='string'&&tb==='string')return coll.compare(a,b);
if(ta==='number'&&tb==='number')return numericTotalOrder(a,b);
if(ta==='boolean'&&tb==='boolean')return a===b?0:a?1:-1;
if(a instanceof Date||b instanceof Date){
const na=a instanceof Date?a.getTime():Number(a);
const nb=b instanceof Date?b.getTime():Number(b);
return numericTotalOrder(na,nb);
}
return defaultCompare(a,b);
};
}
function dictRanks(dict,locale){
if(dict&&typeof dict.ranks==='function')return dict.ranks(locale);
const table=dict&&typeof dict.values==='function'?dict.values():[];
const n=table.length;
const cmp=valueComparator(locale);
const order=new Array(n);
for(let i=0;i<n;i++)order[i]=i;
order.sort((a,b)=>cmp(table[a],table[b])||a-b);
const ranks=new Uint32Array(n);
for(let r=0;r<n;r++)ranks[order[r]]=r;
return ranks;
}
function isMissing(v){
return v===null||v===undefined||(typeof v==='number'&&Number.isNaN(v));
}
});
__def("packages/core/src/compute/sort.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"radixSortFloat64",{enumerable:true,get:function(){return radixSortFloat64;}});
Object.defineProperty(__exports,"radixSortInt32",{enumerable:true,get:function(){return radixSortInt32;}});
Object.defineProperty(__exports,"rankSortDictionary",{enumerable:true,get:function(){return rankSortDictionary;}});
Object.defineProperty(__exports,"mergeSortComparator",{enumerable:true,get:function(){return mergeSortComparator;}});
Object.defineProperty(__exports,"rankSortStrings",{enumerable:true,get:function(){return rankSortStrings;}});
Object.defineProperty(__exports,"collateStringRanks",{enumerable:true,get:function(){return collateStringRanks;}});
Object.defineProperty(__exports,"sortColumn",{enumerable:true,get:function(){return sortColumn;}});
Object.defineProperty(__exports,"sortMulti",{enumerable:true,get:function(){return sortMulti;}});
const __m0=__req("packages/core/src/compute/handle.js");
const bitReader=__m0["bitReader"];
const dictRanks=__m0["dictRanks"];
const dictSize=__m0["dictSize"];
const identity=__m0["identity"];
const isMissing=__m0["isMissing"];
const presenceReader=__m0["presenceReader"];
const rowCount=__m0["rowCount"];
const valueComparator=__m0["valueComparator"];
const valueReader=__m0["valueReader"];
const EMPTY_INDICES=new Uint32Array(0);
const SCRATCH=new ArrayBuffer(8);
const SCRATCH_F64=new Float64Array(SCRATCH);
const SCRATCH_U32=new Uint32Array(SCRATCH);
const HI=(()=>{
SCRATCH_F64[0]=-1;
return(SCRATCH_U32[1]&0x80000000)!==0?1:0;
})();
const LO=HI===1?0:1;
function transformDouble(value,out){
SCRATCH_F64[0]=value;
let hi=SCRATCH_U32[HI];
let lo=SCRATCH_U32[LO];
if((hi&0x80000000)!==0){
hi=~hi>>>0;
lo=~lo>>>0;
}else{
hi=(hi^0x80000000)>>>0;
}
out[0]=lo;
out[1]=hi;
}
function radixLsd64(idx,lo,hi,n){
if(n<2)return idx;
const hist=new Uint32Array(256*8);
for(let i=0;i<n;i++){
const l=lo[i];
const h=hi[i];
hist[l&0xff]++;
hist[256+((l>>>8)&0xff)]++;
hist[512+((l>>>16)&0xff)]++;
hist[768+((l>>>24)&0xff)]++;
hist[1024+(h&0xff)]++;
hist[1280+((h>>>8)&0xff)]++;
hist[1536+((h>>>16)&0xff)]++;
hist[1792+((h>>>24)&0xff)]++;
}
let srcIdx=idx;
let srcLo=lo;
let srcHi=hi;
let dstIdx=new Uint32Array(n);
let dstLo=new Uint32Array(n);
let dstHi=new Uint32Array(n);
const offset=new Uint32Array(256);
for(let pass=0;pass<8;pass++){
const base=pass<<8;
const shift=(pass&3)<<3;
const useHi=pass>=4;
let skip=false;
for(let b=0;b<256;b++){
if(hist[base+b]===n){skip=true;break;}
}
if(skip)continue;
let sum=0;
for(let b=0;b<256;b++){
offset[b]=sum;
sum+=hist[base+b];
}
for(let i=0;i<n;i++){
const l=srcLo[i];
const h=srcHi[i];
const digit=((useHi?h:l)>>>shift)&0xff;
const p=offset[digit]++;
dstIdx[p]=srcIdx[i];
dstLo[p]=l;
dstHi[p]=h;
}
let t=srcIdx;srcIdx=dstIdx;dstIdx=t;
t=srcLo;srcLo=dstLo;dstLo=t;
t=srcHi;srcHi=dstHi;dstHi=t;
}
return srcIdx;
}
function radixLsd32(idx,keys,n){
if(n<2)return idx;
const hist=new Uint32Array(256*4);
for(let i=0;i<n;i++){
const k=keys[i];
hist[k&0xff]++;
hist[256+((k>>>8)&0xff)]++;
hist[512+((k>>>16)&0xff)]++;
hist[768+((k>>>24)&0xff)]++;
}
let srcIdx=idx;
let srcKeys=keys;
let dstIdx=new Uint32Array(n);
let dstKeys=new Uint32Array(n);
const offset=new Uint32Array(256);
for(let pass=0;pass<4;pass++){
const base=pass<<8;
const shift=pass<<3;
let skip=false;
for(let b=0;b<256;b++){
if(hist[base+b]===n){skip=true;break;}
}
if(skip)continue;
let sum=0;
for(let b=0;b<256;b++){
offset[b]=sum;
sum+=hist[base+b];
}
for(let i=0;i<n;i++){
const k=srcKeys[i];
const p=offset[(k>>>shift)&0xff]++;
dstIdx[p]=srcIdx[i];
dstKeys[p]=k;
}
let t=srcIdx;srcIdx=dstIdx;dstIdx=t;
t=srcKeys;srcKeys=dstKeys;dstKeys=t;
}
return srcIdx;
}
function countingSort(idx,keys,n,radix){
const counts=new Uint32Array(radix+1);
for(let i=0;i<n;i++)counts[keys[i]]++;
let sum=0;
for(let k=0;k<=radix;k++){
const c=counts[k];
counts[k]=sum;
sum+=c;
}
const out=new Uint32Array(n);
for(let i=0;i<n;i++)out[counts[keys[i]]++]=idx[i];
return out;
}
function sortUint32Keys(idx,keys,n,radix){
if(n<2)return idx;
if(radix<=65536||radix<=n*2)return countingSort(idx,keys,n,radix);
return radixLsd32(idx,keys,n);
}
function exact(buffer,n){
if(buffer.length===n&&buffer.byteOffset===0)return buffer;
return Uint32Array.prototype.slice.call(buffer,0,n);
}
function radixSortFloat64(values,order,descending=false){
const src=order||identity(values.length);
const n=src.length;
if(n<2)return Uint32Array.from(src);
const idx=new Uint32Array(n);
const lo=new Uint32Array(n);
const hi=new Uint32Array(n);
const nans=new Uint32Array(n);
const pair=new Uint32Array(2);
let m=0;
let nanCount=0;
for(let i=0;i<n;i++){
const row=src[i];
const v=values[row];
if(Number.isNaN(v)){nans[nanCount++]=row;continue;}
transformDouble(v,pair);
if(descending){
lo[m]=~pair[0]>>>0;
hi[m]=~pair[1]>>>0;
}else{
lo[m]=pair[0];
hi[m]=pair[1];
}
idx[m++]=row;
}
const sorted=radixLsd64(idx.subarray(0,m),lo.subarray(0,m),hi.subarray(0,m),m);
if(nanCount===0)return exact(sorted,m);
const out=new Uint32Array(n);
out.set(sorted.subarray(0,m),0);
out.set(nans.subarray(0,nanCount),m);
return out;
}
function radixSortInt32(values,order,descending=false){
const src=order||identity(values.length);
const n=src.length;
if(n<2)return Uint32Array.from(src);
const idx=Uint32Array.from(src);
const keys=new Uint32Array(n);
for(let i=0;i<n;i++){
const k=(values[idx[i]]^0x80000000)>>>0;
keys[i]=descending?(~k>>>0):k;
}
return exact(radixLsd32(idx,keys,n),n);
}
function rankSortDictionary(handle,order,opts={}){
const src=order||identity(rowCount(handle,opts));
const n=src.length;
if(n<2)return Uint32Array.from(src);
const ranks=dictRanks(handle.dict,opts.locale);
const codes=handle.values;
const present=presenceReader(handle);
const size=Math.max(dictSize(handle.dict),ranks.length);
const absentRank=size;
const idx=Uint32Array.from(src);
const keys=new Uint32Array(n);
const descending=!!opts.descending;
for(let i=0;i<n;i++){
const row=idx[i];
let rank=present&&present(row)===0?absentRank:ranks[codes[row]];
if(rank===undefined)rank=absentRank;
keys[i]=descending?absentRank-rank:rank;
}
return exact(sortUint32Keys(idx,keys,n,size+1),n);
}
function mergeSortComparator(values,order,compare){
const n=order.length;
let src=Uint32Array.from(order);
if(n<2)return src;
let dst=new Uint32Array(n);
for(let width=1;width<n;width<<=1){
for(let start=0;start<n;start+=width<<1){
const mid=Math.min(start+width,n);
const end=Math.min(start+(width<<1),n);
let i=start;
let j=mid;
let k=start;
while(i<mid&&j<end){
dst[k++]=compare(values[src[i]],values[src[j]])<=0?src[i++]:src[j++];
}
while(i<mid)dst[k++]=src[i++];
while(j<end)dst[k++]=src[j++];
}
const t=src;src=dst;dst=t;
}
return src;
}
function sortBitsetColumn(handle,idx,descending){
const bit=bitReader(handle.values);
const n=idx.length;
const out=new Uint32Array(n);
const first=descending?1:0;
let k=0;
for(let i=0;i<n;i++)if(bit(idx[i])===first)out[k++]=idx[i];
for(let i=0;i<n;i++)if(bit(idx[i])!==first)out[k++]=idx[i];
return out;
}
function indexableValues(handle,idx){
const values=handle.values;
const kind=handle.kind;
const direct=(kind==='object'||kind===undefined)&&(Array.isArray(values)||ArrayBuffer.isView(values));
if(direct&&!handle.presence)return values;
const reader=valueReader(handle);
const materialised=new Array(rowCount(handle)||0);
for(let i=0;i<idx.length;i++){
const row=idx[i];
materialised[row]=reader(row);
}
return materialised;
}
function allStrings(values,idx){
for(let i=0;i<idx.length;i++){
if(typeof values[idx[i]]!=='string')return false;
}
return true;
}
function rankSortStrings(idx,codes,ranks,d,descending){
const n=idx.length;
const idxOut=Uint32Array.from(idx);
if(n<2||d<1)return idxOut;
const keys=new Uint32Array(n);
const top=d-1;
for(let i=0;i<n;i++){
const rank=ranks[codes[i]];
keys[i]=descending?top-rank:rank;
}
return exact(sortUint32Keys(idxOut,keys,n,d),n);
}
function collateStringRanks(table,d,locale){
const compare=valueComparator(locale);
const order=new Array(d);
for(let i=0;i<d;i++)order[i]=i;
order.sort((a,b)=>compare(table[a],table[b])||a-b);
const ranks=new Uint32Array(d);
for(let r=0;r<d;r++)ranks[order[r]]=r;
return ranks;
}
function keyedSortStrings(values,idx,opts){
const n=idx.length;
const codeOf=new Map();
const table=[];
const codes=new Uint32Array(n);
for(let i=0;i<n;i++){
const v=values[idx[i]];
let c=codeOf.get(v);
if(c===undefined){c=table.length;codeOf.set(v,c);table.push(v);}
codes[i]=c;
}
const d=table.length;
const ranks=collateStringRanks(table,d,opts.locale);
return rankSortStrings(idx,codes,ranks,d,!!opts.descending);
}
function sortByComparator(handle,idx,opts){
const values=indexableValues(handle,idx);
if(idx.length>=2&&allStrings(values,idx)){
const index=handle.stringRank;
if(index&&index.version===handle.version&&index.usable(idx,values,opts.locale)){
return index.sort(idx,opts);
}
return keyedSortStrings(values,idx,opts);
}
const base=valueComparator(opts.locale);
const compare=opts.descending?(a,b)=>base(b,a):base;
return mergeSortComparator(values,idx,compare);
}
function sortByCompare(handle,idx,opts){
const values=indexableValues(handle,idx);
const user=opts.compare;
const descending=!!opts.descending;
const compare=descending
?(a,b)=>-user(a,b,undefined,undefined,true)
:(a,b)=>user(a,b,undefined,undefined,false);
return mergeSortComparator(values,idx,compare);
}
function partitionPresent(handle,idx){
const n=idx.length;
const present=presenceReader(handle);
const values=handle.values;
const checkNaN=handle.kind==='float64'&&!!values;
const looseKind=handle.kind==='object'||handle.kind==='multi'||handle.kind===undefined;
if(!present&&!checkNaN&&!looseKind)return{present:idx,absent:EMPTY_INDICES};
const keep=new Uint32Array(n);
const drop=new Uint32Array(n);
let p=0;
let a=0;
if(present&&checkNaN){
for(let i=0;i<n;i++){
const row=idx[i];
if(present(row)===1&&!Number.isNaN(values[row]))keep[p++]=row;else drop[a++]=row;
}
}else if(checkNaN&&!present){
for(let i=0;i<n;i++){
const row=idx[i];
if(!Number.isNaN(values[row]))keep[p++]=row;else drop[a++]=row;
}
}else if(present&&!looseKind){
for(let i=0;i<n;i++){
const row=idx[i];
if(present(row)===1)keep[p++]=row;else drop[a++]=row;
}
}else{
const reader=valueReader(handle);
for(let i=0;i<n;i++){
const row=idx[i];
if(!isMissing(reader(row)))keep[p++]=row;else drop[a++]=row;
}
}
if(a===0)return{present:idx,absent:EMPTY_INDICES};
return{present:keep.subarray(0,p),absent:drop.subarray(0,a)};
}
function joinRuns(sorted,absent,nullsFirst){
if(absent.length===0)return sorted;
const out=new Uint32Array(sorted.length+absent.length);
if(nullsFirst){
out.set(absent,0);
out.set(sorted,absent.length);
}else{
out.set(sorted,0);
out.set(absent,sorted.length);
}
return out;
}
function sortColumn(handle,order,opts={}){
const src=order||identity(rowCount(handle,opts));
if(!handle||src.length<2)return Uint32Array.from(src);
const{present,absent}=partitionPresent(handle,src);
if(present.length===0)return Uint32Array.from(src);
const descending=!!opts.descending;
let sorted;
if(typeof opts.compare==='function'){
sorted=sortByCompare(handle,present,opts);
}else{
switch(handle.kind){
case'float64':
sorted=radixSortFloat64(handle.values,present,descending);
break;
case'int32':
sorted=radixSortInt32(handle.values,present,descending);
break;
case'dictionary':
sorted=rankSortDictionary(handle,present,opts);
break;
case'bitset':
sorted=sortBitsetColumn(handle,present,descending);
break;
default:
sorted=sortByComparator(handle,present,opts);
break;
}
}
return joinRuns(sorted,absent,!!opts.nullsFirst);
}
function sortMulti(handles,entries,order,opts={}){
const list=entries||[];
const first=(list.length&&(list[0].handle||byId(handles,list[0].col)))||(handles&&handles[0]);
let current=order||identity(rowCount(first,opts));
for(let i=list.length-1;i>=0;i--){
const entry=list[i];
const handle=entry.handle||byId(handles,entry.col)||(handles&&handles[i]);
if(!handle)continue;
current=sortColumn(handle,current,{
descending:entry.descending!==undefined?!!entry.descending:entry.dir==='desc',
nullsFirst:!!entry.nullsFirst,
locale:entry.locale!==undefined?entry.locale:opts.locale,
compare:entry.compare,
});
}
return current instanceof Uint32Array?current:Uint32Array.from(current);
}
function byId(handles,id){
if(!handles||id===undefined)return undefined;
for(let i=0;i<handles.length;i++)if(handles[i]&&handles[i].id===id)return handles[i];
return undefined;
}
});
__def("packages/core/src/store/stringrank.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"DEFAULT_MAX_DISTINCT",{enumerable:true,get:function(){return DEFAULT_MAX_DISTINCT;}});
Object.defineProperty(__exports,"StringRankIndex",{enumerable:true,get:function(){return StringRankIndex;}});
const __m0=__req("packages/core/src/compute/sort.js");
const collateStringRanks=__m0["collateStringRanks"];
const rankSortStrings=__m0["rankSortStrings"];
const DEFAULT_MAX_DISTINCT=100000;
class StringRankIndex{
#table=[];
#codeOf=new Map();
#codeByRow;
#length=0;
#generation=0;
#stamp=-1;
#maxDistinct;
#capped=false;
#ranks=null;
#ranksGeneration=-1;
#ranksLocale='\u0000';
constructor(maxDistinct=DEFAULT_MAX_DISTINCT){
this.#maxDistinct=maxDistinct>0?maxDistinct:DEFAULT_MAX_DISTINCT;
this.#codeByRow=new Uint32Array(0);
}
get version(){return this.#stamp;}
set version(version){this.#stamp=version;}
get generation(){return this.#generation;}
get size(){return this.#table.length;}
get capped(){return this.#capped;}
get length(){return this.#length;}
get bytes(){
let total=this.#codeByRow.byteLength;
const table=this.#table;
for(let i=0;i<table.length;i++)total+=table[i].length*2+24;
return total;
}
#intern(value,capOnGrowth){
const existing=this.#codeOf.get(value);
if(existing!==undefined)return existing;
if(this.#capped)return-1;
if(capOnGrowth&&this.#table.length>=this.#maxDistinct){
this.#capped=true;
return-1;
}
const code=this.#table.length;
this.#table.push(value);
this.#codeOf.set(value,code);
this.#generation++;
return code;
}
build(values,count){
const n=count|0;
this.#table=[];
this.#codeOf=new Map();
this.#generation=0;
this.#capped=false;
this.#ranks=null;
this.#ranksGeneration=-1;
this.#codeByRow=new Uint32Array(n);
for(let row=0;row<n;row++){
const v=values[row];
const code=typeof v==='string'?this.#intern(v,false):-1;
this.#codeByRow[row]=code<0?0:code;
}
this.#length=n;
}
append(values,from,count){
const to=(from|0)+(count|0);
if(to>this.#codeByRow.length){
const next=new Uint32Array(to);
next.set(this.#codeByRow.subarray(0,this.#length));
this.#codeByRow=next;
}
for(let row=from|0;row<to;row++){
const v=values[row];
const code=typeof v==='string'?this.#intern(v,true):-1;
this.#codeByRow[row]=code<0?0:code;
}
this.#length=Math.max(this.#length,to);
}
ranks(locale){
const key=locale||'';
if(this.#ranks&&this.#ranksGeneration===this.#generation&&this.#ranksLocale===key){
return this.#ranks;
}
const ranks=collateStringRanks(this.#table,this.#table.length,locale);
this.#ranks=ranks;
this.#ranksGeneration=this.#generation;
this.#ranksLocale=key;
return ranks;
}
usable(idx,values,locale){
if(this.#capped||this.#table.length===0)return false;
const codeByRow=this.#codeByRow;
const table=this.#table;
const covered=this.#length;
for(let i=0;i<idx.length;i++){
const row=idx[i];
if(row>=covered)return false;
if(table[codeByRow[row]]!==values[row])return false;
}
return true;
}
sort(idx,opts){
const ranks=this.ranks(opts.locale);
const d=this.#table.length;
const n=idx.length;
const codes=new Uint32Array(n);
const codeByRow=this.#codeByRow;
for(let i=0;i<n;i++)codes[i]=codeByRow[idx[i]];
return rankSortStrings(idx,codes,ranks,d,!!opts.descending);
}
}
});
__def("packages/core/src/store/columnstore.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"REMOVED",{enumerable:true,get:function(){return REMOVED;}});
Object.defineProperty(__exports,"toFloat",{enumerable:true,get:function(){return toFloat;}});
Object.defineProperty(__exports,"ColumnHandle",{enumerable:true,get:function(){return ColumnHandle;}});
Object.defineProperty(__exports,"ColumnStore",{enumerable:true,get:function(){return ColumnStore;}});
const __m0=__req("packages/core/src/internal/util.js");
const warnOnce=__m0["warnOnce"];
const isFunction=__m0["isFunction"];
const pathGetter=__m0["pathGetter"];
const __m1=__req("packages/core/src/store/bitset.js");
const Bitset=__m1["Bitset"];
const __m2=__req("packages/core/src/store/dictionary.js");
const Dictionary=__m2["Dictionary"];
const __m3=__req("packages/core/src/store/multivalue.js");
const MultiValue=__m3["MultiValue"];
const __m4=__req("packages/core/src/store/stringrank.js");
const StringRankIndex=__m4["StringRankIndex"];
const REMOVED=0xFFFFFFFF;
const DEFAULT_CAPACITY=1024;
const DEFAULT_COLUMNAR_BELOW=5000;
const DEFAULT_COMPACT_RATIO=0.2;
const KINDS=new Set(['float64','int32','bitset','dictionary','object','multi']);
function absent(v){return v===null||v===undefined;}
function toFloat(v){
if(typeof v==='number')return v;
if(v instanceof Date)return v.getTime();
if(typeof v==='boolean')return v?1:0;
const n=Number(v);
return Number.isNaN(n)&&typeof v==='string'?Date.parse(v):n;
}
function toInt(v){
const n=toFloat(v);
return Number.isFinite(n)?n|0:0;
}
function toMembers(v){
if(absent(v))return[];
return Array.isArray(v)?v:[v];
}
function decodeFrom(old,p){
switch(old.kind){
case'float64':
case'int32':return old.buf[p];
case'bitset':return old.bits.get(p)===1;
case'dictionary':return old.dict.valueOf(old.buf[p]);
case'multi':{
const codes=old.mv.at(p);
const out=new Array(codes.length);
for(let i=0;i<codes.length;i++)out[i]=old.dict.valueOf(codes[i]);
return out;
}
default:return old.buf[p];
}
}
function decodePacked(frag,i){
switch(frag.kind){
case'float64':
case'int32':return frag.values[i];
case'bitset':return(frag.values[i>>3]&(1<<(i&7)))!==0;
case'dictionary':return(frag.table||[])[frag.values[i]];
case'multi':{
const start=frag.offsets[i];
const end=frag.offsets[i+1];
const table=frag.table||[];
const out=new Array(end-start);
for(let k=start;k<end;k++)out[k-start]=table[frag.values[k]];
return out;
}
default:return frag.values[i];
}
}
class ColumnHandle{
#id;
#kind;
#target;
#nullable;
#read;
#host;
#seed;
#columnar=false;
#buf=null;
#bits=null;
#mv=null;
#dict=null;
#stringRank=null;
#stringRankOn;
#stringRankMax;
#stringRankVersion=-1;
#presence=null;
#capacity=0;
#version=0;
#overlay=null;
#cache=null;
#released=false;
constructor(schema,host){
this.#id=schema.id;
const kind=KINDS.has(schema.kind)?schema.kind:'object';
if(schema.kind&&!KINDS.has(schema.kind)){
warnOnce(`store.kind.${schema.id}`,
`column "${schema.id}" declares unknown storage kind "${schema.kind}"; falling back to object`);
}
this.#target=kind;
this.#kind=kind;
this.#nullable=schema.nullable!==false;
this.#seed=schema.dictionary??null;
this.#host=host;
this.#read=isFunction(schema.read)
?schema.read
:pathGetter(schema.field||schema.id);
this.#stringRankOn=schema.stringRankIndex!=='off';
this.#stringRankMax=typeof schema.stringRankMaxDistinct==='number'
?schema.stringRankMaxDistinct
:0;
this.#overlay=new Map();
}
get id(){return this.#id;}
get kind(){return this.#columnar?this.#kind:'object';}
get target(){return this.#target;}
get nullable(){return this.#nullable;}
get values(){
if(this.#released)return null;
if(!this.#columnar)return this.#lazy().values;
if(this.#kind==='bitset')return this.#bits.words;
if(this.#kind==='multi')return this.#mv.values;
return this.#buf;
}
get presence(){
if(this.#released||!this.#nullable)return null;
return this.#columnar?this.#presence:this.#lazy().presence;
}
get dict(){
if(this.#released||!this.#columnar)return null;
return this.#kind==='dictionary'||this.#kind==='multi'?this.#dict:null;
}
get stringRank(){
if(this.#released||!this.#stringRankOn)return null;
if(this.kind!=='object')return null;
const values=this.values;
const n=this.#host.physical();
if(!values||n===0)return null;
if(this.#stringRank===null){
if(typeof values[0]!=='string')return null;
const index=new StringRankIndex(this.#resolveStringRankMax());
index.build(values,n);
index.version=this.#version;
this.#stringRank=index;
this.#stringRankVersion=this.#version;
return index.capped?null:index;
}
if(this.#stringRank.length<n){
this.#stringRank.append(values,this.#stringRank.length,n-this.#stringRank.length);
}
this.#stringRank.version=this.#version;
this.#stringRankVersion=this.#version;
return this.#stringRank.capped?null:this.#stringRank;
}
#resolveStringRankMax(){return this.#stringRankMax;}
#dropStringRank(){
this.#stringRank=null;
this.#stringRankVersion=-1;
}
get offsets(){
if(this.#released||!this.#columnar||this.#kind!=='multi')return null;
return this.#mv.offsets;
}
get version(){return this.#version;}
get bytes(){
let total=0;
if(this.#buf)total+=this.#buf.byteLength??this.#buf.length*8;
if(this.#bits)total+=this.#bits.bytes;
if(this.#mv)total+=this.#mv.bytes;
if(this.#presence)total+=this.#presence.bytes;
if(this.#dict)total+=this.#dict.bytes;
if(this.#overlay)total+=this.#overlay.size*24;
return total;
}
get(physical){
if(this.#released)return undefined;
if(physical<0||physical>=this.#host.physical())return undefined;
if(!this.#columnar)return this.#rowValue(physical);
if(this.#nullable&&this.#presence.get(physical)===0)return null;
switch(this.#kind){
case'float64':
case'int32':return this.#buf[physical];
case'bitset':return this.#bits.get(physical)===1;
case'dictionary':return this.#dict.valueOf(this.#buf[physical]);
case'multi':{
const codes=this.#mv.at(physical);
const out=new Array(codes.length);
for(let i=0;i<codes.length;i++)out[i]=this.#dict.valueOf(codes[i]);
return out;
}
default:return this.#buf[physical];
}
}
set(physical,value){
if(this.#released)return;
if(this.#columnar)this.#writeValue(physical,value);
else this.#overlay.set(physical,value===undefined?null:value);
if(this.#stringRank)this.#dropStringRank();
this.#version++;
}
read(row){return this.#read(row);}
appendColumn(objects,from,n){
if(this.#released||n<=0)return;
if(!this.#columnar){this.#version++;return;}
const read=this.#read;
const nullable=this.#nullable;
const presence=this.#presence;
let sawAbsent=false;
switch(this.#kind){
case'float64':{
const buf=this.#buf;
for(let i=0;i<n;i++){
const v=read(objects[i]);
if(typeof v==='number'){
if(nullable)presence.set(from+i);
buf[from+i]=v;
continue;
}
const gone=v===null||v===undefined;
if(nullable)presence.assign(from+i,!gone);
else if(gone)sawAbsent=true;
buf[from+i]=gone?NaN:toFloat(v);
}
break;
}
case'int32':{
const buf=this.#buf;
for(let i=0;i<n;i++){
const v=read(objects[i]);
const gone=v===null||v===undefined;
if(nullable)presence.assign(from+i,!gone);
else if(gone)sawAbsent=true;
buf[from+i]=gone?0:toInt(v);
}
break;
}
case'bitset':{
const bits=this.#bits;
for(let i=0;i<n;i++){
const v=read(objects[i]);
const gone=v===null||v===undefined;
if(nullable)presence.assign(from+i,!gone);
else if(gone)sawAbsent=true;
bits.assign(from+i,!gone&&!!v);
}
break;
}
case'dictionary':{
const buf=this.#buf;
const dict=this.#dict;
for(let i=0;i<n;i++){
const v=read(objects[i]);
const gone=v===null||v===undefined;
if(nullable)presence.assign(from+i,!gone);
else if(gone)sawAbsent=true;
buf[from+i]=gone?0:dict.codeOf(v);
}
break;
}
case'multi':{
const mv=this.#mv;
const dict=this.#dict;
for(let i=0;i<n;i++){
const v=read(objects[i]);
const gone=v===null||v===undefined;
if(nullable)presence.assign(from+i,!gone);
else if(gone)sawAbsent=true;
const members=toMembers(v);
const codes=new Array(members.length);
for(let k=0;k<members.length;k++)codes[k]=dict.codeOf(members[k]);
mv.write(from+i,codes);
}
break;
}
default:{
const buf=this.#buf;
for(let i=0;i<n;i++){
const v=read(objects[i]);
const gone=v===null||v===undefined;
if(nullable)presence.assign(from+i,!gone);
else if(gone)sawAbsent=true;
buf[from+i]=gone?null:v;
}
break;
}
}
if(sawAbsent){
warnOnce(`store.null.${this.#id}`,
`column "${this.#id}" is declared non-nullable but received null; storing a filler value`);
}
if(this.#stringRank&&this.#kind==='object'){
this.#stringRank.append(this.#buf,from,n);
this.#stringRank.version=this.#version+1;
this.#stringRankVersion=this.#version+1;
}
this.#version++;
}
appendPacked(frag,from,n){
if(this.#released||n<=0||!frag)return;
if(!this.#columnar){this.#version++;return;}
const presence=this.#presence;
const fragPresence=frag.presence;
if(presence){
if(fragPresence){
for(let i=0;i<n;i++)presence.assign(from+i,(fragPresence[i>>3]&(1<<(i&7)))!==0);
}else{
for(let i=0;i<n;i++)presence.set(from+i);
}
}
const kindsAgree=frag.kind===this.#kind;
if(kindsAgree&&(this.#kind==='float64'||this.#kind==='int32')){
this.#buf.set(frag.values,from);
this.#version++;
return;
}
if(kindsAgree&&this.#kind==='bitset'){
const words=frag.values;
for(let i=0;i<n;i++)this.#bits.assign(from+i,(words[i>>3]&(1<<(i&7)))!==0);
this.#version++;
return;
}
if(kindsAgree&&this.#kind==='dictionary'){
const table=frag.table||[];
const remap=new Uint32Array(table.length);
for(let t=0;t<table.length;t++)remap[t]=this.#dict.codeOf(table[t]);
const codes=frag.values;
const buf=this.#buf;
const pres=presence;
for(let i=0;i<n;i++){
if(pres&&(fragPresence?(fragPresence[i>>3]&(1<<(i&7)))===0:false)){buf[from+i]=0;continue;}
buf[from+i]=remap[codes[i]]??0;
}
this.#version++;
return;
}
if(kindsAgree&&this.#kind==='multi'){
const table=frag.table||[];
const remap=new Uint32Array(table.length);
for(let t=0;t<table.length;t++)remap[t]=this.#dict.codeOf(table[t]);
const flat=frag.values;
const offsets=frag.offsets;
for(let i=0;i<n;i++){
const start=offsets[i];
const end=offsets[i+1];
const codes=new Array(end-start);
for(let k=start;k<end;k++)codes[k-start]=remap[flat[k]]??0;
this.#mv.write(from+i,codes);
}
this.#version++;
return;
}
for(let i=0;i<n;i++){
const gone=fragPresence
?(fragPresence[i>>3]&(1<<(i&7)))===0
:(frag.kind==='object'?frag.values[i]===null:false);
this.#writeValue(from+i,gone?null:decodePacked(frag,i));
}
this.#version++;
}
touch(){this.#version++;}
columnarise(capacity,fill){
if(this.#columnar||this.#released)return;
const values=new Array(fill);
for(let p=0;p<fill;p++)values[p]=this.#rowValue(p);
this.#kind=this.#target;
this.#columnar=true;
this.#alloc(capacity);
for(let p=0;p<fill;p++)this.#writeValue(p,values[p]);
this.#overlay.clear();
this.#cache=null;
this.#dropStringRank();
this.#version++;
}
grow(capacity){
if(!this.#columnar||this.#released||capacity<=this.#capacity)return;
switch(this.#kind){
case'float64':case'int32':case'dictionary':{
const next=new this.#buf.constructor(capacity);
next.set(this.#buf);
this.#buf=next;
break;
}
case'bitset':this.#bits.grow(capacity);break;
case'multi':break;
default:this.#buf.length=capacity;break;
}
if(this.#presence)this.#presence.grow(capacity);
this.#capacity=capacity;
this.#version++;
}
convert(kind){
if(this.#released||!KINDS.has(kind))return false;
this.#target=kind;
if(!this.#columnar||kind===this.#kind)return false;
const old={kind:this.#kind,buf:this.#buf,bits:this.#bits,mv:this.#mv,dict:this.#dict};
const n=this.#host.physical();
const presence=this.#presence;
this.#kind=kind;
this.#alloc(this.#capacity);
for(let p=0;p<n;p++){
const gone=presence!==null&&presence.get(p)===0;
if(gone&&kind!=='multi')continue;
this.#writeValue(p,gone?null:decodeFrom(old,p));
}
this.#dropStringRank();
this.#version++;
return true;
}
compact(remap,oldFill,liveCount){
if(this.#released)return;
this.#dropStringRank();
if(!this.#columnar){
const overlay=this.#overlay;
if(overlay.size){
const next=new Map();
for(const[p,v]of overlay)if(remap[p]!==REMOVED)next.set(remap[p],v);
this.#overlay=next;
}
this.#cache=null;
this.#version++;
return;
}
switch(this.#kind){
case'float64':case'int32':case'dictionary':case'object':{
const buf=this.#buf;
for(let p=0;p<oldFill;p++)if(remap[p]!==REMOVED)buf[remap[p]]=buf[p];
if(this.#kind==='object')for(let p=liveCount;p<oldFill;p++)buf[p]=undefined;
break;
}
case'bitset':{
const bits=this.#bits;
for(let p=0;p<oldFill;p++)if(remap[p]!==REMOVED)bits.assign(remap[p],bits.get(p)===1);
for(let p=liveCount;p<oldFill;p++)bits.clear(p);
break;
}
case'multi':this.#mv.compact(remap,liveCount,REMOVED);break;
default:break;
}
if(this.#presence){
const pres=this.#presence;
for(let p=0;p<oldFill;p++)if(remap[p]!==REMOVED)pres.assign(remap[p],pres.get(p)===1);
for(let p=liveCount;p<oldFill;p++)pres.clear(p);
}
this.#version++;
}
release(){
if(this.#released)return;
this.#released=true;
this.#buf=null;
if(this.#bits)this.#bits.release();
this.#bits=null;
if(this.#mv)this.#mv.release();
this.#mv=null;
if(this.#presence)this.#presence.release();
this.#presence=null;
this.#dict=null;
this.#stringRank=null;
this.#overlay=new Map();
this.#cache=null;
this.#capacity=0;
this.#version++;
}
#alloc(capacity){
const cap=Math.max(1,capacity);
this.#buf=null;
this.#bits=null;
this.#mv=null;
switch(this.#kind){
case'float64':this.#buf=new Float64Array(cap);break;
case'int32':this.#buf=new Int32Array(cap);break;
case'bitset':this.#bits=new Bitset(cap);break;
case'dictionary':
this.#buf=new Uint32Array(cap);
this.#dict=this.#dict??new Dictionary(this.#seed??[]);
break;
case'multi':
this.#mv=new MultiValue({rows:cap,values:cap});
this.#dict=this.#dict??new Dictionary(this.#seed??[]);
break;
default:this.#buf=new Array(cap);break;
}
if(this.#kind!=='dictionary'&&this.#kind!=='multi')this.#dict=null;
if(this.#nullable){
if(this.#presence)this.#presence.grow(cap);
else this.#presence=new Bitset(cap);
}
this.#capacity=cap;
}
#writeValue(p,value){
const gone=absent(value);
if(this.#nullable)this.#presence.assign(p,!gone);
else if(gone){
warnOnce(`store.null.${this.#id}`,
`column "${this.#id}" is declared non-nullable but received null; storing a filler value`);
}
switch(this.#kind){
case'float64':this.#buf[p]=gone?NaN:toFloat(value);break;
case'int32':this.#buf[p]=gone?0:toInt(value);break;
case'bitset':this.#bits.assign(p,!gone&&!!value);break;
case'dictionary':this.#buf[p]=gone?0:this.#dict.codeOf(value);break;
case'multi':{
const members=toMembers(value);
const codes=new Array(members.length);
for(let i=0;i<members.length;i++)codes[i]=this.#dict.codeOf(members[i]);
this.#mv.write(p,codes);
break;
}
default:this.#buf[p]=gone?null:value;break;
}
}
#rowValue(p){
if(this.#overlay.has(p))return this.#overlay.get(p);
const v=this.#read(this.#host.rowAt(p));
return v===undefined?null:v;
}
#lazy(){
if(this.#cache&&this.#cache.version===this.#version)return this.#cache;
const n=this.#host.physical();
const values=new Array(n);
const presence=this.#nullable?new Bitset(n):null;
for(let p=0;p<n;p++){
const v=this.#rowValue(p);
values[p]=v;
if(presence&&!absent(v))presence.set(p);
}
this.#cache={version:this.#version,values,presence};
return this.#cache;
}
}
class ColumnStore{
#schema;
#handles=new Map();
#list=[];
#rows=[];
#fill=0;
#capacity=0;
#live=0;
#dead=0;
#tombs=new Bitset(0);
#columnar=false;
#retainSource=true;
#columnarBelow;
#initial;
#ratio;
#destroyed=false;
constructor(schema,opts={}){
this.#schema=Array.isArray(schema)?schema:[];
this.#initial=Math.max(1,opts.initialCapacity??DEFAULT_CAPACITY);
this.#retainSource=opts.retainSource!==false;
this.#columnarBelow=this.#retainSource?(opts.columnarBelow??DEFAULT_COLUMNAR_BELOW):0;
this.#ratio=opts.compactRatio??DEFAULT_COMPACT_RATIO;
const host={
rowAt:(p)=>this.#rows[p],
physical:()=>this.#fill,
};
for(const entry of this.#schema){
if(!entry||!entry.id)continue;
if(this.#handles.has(entry.id)){
warnOnce(`store.dup.${entry.id}`,`duplicate column id "${entry.id}" in the store schema; ignoring the second`);
continue;
}
const handle=new ColumnHandle(entry,host);
this.#handles.set(entry.id,handle);
this.#list.push(handle);
}
if(this.#columnarBelow<=0)this.#columnarise();
}
get count(){return this.#live;}
get physical(){return this.#fill;}
get capacity(){return this.#columnar?this.#capacity:this.#rows.length;}
get tombstones(){return this.#dead;}
get columnar(){return this.#columnar;}
get destroyed(){return this.#destroyed;}
get bytes(){
let total=this.#tombs.bytes+this.#rows.length*8;
for(const h of this.#list)total+=h.bytes;
return total;
}
append(objects){
const from=this.#fill;
if(this.#destroyed||!objects)return{from,to:from};
const n=objects.length|0;
if(n===0)return{from,to:from};
if(this.#retainSource)for(let i=0;i<n;i++)this.#rows[from+i]=objects[i];
this.#fill=from+n;
this.#live+=n;
this.#tombs.grow(this.#fill);
if(!this.#columnar){
if(this.#fill>=this.#columnarBelow)this.#columnarise();
else for(const h of this.#list)h.touch();
return{from,to:this.#fill};
}
this.#ensure(this.#fill);
const cols=this.#list;
for(let c=0;c<cols.length;c++)cols[c].appendColumn(objects,from,n);
return{from,to:this.#fill};
}
appendPacked(chunk,objects){
const from=this.#fill;
if(this.#destroyed||!chunk)return{from,to:from};
const n=chunk.count|0;
if(n===0)return{from,to:from};
if(this.#retainSource&&objects){
for(let i=0;i<n;i++)this.#rows[from+i]=objects[i];
}
this.#fill=from+n;
this.#live+=n;
this.#tombs.grow(this.#fill);
if(!this.#columnar)this.#columnarise();
this.#ensure(this.#fill);
const byId=new Map();
for(const col of chunk.columns)byId.set(col.id,col);
for(const h of this.#list){
const frag=byId.get(h.id);
if(frag)h.appendPacked(frag,from,n);
else{
h.grow(this.#capacity);
}
}
return{from,to:this.#fill};
}
source(physical){
if(this.#retainSource)return this.#rows[physical];
if(physical<0||physical>=this.#fill)return undefined;
return this.#reconstruct(physical);
}
setSource(physical,object){
if(this.#destroyed)return;
if(physical<0||physical>=this.#fill)return;
if(!this.#retainSource)return;
this.#rows[physical]=object;
}
get(colId,physical){
const h=this.#handles.get(colId);
return h?h.get(physical):undefined;
}
set(colId,physical,value){
if(this.#destroyed)return;
if(physical<0||physical>=this.#fill)return;
const h=this.#handles.get(colId);
if(!h){
warnOnce(`store.set.${colId}`,`set() on unknown column "${colId}"`);
return;
}
h.set(physical,value);
}
remove(physical){
if(this.#destroyed||physical<0||physical>=this.#fill)return false;
if(this.#tombs.get(physical)===1)return false;
this.#tombs.set(physical);
this.#dead++;
this.#live--;
return true;
}
live(physical){
if(physical<0||physical>=this.#fill)return false;
return this.#tombs.get(physical)===0;
}
compact(opts={}){
if(this.#destroyed||this.#dead===0)return null;
if(!opts.force&&this.#dead/this.#fill<this.#ratio)return null;
const oldFill=this.#fill;
const remap=new Uint32Array(oldFill);
let w=0;
for(let p=0;p<oldFill;p++)remap[p]=this.#tombs.get(p)===1?REMOVED:w++;
for(const h of this.#list)h.compact(remap,oldFill,w);
if(this.#retainSource){
const rows=this.#rows;
for(let p=0;p<oldFill;p++)if(remap[p]!==REMOVED)rows[remap[p]]=rows[p];
rows.length=w;
}
this.#fill=w;
this.#live=w;
this.#dead=0;
this.#tombs=new Bitset(Math.max(this.#capacity,w));
return remap;
}
column(colId){return this.#handles.get(colId);}
columns(){return this.#list.slice();}
liveIndices(){
const out=new Uint32Array(this.#live);
if(this.#dead===0){
for(let p=0;p<this.#fill;p++)out[p]=p;
return out;
}
let k=0;
for(let p=0;p<this.#fill;p++)if(this.#tombs.get(p)===0)out[k++]=p;
return out;
}
convert(colId,kind){
const h=this.#handles.get(colId);
return h?h.convert(kind):false;
}
destroy(){
if(this.#destroyed)return;
this.#destroyed=true;
for(const h of this.#list)h.release();
this.#handles.clear();
this.#list.length=0;
this.#rows.length=0;
this.#tombs.release();
this.#fill=0;
this.#live=0;
this.#dead=0;
this.#capacity=0;
}
#ensure(n){
if(n<=this.#capacity)return;
let cap=this.#capacity||this.#initial;
while(cap<n)cap*=2;
for(const h of this.#list)h.grow(cap);
this.#tombs.grow(cap);
this.#capacity=cap;
}
#reconstruct(physical){
const out={};
for(const h of this.#list)out[h.id]=h.get(physical);
return out;
}
#columnarise(){
if(this.#columnar)return;
let cap=this.#initial;
while(cap<this.#fill)cap*=2;
this.#columnar=true;
this.#capacity=cap;
this.#tombs.grow(cap);
for(const h of this.#list)h.columnarise(cap,this.#fill);
}
}
});
__def("packages/core/src/compute/incremental.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"INCREMENTAL_FNS",{enumerable:true,get:function(){return INCREMENTAL_FNS;}});
Object.defineProperty(__exports,"INCREMENTAL_KINDS",{enumerable:true,get:function(){return INCREMENTAL_KINDS;}});
Object.defineProperty(__exports,"MAGNITUDE_LIMIT",{enumerable:true,get:function(){return MAGNITUDE_LIMIT;}});
Object.defineProperty(__exports,"isIncremental",{enumerable:true,get:function(){return isIncremental;}});
Object.defineProperty(__exports,"RunningTotal",{enumerable:true,get:function(){return RunningTotal;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__req("packages/core/src/store/columnstore.js");
const toFloat=__m0["toFloat"];
const INCREMENTAL_FNS=Object.freeze(['sum','avg','countValues','min','max']);
const INCREMENTAL_KINDS=Object.freeze(['float64']);
const MAGNITUDE_LIMIT=1e15;
function isIncremental(fn,handle){
if(typeof fn!=='string'||!INCREMENTAL_FNS.includes(fn))return false;
return!!handle&&INCREMENTAL_KINDS.includes((handle.kind));
}
function storedNumber(v){
if(v===null||v===undefined)return null;
const n=toFloat(v);
return Number.isNaN(n)?null:n;
}
class RunningTotal{
#fn;
#sum=0;
#comp=0;
#count=0;
#min=Infinity;
#max=-Infinity;
#valid=false;
#poisoned=false;
constructor(fn){this.#fn=fn;}
get valid(){return this.#valid&&!this.#poisoned;}
get fn(){return this.#fn;}
seed(values){
this.#reset();
for(let i=0;i<values.length;i++){
const v=storedNumber(values[i]);
if(v!==null)this.#add(v);
}
this.#valid=true;
}
seedColumn(values,present,indices){
this.#reset();
for(let i=0;i<indices.length;i++){
const row=indices[i];
if(present&&present(row)===0)continue;
const v=values[row];
if(Number.isNaN(v))continue;
this.#add(v);
}
this.#valid=true;
}
#reset(){
this.#sum=0;this.#comp=0;this.#count=0;
this.#min=Infinity;this.#max=-Infinity;
this.#poisoned=false;
}
#add(v){
if(!Number.isFinite(v)){this.#poisoned=true;return;}
const t=this.#sum+v;
this.#comp+=Math.abs(this.#sum)>=Math.abs(v)?(this.#sum-t)+v:(v-t)+this.#sum;
this.#sum=t;
this.#count++;
if(v<this.#min)this.#min=v;
if(v>this.#max)this.#max=v;
}
#remove(v){
const t=this.#sum-v;
this.#comp+=Math.abs(this.#sum)>=Math.abs(v)?(this.#sum-t)-v:(-v-t)+this.#sum;
this.#sum=t;
this.#count--;
}
update(before,after){
if(!this.#valid)return false;
const was=storedNumber(before);
const now=storedNumber(after);
if(was===null&&now===null)return true;
if((was!==null&&!Number.isFinite(was))||(now!==null&&!Number.isFinite(now)))return false;
if(this.#fn==='min'||this.#fn==='max'){
if(was===null||now===null)return false;
if(this.#fn==='min'){
if(now<this.#min){this.#min=now;return true;}
return was>this.#min;
}
if(now>this.#max){this.#max=now;return true;}
return was<this.#max;
}
if(Math.abs(this.#sum)>MAGNITUDE_LIMIT)return false;
if(was!==null)this.#remove(was);
if(now!==null)this.#add(now);
return true;
}
value(){
switch(this.#fn){
case'countValues':return this.#count;
case'avg':return this.#count?(this.#sum+this.#comp)/this.#count:null;
case'min':return this.#count?this.#min:null;
case'max':return this.#count?this.#max:null;
default:return this.#sum+this.#comp;
}
}
invalidate(){this.#valid=false;}
}
const __default=RunningTotal;
});
__def("packages/core/src/source/filterwire.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"HOST_FILTER_WARN_ID",{enumerable:true,get:function(){return HOST_FILTER_WARN_ID;}});
Object.defineProperty(__exports,"WHERE_PAGE_RELATIVE_WARN_ID",{enumerable:true,get:function(){return WHERE_PAGE_RELATIVE_WARN_ID;}});
Object.defineProperty(__exports,"warnWherePageRelative",{enumerable:true,get:function(){return warnWherePageRelative;}});
Object.defineProperty(__exports,"warnHostFilterNotApplied",{enumerable:true,get:function(){return warnHostFilterNotApplied;}});
Object.defineProperty(__exports,"WHERE_NOT_APPLIED_WARN_ID",{enumerable:true,get:function(){return WHERE_NOT_APPLIED_WARN_ID;}});
Object.defineProperty(__exports,"warnWhereNotApplied",{enumerable:true,get:function(){return warnWhereNotApplied;}});
Object.defineProperty(__exports,"WHERE_OVER_THRESHOLD_WARN_ID",{enumerable:true,get:function(){return WHERE_OVER_THRESHOLD_WARN_ID;}});
Object.defineProperty(__exports,"warnWhereOverThreshold",{enumerable:true,get:function(){return warnWhereOverThreshold;}});
Object.defineProperty(__exports,"RELATIVE_TOKENS",{enumerable:true,get:function(){return RELATIVE_TOKENS;}});
Object.defineProperty(__exports,"parseRelativeToken",{enumerable:true,get:function(){return parseRelativeToken;}});
Object.defineProperty(__exports,"resolveRelativeRange",{enumerable:true,get:function(){return resolveRelativeRange;}});
Object.defineProperty(__exports,"relativeTokenOf",{enumerable:true,get:function(){return relativeTokenOf;}});
Object.defineProperty(__exports,"relativeTokenHelp",{enumerable:true,get:function(){return relativeTokenHelp;}});
Object.defineProperty(__exports,"isResolvableRelative",{enumerable:true,get:function(){return isResolvableRelative;}});
Object.defineProperty(__exports,"resolveRelativeCondition",{enumerable:true,get:function(){return resolveRelativeCondition;}});
Object.defineProperty(__exports,"isGroup",{enumerable:true,get:function(){return isGroup;}});
Object.defineProperty(__exports,"forEachCondition",{enumerable:true,get:function(){return forEachCondition;}});
Object.defineProperty(__exports,"mapConditions",{enumerable:true,get:function(){return mapConditions;}});
Object.defineProperty(__exports,"normaliseFilterSet",{enumerable:true,get:function(){return normaliseFilterSet;}});
Object.defineProperty(__exports,"buildFilterSet",{enumerable:true,get:function(){return buildFilterSet;}});
Object.defineProperty(__exports,"wireFilters",{enumerable:true,get:function(){return wireFilters;}});
Object.defineProperty(__exports,"querySignature",{enumerable:true,get:function(){return querySignature;}});
const __m0=__req("packages/core/src/internal/util.js");
const warnOnce=__m0["warnOnce"];
const HOST_FILTER_WARN_ID='source.remote.hostFilter';
const WHERE_PAGE_RELATIVE_WARN_ID='source.where.pageRelative';
function warnWherePageRelative(names,mode){
if(!Array.isArray(names)||!names.length)return;
warnOnce(
WHERE_PAGE_RELATIVE_WARN_ID,
`filters.where (${names.join(', ')}) runs over the rows the ${mode} source fetched,`,
'not the whole dataset: match counts and totals are page-relative.',
'Give the predicate a { condition } twin so the engine can narrow the fetch itself.',
);
}
function warnHostFilterNotApplied(ctx){
const host=(ctx||{}).config;
if(!host||!host.hostFilter)return;
warnOnce(
HOST_FILTER_WARN_ID,
'config.hostFilter is not sent to a server-delegated source (paged/remote) and is not applied:',
'filtering only the fetched window would give wrong counts and totals.',
'Express the filter as a FilterSet condition, or send it through config.context.',
);
}
const WHERE_NOT_APPLIED_WARN_ID='source.where.notApplied';
function warnWhereNotApplied(name,mode){
warnOnce(
`${WHERE_NOT_APPLIED_WARN_ID}:${name}`,
`filters.where("${name}") is registered but the ${mode} source holds only its fetched window`,
'and cannot run the predicate over it: the predicate is not applied there, and rows it would',
'exclude stay on screen.',
'Give it a { condition } twin so the engine can narrow the fetch itself — that is the',
'supported route for a paged or remote source.',
);
}
const WHERE_OVER_THRESHOLD_WARN_ID='source.where.overThreshold';
function warnWhereOverThreshold(names,adapter,size,limit){
if(!Array.isArray(names)||!names.length)return;
warnOnce(
`${WHERE_OVER_THRESHOLD_WARN_ID}:${adapter}`,
`filters.where (${names.join(', ')}) is not applied to the ${adapter} pushdown source:`,
size===null
?'the adapter reports no row total, so the size of the matching set is unknown.'
:`its matching set is ${size.toLocaleString()} rows, at or past the whereRowLimit of `
+`${limit.toLocaleString()}.`,
'Running the predicate needs every matching row fetched and held here, which is the whole',
'download a pushdown source exists to avoid, so it is refused and the rows the predicate',
'would exclude stay on screen.',
'Give the predicate a { condition } twin so the engine narrows the fetch itself — that is the',
'route that works at any size — or raise `whereRowLimit` on the source config if you want the',
'download.',
);
}
const zoneFormatters=new Map();
function zoneFormatter(tz){
let f=zoneFormatters.get(tz);
if(!f){
f=new Intl.DateTimeFormat('en-US',{
timeZone:tz,
hourCycle:'h23',
year:'numeric',
month:'2-digit',
day:'2-digit',
hour:'2-digit',
minute:'2-digit',
second:'2-digit',
});
zoneFormatters.set(tz,f);
}
return f;
}
function localZone(){
try{
return Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';
}catch{
return'UTC';
}
}
function partsIn(ms,tz){
const parts=zoneFormatter(tz).formatToParts(new Date(ms));
const out={y:1970,m:1,d:1,h:0,mi:0,s:0};
for(const p of parts){
if(p.type==='year')out.y=+p.value;
else if(p.type==='month')out.m=+p.value;
else if(p.type==='day')out.d=+p.value;
else if(p.type==='hour')out.h=+p.value%24;
else if(p.type==='minute')out.mi=+p.value;
else if(p.type==='second')out.s=+p.value;
}
return out;
}
function zoneOffset(ms,tz){
const p=partsIn(ms,tz);
const asUtc=Date.UTC(p.y,p.m-1,p.d,p.h,p.mi,p.s);
return asUtc-Math.floor(ms/1000)*1000;
}
function startOfLocalDay(y,m,d,tz){
const naive=Date.UTC(y,m-1,d);
const first=naive-zoneOffset(naive,tz);
const second=naive-zoneOffset(first,tz);
return second;
}
function localDayPlus(date,n,tz){
const shifted=new Date(Date.UTC(date.y,date.m-1,date.d+n));
return startOfLocalDay(
shifted.getUTCFullYear(),shifted.getUTCMonth()+1,shifted.getUTCDate(),tz,
);
}
const RELATIVE_TOKENS=Object.freeze([
'today',
'yesterday',
'lastNDays',
'thisWeek',
'thisMonth',
'thisQuarter',
'thisYear',
'quarterToDate',
'yearToDate',
]);
function parseRelativeToken(token,fallbackN){
if(typeof token!=='string')return null;
const flat=token.trim().toLowerCase().replace(/[\s_-]+/g,'');
const counted=/^last(\d*|n)days?$/.exec(flat);
if(counted){
const digits=counted[1]==='n'?'':counted[1];
const n=digits?Number(digits):Number(fallbackN);
if(!Number.isFinite(n)||n<=0)return null;
return{kind:'lastNDays',n:Math.floor(n)};
}
switch(flat){
case'today':return{kind:'today',n:0};
case'yesterday':return{kind:'yesterday',n:0};
case'thisweek':return{kind:'thisWeek',n:0};
case'thismonth':return{kind:'thisMonth',n:0};
case'thisquarter':return{kind:'thisQuarter',n:0};
case'thisyear':return{kind:'thisYear',n:0};
case'quartertodate':case'qtd':return{kind:'quarterToDate',n:0};
case'yeartodate':case'ytd':return{kind:'yearToDate',n:0};
default:return null;
}
}
function resolveRelativeRange(token,opts={}){
const parsed=parseRelativeToken(token,opts.n);
if(!parsed)return null;
const tz=opts.tz||localZone();
const nowMs=opts.now instanceof Date?opts.now.getTime()
:typeof opts.now==='number'?opts.now:Date.now();
const today=partsIn(nowMs,tz);
const startToday=startOfLocalDay(today.y,today.m,today.d,tz);
const startTomorrow=localDayPlus(today,1,tz);
let from=startToday;
let to=startTomorrow;
switch(parsed.kind){
case'today':
break;
case'yesterday':
from=localDayPlus(today,-1,tz);
to=startToday;
break;
case'lastNDays':
to=opts.includeToday?startTomorrow:startToday;
from=localDayPlus(today,opts.includeToday?-(parsed.n-1):-parsed.n,tz);
break;
case'thisWeek':{
const startsOn=Number.isFinite(opts.weekStartsOn)?Number(opts.weekStartsOn):1;
const dow=new Date(Date.UTC(today.y,today.m-1,today.d)).getUTCDay();
const back=(dow-startsOn+7)%7;
from=localDayPlus(today,-back,tz);
to=localDayPlus(today,-back+7,tz);
break;
}
case'thisMonth':
from=startOfLocalDay(today.y,today.m,1,tz);
to=startOfLocalDay(today.m===12?today.y+1:today.y,today.m===12?1:today.m+1,1,tz);
break;
case'thisQuarter':
case'quarterToDate':{
const qStart=Math.floor((today.m-1)/3)*3+1;
from=startOfLocalDay(today.y,qStart,1,tz);
to=parsed.kind==='quarterToDate'
?startTomorrow
:(qStart===10
?startOfLocalDay(today.y+1,1,1,tz)
:startOfLocalDay(today.y,qStart+3,1,tz));
break;
}
case'thisYear':
case'yearToDate':
from=startOfLocalDay(today.y,1,1,tz);
to=parsed.kind==='yearToDate'?startTomorrow:startOfLocalDay(today.y+1,1,1,tz);
break;
default:
return null;
}
return{
from:new Date(from).toISOString(),
to:new Date(to).toISOString(),
bounds:'[)',
tz,
relative:parsed.kind==='lastNDays'?`last${parsed.n}Days`:parsed.kind,
n:parsed.n,
};
}
function relativeTokenOf(condition){
if(!condition||typeof condition!=='object')return null;
const value=(condition.value);
if(value&&typeof value==='object'&&!Array.isArray(value)&&'relative'in value){
return{token:String(value.relative),n:typeof value.n==='number'?value.n:undefined};
}
if(condition.op==='relative'&&typeof condition.value==='string'){
return{token:condition.value,n:undefined};
}
const meta=condition.meta;
if(meta&&typeof meta.relative==='string'&&condition.value===undefined){
return{token:meta.relative,n:typeof meta.n==='number'?meta.n:undefined};
}
return null;
}
function relativeTokenHelp(){
const plain=RELATIVE_TOKENS.filter((token)=>token!=='lastNDays');
return`Known tokens: ${plain.join(', ')}. "lastNDays" needs a count: pass "last7Days", `
+"or the value object { relative: 'lastNDays', n: 7 }.";
}
function isResolvableRelative(condition){
const found=relativeTokenOf(condition);
if(!found)return false;
return parseRelativeToken(found.token,found.n)!==null;
}
function resolveRelativeCondition(condition,opts={}){
const found=relativeTokenOf(condition);
if(!found)return condition;
const range=resolveRelativeRange(found.token,{...opts,n:found.n});
if(!range){
warnOnce(
`source.relative.${found.token}`,
`unknown relative date token "${found.token}"; sending the condition unresolved.`,
relativeTokenHelp(),
);
return condition;
}
return{
...condition,
type:condition.type||'date',
op:'between',
value:[range.from,range.to],
bounds:condition.bounds||range.bounds,
meta:{...(condition.meta||{}),relative:range.relative,tz:range.tz},
};
}
function isGroup(node){
return!!node&&typeof node==='object'&&Array.isArray((node).conditions);
}
function forEachCondition(filters,visit){
if(!filters)return;
if(isGroup(filters)){
for(const child of(filters).conditions)forEachCondition(child,visit);
return;
}
visit((filters));
}
function mapConditions(filters,fn){
if(!filters)return null;
if(!isGroup(filters))return fn((filters));
const group=(filters);
let changed=false;
const out=[];
for(const child of group.conditions){
const mapped=mapConditions(child,fn);
if(mapped!==child)changed=true;
if(mapped)out.push(mapped);
}
if(!changed&&out.length===group.conditions.length)return filters;
return out.length?{...group,conditions:out}:null;
}
function prune(filters){
if(!filters)return null;
if(!isGroup(filters))return filters;
const group=(filters);
const kept=[];
for(const child of group.conditions){
const p=prune(child);
if(p)kept.push(p);
}
return kept.length?{op:group.op,conditions:kept}:null;
}
function normaliseFilterSet(filters){
const pruned=prune(filters);
if(!pruned)return null;
if(isGroup(pruned)&&(pruned).op==='and'){
return(pruned);
}
return{op:'and',conditions:[pruned]};
}
function buildFilterSet(conditions){
const list=(conditions||[]).filter(Boolean);
return list.length?{op:'and',conditions:list}:null;
}
function wireFilters(filters,opts={}){
const resolved=mapConditions(filters,(c)=>resolveRelativeCondition(c,opts));
return normaliseFilterSet(resolved);
}
function querySignature(query){
const filterKey=signFilters(query.filters??null);
return JSON.stringify([
filterKey,
query.sort??null,
query.quick??'',
query.groupBy??null,
query.pivotBy??null,
safeContext(query.context),
]);
}
function signFilters(filters){
if(!filters)return null;
if(isGroup(filters)){
const g=(filters);
return{op:g.op,conditions:g.conditions.map(signFilters)};
}
const c=(filters);
const token=c.meta&&typeof c.meta.relative==='string'?c.meta.relative:relativeTokenOf(c)?.token;
return{
col:c.col,
op:c.op,
bounds:c.bounds??null,
caseSensitive:c.caseSensitive??false,
value:token?`rel:${token}`:c.value??null,
};
}
function safeContext(context){
if(context===undefined||context===null)return null;
try{
return JSON.parse(JSON.stringify(context));
}catch{
warnOnce('source.context.unserialisable','config.context is not JSON-serialisable; cache keys ignore it.');
return'[unserialisable]';
}
}
});
__def("packages/core/src/source/memory.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"STAGES",{enumerable:true,get:function(){return STAGES;}});
Object.defineProperty(__exports,"PIPELINE_SETTLE_REASONS",{enumerable:true,get:function(){return PIPELINE_SETTLE_REASONS;}});
Object.defineProperty(__exports,"isPipelineSettle",{enumerable:true,get:function(){return isPipelineSettle;}});
Object.defineProperty(__exports,"createMemorySource",{enumerable:true,get:function(){return createMemorySource;}});
Object.defineProperty(__exports,"quickMatcher",{enumerable:true,get:function(){return quickMatcher;}});
Object.defineProperty(__exports,"MemorySource",{enumerable:true,get:function(){return MemorySource;}});
const __m0=__req("packages/core/src/internal/util.js");
const Lru=__m0["Lru"];
const warnOnce=__m0["warnOnce"];
const fail=__m0["fail"];
const isFunction=__m0["isFunction"];
const defaultCompare=__m0["defaultCompare"];
const mergeRow=__m0["mergeRow"];
const __m1=__req("packages/core/src/source/rows.js");
const createRowFactory=__m1["createRowFactory"];
const groupKey=__m1["groupKey"];
const groupPathElement=__m1["groupPathElement"];
const groupPrefixKeys=__m1["groupPrefixKeys"];
const lazyRowView=__m1["lazyRowView"];
const __m2=__req("packages/core/src/source/handles.js");
const createHandleProvider=__m2["createHandleProvider"];
const __m3=__req("packages/core/src/compute/incremental.js");
const RunningTotal=__m3["RunningTotal"];
const isIncremental=__m3["isIncremental"];
const __m4=__req("packages/core/src/compute/handle.js");
const presenceReader=__m4["presenceReader"];
const __m5=__req("packages/core/src/source/filterwire.js");
const querySignature=__m5["querySignature"];
const mapConditions=__m5["mapConditions"];
const resolveRelativeCondition=__m5["resolveRelativeCondition"];
const __m6=__req("packages/core/src/source/tree.js");
const buildTree=__m6["buildTree"];
const resolveTree=__m6["resolveTree"];
const indexTree=__m6["indexTree"];
const STAGES=Object.freeze(['filter','sort','group','total','pivot','flatten']);
const PIPELINE_SETTLE_REASONS=Object.freeze(['pipeline:sort','pipeline:sort:error']);
function isPipelineSettle(payload){
const reason=payload&&typeof payload==='object'
?(payload).reason:null;
return typeof reason==='string'&&PIPELINE_SETTLE_REASONS.includes(reason);
}
const EMPTY_HEADINGS=Object.freeze([]);
function totalFnFor(column,scope){
if(!column)return null;
const override=scope==='grand'?column.grandTotal:column.groupTotal;
return override!=null?override:column.total;
}
function reducibleInTotals(column){
return!!column&&!column.running;
}
function comparatorFor(column){
if(!column)return undefined;
if(column.value&&isFunction(column.value.compare))return column.value.compare;
return isFunction(column.compare)?column.compare:undefined;
}
function isPromise(value){
return!!value&&(typeof value==='object'||typeof value==='function')
&&isFunction((value).then);
}
const EMPTY_INDICES=new Uint32Array(0);
const DEFAULT_ROW_CACHE=4096;
function createMemorySource(config,ctx){
return new MemorySource(config,ctx);
}
function quickMatcher(needle,mode){
if(mode==='regex'){
try{
const re=new RegExp(needle,'iu');
return(text)=>re.test(text);
}catch{
return(text)=>text.includes(needle);
}
}
if(mode==='fuzzy'){
const chars=[...needle].filter((c)=>c!==' ');
if(!chars.length)return()=>true;
return(text)=>{
let at=0;
for(let i=0;i<text.length&&at<chars.length;i++){
if(text[i]===chars[at])at++;
}
return at===chars.length;
};
}
if(mode==='words'){
const terms=needle.split(/\s+/).filter(Boolean);
if(!terms.length)return()=>true;
return(text)=>terms.every((t)=>text.includes(t));
}
return(text)=>text.includes(needle);
}
class MemorySource{
mode='memory';
#ctx;
#config;
#rows;
#dropped=false;
#priorRows=null;
#factory;
#handles;
#stages=new Map();
#pipelineActive=false;
#dataVersion=0;
#queryVersion=0;
#stateCache=new Map();
#keyMemo=new Map();
#rowCache;
#keyIndex=null;
#snapshot=MemorySource.#emptySnapshot();
static#emptySnapshot(){
return{
slots:[],groupHeadings:EMPTY_HEADINGS,pivot:null,inlineLeaves:null,
matchCount:0,grand:null,grandLeaves:0,groupLeaves:null,
};
}
#inflight=new Map();
#pipelineWorker=null;
#running=new Map();
#runningId=-1;
#totalDeltas=[];
#groupRunning=new Map();
#groupRunningId=-1;
#groupDeltas=[];
#totalsDirty=null;
#totalsBasis=null;
#lastGrand=null;
#unfilteredCache=null;
#unfilteredGroupCache=null;
#treeMeta=null;
#treeTotals=null;
#tree=null;
#treeFor=Symbol('unset');
#quickText=new Map();
#view=null;
get#slots(){return this.#snapshot.slots;}
set#slots(value){this.#snapshot.slots=value;}
get#inlineLeaves(){return this.#snapshot.inlineLeaves;}
set#inlineLeaves(value){this.#snapshot.inlineLeaves=value;}
get#groupHeadings(){return this.#snapshot.groupHeadings;}
set#groupHeadings(value){this.#snapshot.groupHeadings=value;}
get#pivot(){return this.#snapshot.pivot;}
set#pivot(value){this.#snapshot.pivot=value;}
constructor(config,ctx){
this.#config=config||{mode:'memory'};
this.#ctx=ctx||{};
const gridConfig=(this.#ctx.config)||{};
this.#rows=(this.#config.rows||gridConfig.rows||[]);
this.#factory=createRowFactory((this.#ctx));
this.#handles=createHandleProvider(this.#ctx,()=>this.#rows,()=>this.#dropped);
this.#rowCache=new Lru(Number(gridConfig.rowCacheSize)||DEFAULT_ROW_CACHE);
for(const name of STAGES)this.#stages.set(name,{key:null,result:null,id:0,computed:false});
}
count(){
this.#run();
const page=this.#pageSlice();
return page?page.length:this.#slots.length;
}
#pageSlice(){
const size=Number(this.#state('pageSize',0))||0;
if(size<=0)return null;
const total=this.#slots.length;
const pages=Math.max(1,Math.ceil(total/size));
const page=Math.min(Math.max(0,Number(this.#state('page',0))||0),pages-1);
const offset=page*size;
return{offset,length:Math.max(0,Math.min(size,total-offset))};
}
pageInfo(){
this.#run();
const size=Number(this.#state('pageSize',0))||0;
const total=this.#slots.length;
if(size<=0)return{page:0,pageSize:0,total,pageCount:1};
const pageCount=Math.max(1,Math.ceil(total/size));
const page=Math.min(Math.max(0,Number(this.#state('page',0))||0),pageCount-1);
return{page,pageSize:size,total,pageCount};
}
totalCount(){
return this.#baseIndices().length;
}
matchCount(){
this.#run();
return this.#snapshot.matchCount;
}
at(index){
this.#run();
const page=this.#pageSlice();
if(page){
if(index<0||index>=page.length)return undefined;
return this.#materialise(page.offset+index,index);
}
if(index<0||index>=this.#slots.length)return undefined;
return this.#materialise(index,index);
}
byKey(key){
this.#run();
if(!this.#keyIndex)this.#buildKeyIndex();
const physical=this.#keyIndex.get(key);
if(physical!==undefined)return this.#leafRow(physical);
for(let i=0;i<this.#slots.length;i++){
const slot=this.#slots[i];
if(typeof slot!=='number'&&slot&&slot.key===key)return this.#materialise(i);
}
return undefined;
}
loaded(index){
return index>=0&&index<this.count();
}
hint(_start,_end){}
display(){
this.#run();
if(!this.#view)this.#view=this.#buildView();
return this.#view;
}
invalidateQuickText(){
this.#quickText.clear();
this.invalidate('filter');
}
invalidate(stage){
const from=STAGES.indexOf(stage);
if(from<0)fail(`unknown pipeline stage "${stage}"; expected one of ${STAGES.join(', ')}`);
for(let i=from;i<STAGES.length;i++){
const entry=this.#stages.get(STAGES[i]);
entry.key=null;
entry.computed=false;
}
this.#view=null;
}
apply(change){
const rejected=[];
const result={added:[],updated:[],removed:[],rejected};
if(!change)return result;
let structural=false;
const touched=new Set();
if(change.remove&&change.remove.length){
for(const target of change.remove){
const physical=this.#physicalOf(target);
if(physical===undefined){
rejected.push({operation:'remove',id:this.#keyOfTarget(target),reason:'unknown-id'});
continue;
}
result.removed.push(this.#factory.keyOf(
this.#dropped?this.#dataAt(physical):this.#rows[physical],physical,
));
if(!this.#dropped)this.#rows[physical]=undefined;
this.#rowCache.delete(physical);
this.#quickText.delete(physical);
structural=true;
}
}
if(change.update&&change.update.length){
const totalCols=this.#running.size||this.#onlyChangedTotals()
?this.#totalColumns()
:null;
const groupCols=this.#groupRunning.size?this.#groupByColumns():null;
const groupTotalCols=groupCols&&groupCols.length?this.#totalColumns():null;
for(const next of change.update){
const physical=this.#physicalOf(next);
if(physical===undefined){
rejected.push({operation:'update',id:this.#rowKeyOf(next),reason:'unknown-id'});
continue;
}
const previous=this.#dropped
?(this.#priorRows&&this.#priorRows.has(physical)
?this.#priorRows.get(physical):this.#dataAt(physical))
:this.#rows[physical];
const merged=mergeRow(previous,next);
for(const colId of this.#changedColumns(previous,merged))touched.add(colId);
if(!this.#dropped)this.#rows[physical]=merged;
if(totalCols)this.#recordDeltas(totalCols,physical,previous,merged);
if(groupTotalCols){
this.#recordGroupDeltas(groupCols,groupTotalCols,physical,previous,merged);
}
if(this.#keyIndex){
const wasKey=this.#factory.keyOf(previous,physical);
const nowKey=this.#factory.keyOf(merged,physical);
if(wasKey!==nowKey){
this.#keyIndex.delete(wasKey);
this.#keyIndex.set(nowKey,physical);
}
}
this.#rowCache.delete(physical);
this.#quickText.delete(physical);
result.updated.push(this.#leafRow(physical));
}
}
if(change.add&&change.add.length&&this.#dropped){
if(typeof change.at==='number'&&change.at<this.#physicalCount()){
this.#rowCache.clear();
this.#quickText.clear();
}
this.#keyIndex=null;
for(const item of change.add){
const physical=this.#byKeyIndex(this.#rowKeyOf(item));
if(physical!==undefined)result.added.push(this.#leafRow(physical));
}
structural=true;
}else if(change.add&&change.add.length){
const at=typeof change.at==='number'?change.at:this.#rows.length;
const before=this.#rows.length;
let additions=change.add===this.#rows?change.add.slice():change.add;
if(this.#factory.keyed){
const kept=[];
for(const item of additions){
const key=this.#rowKeyOf(item);
if(this.#physicalOf(key)!==undefined){
rejected.push({operation:'add',id:key,reason:'duplicate-id'});
continue;
}
kept.push(item);
}
if(kept.length!==additions.length)additions=kept;
}
if(!additions.length)return result;
if(at>=before){
for(let i=0;i<additions.length;i++)this.#rows.push(additions[i]);
}else{
this.#rows.splice(at,0,...additions);
this.#rowCache.clear();
this.#quickText.clear();
}
for(let i=0;i<additions.length;i++){
result.added.push(this.#leafRow(at>=before?before+i:at+i));
}
structural=true;
}
this.#dataVersion++;
if(structural){
this.#keyIndex=null;
this.#dropTotalState();
}
this.#handles.invalidate();
this.#view=null;
if(structural||this.#affectsQuery(touched)){
this.#queryVersion++;
this.invalidate('filter');
}else{
this.invalidate('total');
}
this.#emit('rows:changed',{
companion:true,
added:result.added.length,updated:result.updated.length,removed:result.removed.length,
});
this.#priorRows=null;
return result;
}
groupHeadingsAt(index){
this.#run();
const list=this.#groupHeadings;
if(!list.length||!Number.isInteger(index)||index<0)return EMPTY_HEADINGS;
let low=0;
let high=list.length-1;
let found=-1;
while(low<=high){
const mid=(low+high)>>1;
if(list[mid].at<=index){found=mid;low=mid+1;}else{high=mid-1;}
}
if(found<0)return EMPTY_HEADINGS;
const chain=[];
for(let row=list[found].row;row;row=row.parent)chain.push(row);
chain.reverse();
return chain;
}
moveRow(from,to){
if(this.#dropped){
warnOnce(
'source.moveRow.dropped',
'rows.move() is not supported under ingest.dropSourceRows: the source '
+'objects have been released and the store cannot reorder its columns in '
+'place. Enable retainSource (leave dropSourceRows off) to reorder rows.',
);
return false;
}
const rows=this.#rows;
if(!Number.isInteger(from)||!Number.isInteger(to))return false;
if(from<0||from>=rows.length)return false;
const target=Math.max(0,Math.min(rows.length-1,to));
if(target===from)return false;
const[moved]=rows.splice(from,1);
rows.splice(target,0,moved);
this.#dataVersion++;
this.#queryVersion++;
this.#keyIndex=null;
this.#rowCache.clear();
this.#quickText.clear();
this.#dropTotalState();
this.#handles.invalidate();
this.#view=null;
this.invalidate('filter');
this.#emit('rows:changed',{
companion:true,added:0,updated:0,removed:0,moved:1,
});
return true;
}
reload(_opts){
this.#abortInflight();
this.#dataVersion++;
this.#queryVersion++;
this.#keyIndex=null;
this.#quickText.clear();
this.#rowCache.clear();
this.#dropTotalState();
this.#handles.invalidate();
this.invalidate('filter');
this.#emit('model:changed',{reason:'reload'});
}
destroy(){
this.#abortInflight();
this.#rowCache.clear();
this.#quickText.clear();
this.#keyIndex=null;
this.#slots=[];
this.#inlineLeaves=null;
this.#view=null;
this.#dropTotalState();
for(const name of STAGES)this.#stages.set(name,{key:null,result:null,id:0,computed:false});
}
#abortInflight(){
for(const{controller}of this.#inflight.values()){
try{controller.abort();}catch{}
}
this.#inflight.clear();
}
setRows(rows){
this.#rows=rows||[];
this.#dropped=false;
this.#dataVersion++;
this.#queryVersion++;
this.#keyIndex=null;
this.#quickText.clear();
this.#rowCache.clear();
this.#dropTotalState();
this.#handles.invalidate();
this.invalidate('filter');
}
notePriorRows(prior){
this.#priorRows=prior||null;
}
releaseSource(){
this.#dropped=true;
this.#rows=[];
this.#keyIndex=null;
this.#rowCache.clear();
this.#quickText.clear();
this.#handles.invalidate();
}
data(){
if(!this.#dropped)return this.#rows;
const store=(this.#ctx.store);
if(!store||!isFunction(store.source))return this.#rows;
const n=store.physical||0;
const out=new Array(n);
for(let p=0;p<n;p++)out[p]=store.live&&!store.live(p)?undefined:store.source(p);
return out;
}
#detailInline(){
const detail=this.#state('detail',null);
if(!detail||!isFunction(detail.isOpen)||detail.inline===false)return null;
return detail;
}
#treeConfig(){
const raw=(this.#ctx.config||{}).tree;
if(this.#treeFor!==raw){
this.#treeFor=raw;
this.#tree=resolveTree(raw,{
dataAt:(physical)=>this.#dataAt(physical),
keyOf:(physical)=>this.#factory.keyOf(this.#dataAt(physical),physical),
});
}
return this.#tree;
}
#grandTotalPinned(){
return(this.#ctx.config||{}).grandTotalRow==='bottom';
}
grandTotalRow(){
if(!this.#grandTotalPinned())return null;
this.#run();
const grand=this.#snapshot.grand;
if(!grand)return null;
return this.#factory.grandTotal(grand,this.#grandLeafCount(this.#snapshot.grandLeaves));
}
pivot(){
this.#run();
return this.#pivot;
}
pivotMatrix(){
this.#run();
const leaves=this.#snapshot.groupLeaves;
const build=(this.#ctx.compute||{}).pivotMatrix;
if(!isFunction(build))return null;
const rowDims=this.#state('groupBy',[]);
const columnDims=this.#state('pivotBy',[]);
if(!rowDims.length&&!columnDims.length)return null;
const measures=this.#totalColumns().map((c)=>({
colId:c.id,
handle:this.#handles.handle(c.id),
fn:totalFnFor(c,'group'),
})).filter((m)=>m.handle&&m.fn);
const config=(this.#ctx.config||{});
return build({
rowHandles:rowDims.map((id)=>this.#handles.handle(id)).filter(Boolean),
columnHandles:columnDims.map((id)=>this.#handles.handle(id)).filter(Boolean),
measures,
leaves:leaves||new Uint32Array(0),
opts:{
locale:config.locale,
separator:(config.pivot||{}).separator,
maxColumns:(config.pivot||{}).maxColumns,
},
});
}
keysForLeaves(indices){
const out=[];
if(!indices)return out;
for(let i=0;i<indices.length;i++){
const row=this.#leafRow(indices[i]);
if(row&&row.key!=null)out.push(String(row.key));
}
return out;
}
leavesOf(key){
this.#run();
const entry=this.#stages.get('group');
const grouped=entry&&entry.result;
if(!grouped||!grouped.nodes)return[];
const node=grouped.nodes.get(key);
if(!node)return[];
const out=[];
const collect=(n)=>{
const children=n.row&&n.row.children;
if(children&&children.length){
for(const child of children){
const sub=grouped.nodes.get(child.key);
if(sub)collect(sub);
}
return;
}
if(!n.leaves)return;
for(let i=0;i<n.leaves.length;i++)out.push(this.#leafRow(n.leaves[i]));
};
collect(node);
return out;
}
#keyOfTarget(target){
return typeof target==='string'?target:this.#rowKeyOf(target);
}
#rowKeyOf(data){
try{return this.#factory.keyOf(data,-1);}catch{return'(unkeyed)';}
}
#physicalOf(target){
if(typeof target==='string')return this.#byKeyIndex(target);
if(this.#factory.keyed){
const key=this.#factory.keyOf(target,-1);
const found=this.#byKeyIndex(key);
if(found!==undefined)return found;
}
if(this.#dropped)return undefined;
const direct=this.#rows.indexOf(target);
return direct>=0?direct:undefined;
}
#byKeyIndex(key){
if(!this.#keyIndex)this.#buildKeyIndex();
return this.#keyIndex.get(key);
}
#buildKeyIndex(){
this.#keyIndex=new Map();
const n=this.#physicalCount();
const store=(this.#ctx.store);
const isLive=store&&isFunction(store.live)?(i)=>store.live(i):null;
for(let i=0;i<n;i++){
if(isLive&&!isLive(i))continue;
const data=this.#dataAt(i);
if(data===undefined)continue;
this.#keyIndex.set(this.#factory.keyOf(data,i),i);
}
}
#changedColumns(previous,next){
const changed=[];
for(const colId of this.#queryColumns()){
if(this.#column(colId)&&this.#moved(colId,previous,next,new Set()))changed.push(colId);
}
return changed;
}
#moved(colId,previous,next,seen){
if(seen.has(colId))return false;
seen.add(colId);
const column=this.#column(colId);
if(!column)return false;
const deps=column.value&&column.value.deps;
if(Array.isArray(deps)&&deps.length){
return deps.some((dep)=>this.#moved(String(dep),previous,next,seen));
}
if(!isFunction(column.getValue))return false;
return!Object.is(column.getValue(previous),column.getValue(next));
}
#queryColumns(){
const ids=new Set();
for(const entry of this.#state('sort',[]))ids.add(entry.col);
for(const colId of this.#state('groupBy',[]))ids.add(colId);
const walk=(node)=>{
if(!node)return;
if(Array.isArray(node.conditions)){node.conditions.forEach(walk);return;}
if(node.col)ids.add(node.col);
};
walk(this.#state('filters',null));
return[...ids];
}
#affectsQuery(touched){
if(!touched.size)return false;
if(this.#state('quick',''))return true;
for(const colId of this.#queryColumns())if(touched.has(colId))return true;
return false;
}
#run(){
if(this.#pipelineActive)return;
this.#pipelineActive=true;
try{
this.#runStages();
}finally{
this.#pipelineActive=false;
}
}
pipelineRunning(){return this.#pipelineActive;}
#runStages(){
const filtered=this.#stage('filter',()=>this.#filterKey(),()=>this.#runFilter());
const sorted=this.#sortStage(filtered);
const grouped=this.#stage('group',()=>this.#groupKey(),()=>this.#runGroup(sorted));
const totalled=this.#stage('total',()=>this.#totalKey(),()=>this.#runTotal(grouped));
this.#stage('pivot',()=>this.#pivotKey(),()=>this.#runPivot(grouped));
this.#stage('flatten',()=>this.#flattenKey(),()=>this.#runFlatten(grouped));
this.#publishSnapshot(grouped,totalled);
}
#sortStage(filtered){
const entry=this.#stages.get('sort');
const key=this.#sortKey();
if(entry.computed&&entry.key===key&&!this.#inflight.has('sort'))return entry.result;
const pending=this.#inflight.get('sort');
if(pending&&pending.key===key)return entry.result;
if(entry.result&&this.#sortOffloadable(filtered)){
this.#offloadSort(filtered,key);
return entry.result;
}
return this.#stage('sort',()=>key,()=>this.#runSort(filtered));
}
#pipelineHost(){
if(this.#pipelineWorker===null){
const provider=(this.#ctx).pipelineWorker;
const host=isFunction(provider)?provider():null;
this.#pipelineWorker=host||false;
}
return this.#pipelineWorker||null;
}
#portableSort(){
const model=this.#state('sort',[]);
if(!Array.isArray(model)||!model.length)return null;
const locale=(this.#ctx.config||{}).locale;
const entries=[];
const handles=[];
for(const entry of model){
const column=this.#column(entry.col);
if(column&&column.running)continue;
if(column&&column.value&&isFunction(column.value.compare))return null;
const handle=this.#handles.handle(entry.col);
if(!handle)return null;
entries.push({
col:entry.col,
descending:entry.dir==='desc',
nullsFirst:!!entry.nullsFirst,
locale,
});
handles.push(handle);
}
if(!entries.length)return null;
return{entries,handles,locale};
}
#sortOffloadable(filtered){
const host=this.#pipelineHost();
if(!host)return false;
const threshold=Number(host.threshold);
if(Number.isFinite(threshold)&&filtered.length<threshold)return false;
return!!this.#portableSort();
}
#offloadSort(filtered,key){
const host=this.#pipelineHost();
const plan=host&&this.#portableSort();
if(!plan){this.#stage('sort',()=>key,()=>this.#runSort(filtered));return;}
const controller=new AbortController();
this.#inflight.set('sort',{key,controller});
const call=plan.entries.length===1
?host.sortColumn(plan.handles[0],filtered,{
descending:plan.entries[0].descending,
nullsFirst:plan.entries[0].nullsFirst,
locale:plan.locale,
signal:controller.signal,
key:'sort',
})
:host.sortMulti(
plan.handles,
plan.entries.map((e,i)=>({...e,handle:plan.handles[i]})),
filtered,
{signal:controller.signal,key:'sort'},
);
if(!isPromise(call)){
this.#inflight.delete('sort');
this.#stage('sort',()=>key,()=>(call));
return;
}
call.then(
(result)=>this.#applySortResult(key,(result)),
(err)=>this.#onOffloadError('sort',key,err),
);
}
#applySortResult(key,result){
const pending=this.#inflight.get('sort');
if(!pending||pending.key!==key)return;
if(this.#sortKey()!==key){this.#inflight.delete('sort');return;}
this.#inflight.delete('sort');
this.#stage('sort',()=>key,()=>result);
this.#view=null;
this.#run();
this.#emit('model:changed',{reason:PIPELINE_SETTLE_REASONS[0]});
}
#onOffloadError(stage,key,err){
const pending=this.#inflight.get(stage);
if(!pending||pending.key!==key)return;
this.#inflight.delete(stage);
if(err&&(err).name==='AbortError')return;
if(stage==='sort'&&this.#sortKey()===key){
const filtered=this.#stage('filter',()=>this.#filterKey(),()=>this.#runFilter());
this.#stage('sort',()=>key,()=>this.#runSort(filtered));
this.#view=null;
this.#run();
warnOnce('source.pipeline.sort','off-thread sort failed; recomputed on the main thread.',err);
this.#emit('model:changed',{reason:PIPELINE_SETTLE_REASONS[1]});
}
}
#publishSnapshot(grouped,totalled){
const leaves=grouped&&grouped.leaves?grouped.leaves.length:this.#snapshot.slots.length;
this.#snapshot.matchCount=leaves;
this.#snapshot.grand=(totalled&&totalled.grand)||null;
this.#snapshot.grandLeaves=grouped&&grouped.leaves?grouped.leaves.length:0;
this.#snapshot.groupLeaves=grouped&&grouped.leaves?grouped.leaves:null;
}
#stage(name,keyFn,computeFn){
const entry=this.#stages.get(name);
const key=keyFn();
if(entry.computed&&entry.key===key)return entry.result;
entry.result=computeFn();
entry.key=key;
entry.computed=true;
entry.id++;
if(name==='flatten')this.#view=null;
return entry.result;
}
#idOf(name){return this.#stages.get(name).id;}
#filterKey(){
const host=(this.#ctx.config||{}).hostFilter;
const hostActive=host&&isFunction(host.active)&&host.active()?1:0;
const where=(this.#state('where',null));
const whereVersion=where?where.version:0;
const sig=this.#memoString('filter.sig',`${this.#stateVersion()}`,()=>querySignature({
filters:this.#state('filters',null),
quick:this.#state('quick',''),
quickMode:this.#state('quickMode','contains'),
}));
return`${this.#queryVersion}|${sig}|${hostActive}|${whereVersion}`;
}
#sortKey(){return`${this.#idOf('filter')}|${JSON.stringify(this.#state('sort',[]))}`;}
#groupKey(){return`${this.#idOf('sort')}|${JSON.stringify(this.#state('groupBy',[]))}`;}
#totalKey(){
const scope=this.#totalFilteredOnly()?'f':'a';
const narrow=(this.#ctx.config||{})
.totalOnlyChangedColumns?'n':'w';
return`${this.#idOf('group')}|${JSON.stringify(this.#state('totals',[]))}`
+`|${scope}${narrow}|${this.#dataVersion}`;
}
#pivotKey(){return`${this.#idOf('group')}|${JSON.stringify(this.#state('pivotBy',[]))}`;}
#flattenKey(){
const shapedByTotals=this.#groupsActive()||!this.#grandTotalPinned();
const totalPart=shapedByTotals?this.#idOf('total'):'x';
const version=`${this.#stateVersion()}`;
const keys=this.#memoString('flatten.keys',version,()=>{
const expanded=this.#state('expanded',null);
return expanded?[...expanded].sort().join('\u0001'):'*';
});
const detailPart=this.#memoString('flatten.detail',version,()=>{
const detail=this.#state('detail',null);
return detail&&isFunction(detail.signature)?detail.signature():'';
});
return`${totalPart}|${this.#idOf('pivot')}|${keys}|${detailPart}`;
}
#groupsActive(){
const by=this.#state('groupBy',[]);
return Array.isArray(by)&&by.length>0;
}
stageState(){
const out=[];
for(const[name,entry]of this.#stages){
const result=entry.result;
out.push({
stage:name,
materialised:!!entry.computed,
bytes:result&&typeof result.byteLength==='number'?result.byteLength:null,
rows:result&&typeof result.length==='number'?result.length:null,
rebuilds:entry.id,
});
}
return out;
}
facetIndices(colId){
return this.#runFilter(colId||null);
}
facetRows(colId){
const indices=this.facetIndices(colId);
const out=[];
for(let i=0;i<indices.length;i++){
const row=this.#leafRow(indices[i]);
if(row)out.push(row);
}
return out;
}
invalidateRows(){
this.#rowCache.clear();
this.#quickText.clear();
this.#view=null;
}
invalidateHandles(){
this.#handles.invalidate();
}
rows(){return this.#rows;}
unfilteredIndices(){
return this.#baseIndices();
}
filteredIndices(){
const entry=this.#stages.get('filter');
if(entry&&entry.computed&&entry.key===this.#filterKey()){
return entry.result||this.#baseIndices();
}
this.#run();
const after=this.#stages.get('filter');
const indices=after&&after.computed?after.result:null;
return indices||this.#baseIndices();
}
unfilteredRows(){
const indices=this.#baseIndices();
const out=[];
for(let i=0;i<indices.length;i++){
const row=this.#leafRow(indices[i]);
if(row)out.push(row);
}
return out;
}
#runFilter(exclude=null){
const base=this.#baseIndices();
const all=this.#state('filters',null);
const pruned=exclude?this.#kernel('pruneColumn')(all,exclude):all;
const filters=this.#resolveRelative(pruned);
const quick=String(this.#state('quick','')||'').trim().toLowerCase();
const matches=quick?quickMatcher(quick,String(this.#state('quickMode','contains')||'contains')):null;
const host=(this.#ctx.config||{}).hostFilter;
const hostActive=!!(host&&isFunction(host.active)&&host.active());
const where=(
this.#state('where',null));
const whereActive=!!(where&&where.active);
if(!filters&&!quick&&!hostActive&&!whereActive)return base;
let mask=null;
if(filters){
const evaluate=this.#kernel('evaluateFilters');
mask=evaluate(filters,{
handle:(colId)=>this.#handles.handle(colId),
count:this.#physicalCount(),
pool:(this.#ctx).pool,
locale:(this.#ctx.config||{}).locale,
});
}
const out=new Uint32Array(base.length);
let n=0;
for(let i=0;i<base.length;i++){
const physical=base[i];
if(mask&&!mask[physical])continue;
if(matches&&!this.#quickPasses(physical,matches))continue;
if(hostActive||whereActive){
const row=this.#leafRow(physical);
if(hostActive&&!host.passes(row))continue;
if(whereActive&&!where.passes(row.data,row.key))continue;
}
out[n++]=physical;
}
if(mask){
const release=(this.#ctx.compute||{}).releaseMask;
if(isFunction(release)){
release({pool:(this.#ctx).pool},mask);
}
}
return out.subarray(0,n);
}
handleFor(colId){
return this.#handles.handle(colId);
}
#runSort(filtered){
const entries=this.#state('sort',[]).filter((entry)=>{
const column=this.#column(entry.col);
if(!column||!column.running)return true;
warnOnce(
`source.sort.running.${entry.col}`,
`sort ignores "${entry.col}": a running total is a function of the sort order, `
+'so it cannot also decide it. Sort by the column it runs over instead.',
);
return false;
});
if(!entries.length)return filtered;
const locale=(this.#ctx.config||{}).locale;
if(entries.length===1){
const entry=entries[0];
const handle=this.#handles.handle(entry.col);
if(!handle){
warnOnce(`source.sort.${entry.col}`,`sort ignores unknown column "${entry.col}".`);
return filtered;
}
const column=this.#column(entry.col);
return this.#kernel('sortColumn')(handle,filtered,{
descending:entry.dir==='desc',
nullsFirst:entry.nullsFirst,
locale,
compare:comparatorFor(column),
});
}
const resolved=[];
for(const entry of entries){
const handle=this.#handles.handle(entry.col);
if(!handle){warnOnce(`source.sort.${entry.col}`,`sort ignores unknown column "${entry.col}".`);continue;}
resolved.push({
handle,
descending:entry.dir==='desc',
nullsFirst:entry.nullsFirst,
locale,
compare:comparatorFor(this.#column(entry.col)),
});
}
if(!resolved.length)return filtered;
return this.#kernel('sortMulti')(resolved.map((r)=>r.handle),resolved,filtered);
}
#runGroup(sorted){
const groupBy=this.#state('groupBy',[]);
const tree=this.#treeConfig();
if(tree&&!groupBy.length){
const built=buildTree(sorted,tree);
this.#treeMeta=indexTree(built.roots,tree.declaresChildren);
return{roots:null,leaves:sorted,nodes:new Map(),tree:built};
}
this.#treeMeta=null;
if(tree&&groupBy.length){
warnOnce(
'tree.grouped',
'grouping is active on a grid configured with `tree`; the grouping wins '
+'and the hierarchy is not shown.',
);
}
if(!groupBy.length)return{roots:null,leaves:sorted,nodes:new Map()};
const configZone=(this.#ctx.config||{}).timeZone;
const handles=[];
const project=[];
let anyProject=false;
for(const colId of groupBy){
const handle=this.#handles.handle(colId);
if(!handle){warnOnce(`source.group.${colId}`,`grouping ignores unknown column "${colId}".`);continue;}
handles.push(handle);
const column=this.#column(colId);
const dataType=column&&column.dataType;
if(dataType&&isFunction(dataType.groupProjector)){
const zone=(column.typeOptions&&column.typeOptions.timeZone)||column.timeZone||configZone||undefined;
const proj=dataType.groupProjector(column,zone);
project.push(proj||null);
if(proj)anyProject=true;
}else{
project.push(null);
}
}
if(!handles.length)return{roots:null,leaves:sorted,nodes:new Map()};
const{keys,buckets}=this.#kernel('groupByColumns')(handles,sorted,{
locale:(this.#ctx.config||{}).locale,
project:anyProject?project:undefined,
});
const pivotLeaf=this.#state('pivotBy',[]).length>0;
const roots=[];
const nodes=new Map();
for(let b=0;b<buckets.length;b++){
const tuple=keys[b];
let parent=null;
const path=[];
for(let level=0;level<tuple.length;level++){
path.push(groupPathElement(tuple[level]));
const key=groupKey(path);
const terminal=level===tuple.length-1;
let node=nodes.get(key);
if(!node){
const row=this.#factory.group({
path:[...path],
column:groupBy[level],
value:tuple[level],
level,
parent,
expanded:pivotLeaf&&terminal?false:this.#isExpanded(key,{
key,column:groupBy[level],value:tuple[level],level,path:[...path],
}),
hasChildren:!(pivotLeaf&&terminal),
});
node={row,leaves:null};
nodes.set(key,node);
if(parent)parent.children.push(row);else roots.push(row);
}
node.row.leafCount+=buckets[b].length;
if(level===tuple.length-1)node.leaves=buckets[b];
parent=node.row;
}
}
const entries=this.#state('sort',[])||[];
const orderSiblings=(siblings,level)=>{
if(!siblings||siblings.length<2){
if(siblings)for(const row of siblings)orderSiblings(row.children,level+1);
return;
}
const colId=groupBy[level];
const entry=entries.find((e)=>e.col===colId);
const direction=entry&&entry.dir==='desc'?-1:1;
siblings.sort((a,b)=>direction*defaultCompare(a.groupValue,b.groupValue));
for(const row of siblings)orderSiblings(row.children,level+1);
};
orderSiblings(roots,0);
return{roots,leaves:sorted,nodes,keys,buckets};
}
#reduceTree(tree,columns){
const byPhysical=new Map();
const byKey=new Map();
const walk=(node)=>{
const leaves=node.physical>=0?[node.physical]:[];
for(const child of node.children)leaves.push(...walk(child));
if(node.children.length){
const totals=this.#totalsFor(columns,Uint32Array.from(leaves),null);
if(node.physical>=0)byPhysical.set(node.physical,totals);
else byKey.set(node.key,totals);
}
return leaves;
};
for(const root of tree.roots)walk(root);
return{byPhysical,byKey};
}
#runTotal(grouped){
const columns=this.#totalColumns();
if(!columns.length){this.#totalsBasis=null;this.#lastGrand=null;return{grand:null};}
const basis={id:this.#idOf('group'),columns:columns.map((c)=>c.id).join('\u0001')};
const reusable=this.#totalsBasis
&&this.#totalsBasis.id===basis.id
&&this.#totalsBasis.columns===basis.columns;
const dirty=this.#onlyChangedTotals()&&reusable?this.#totalsDirty:null;
const groupColumns=dirty?columns.filter((c)=>dirty.has(c.id)):columns;
const previousGrand=reusable?this.#lastGrand:null;
const unfiltered=this.#totalFilteredOnly()?null:this.#unfilteredGroups();
const groupRunning=this.#syncGroupRunning(columns);
const reduce=(row)=>{
const node=grouped.nodes.get(row.key);
let leaves=node&&node.leaves?node.leaves:null;
if(row.children&&row.children.length){
const parts=row.children.map(reduce);
let total=0;
for(const p of parts)total+=p.length;
const merged=new Uint32Array(total);
let at=0;
for(const p of parts){merged.set(p,at);at+=p.length;}
leaves=merged;
}
leaves=leaves||new Uint32Array(0);
const scope=unfiltered?(unfiltered.get(row.key)||EMPTY_INDICES):leaves;
const computed=this.#groupTotalsFor(
row,dirty?groupColumns:columns,scope,groupRunning,
);
row.totals=dirty?{...row.totals,...computed}:computed;
return leaves;
};
if(grouped.roots&&groupColumns.length)for(const root of grouped.roots)reduce(root);
this.#treeTotals=grouped.tree?this.#reduceTree(grouped.tree,columns):null;
this.#totalsBasis=basis;
this.#totalsDirty=this.#onlyChangedTotals()?new Set():null;
const grandConfig=(this.#ctx.config||{}).grandTotalRow;
if(!grandConfig){this.#totalDeltas.length=0;this.#lastGrand=null;return{grand:null};}
const grandScope=unfiltered?this.#unfilteredScope():grouped.leaves;
this.#lastGrand=this.#grandTotals(columns,grandScope,dirty,previousGrand);
return{grand:this.#lastGrand};
}
#grandTotals(columns,leaves,dirty,carried){
const totals={};
const id=this.#idOf('group');
if(id!==this.#runningId){this.#dropRunning();this.#runningId=id;}
const deltas=this.#totalDeltas;
this.#totalDeltas=[];
for(const column of columns){
if(!reducibleInTotals(column))continue;
const handle=this.#handles.handle(column.id);
if(!handle)continue;
const fn=totalFnFor(column,'grand');
if(!isIncremental(fn,handle)){
if(dirty&&!dirty.has(column.id)&&carried&&column.id in carried){
totals[column.id]=carried[column.id];
continue;
}
totals[column.id]=this.#totalsFor([column],leaves,null,'grand')[column.id];
continue;
}
let running=this.#running.get(column.id);
if(!running||running.fn!==fn){
running=new RunningTotal((fn));
this.#running.set(column.id,running);
}
if(running.valid){
let absorbed=true;
for(const delta of deltas){
if(delta.colId!==column.id)continue;
if(!running.update(delta.before,delta.after)){absorbed=false;break;}
}
if(absorbed){totals[column.id]=running.value();continue;}
running.invalidate();
}
running.seedColumn(handle.values,presenceReader(handle),leaves);
totals[column.id]=running.valid
?running.value()
:this.#totalsFor([column],leaves,null,'grand')[column.id];
}
return totals;
}
#syncGroupRunning(columns){
const id=this.#idOf('group');
if(id!==this.#groupRunningId){
this.#groupRunning.clear();
this.#groupRunningId=id;
}
const incremental=new Map();
for(const column of columns){
if(!reducibleInTotals(column))continue;
const handle=this.#handles.handle(column.id);
const fn=totalFnFor(column,'group');
if(isIncremental(fn,handle))incremental.set(column.id,{fn:(fn)});
}
if(!incremental.size){this.#groupDeltas.length=0;return incremental;}
const deltas=this.#groupDeltas;
this.#groupDeltas=[];
for(const record of deltas){
const{beforeKeys,afterKeys,colDeltas}=record;
const beforeSet=beforeKeys.length?new Set(beforeKeys):null;
const afterSet=afterKeys.length?new Set(afterKeys):null;
for(const{colId,before,after}of colDeltas){
if(!incremental.has(colId))continue;
for(const key of beforeKeys){
if(afterSet&&afterSet.has(key))continue;
this.#applyGroupDelta(key,colId,before,null);
}
for(const key of afterKeys){
if(beforeSet&&beforeSet.has(key))continue;
this.#applyGroupDelta(key,colId,null,after);
}
for(const key of beforeKeys){
if(afterSet&&!afterSet.has(key))continue;
this.#applyGroupDelta(key,colId,before,after);
}
}
}
return incremental;
}
#applyGroupDelta(key,colId,before,after){
const perColumn=this.#groupRunning.get(key);
const running=perColumn&&perColumn.get(colId);
if(!running||!running.valid)return;
if(!running.update(before,after))running.invalidate();
}
#groupTotalsFor(row,columns,scope,incremental){
if(!incremental||!incremental.size)return this.#totalsFor(columns,scope,row);
const totals={};
let rescan=null;
let perColumn=this.#groupRunning.get(row.key);
for(const column of columns){
const spec=incremental.get(column.id);
if(!spec){(rescan||(rescan=[])).push(column);continue;}
const handle=this.#handles.handle(column.id);
if(!handle)continue;
if(!perColumn){perColumn=new Map();this.#groupRunning.set(row.key,perColumn);}
let running=perColumn.get(column.id);
if(!running||running.fn!==spec.fn){
running=new RunningTotal(spec.fn);
perColumn.set(column.id,running);
}
if(!running.valid){
running.seedColumn(handle.values,presenceReader(handle),scope);
}
if(running.valid){totals[column.id]=running.value();continue;}
(rescan||(rescan=[])).push(column);
}
if(rescan)Object.assign(totals,this.#totalsFor(rescan,scope,row));
return totals;
}
#recordDeltas(columns,physical,previous,merged){
const counted=!this.#totalFilteredOnly()||this.#inFilteredSet(physical);
for(const column of columns){
const read=column.getValue;
if(!isFunction(read))continue;
const before=read(previous);
const after=read(merged);
if(Object.is(before,after))continue;
if(this.#totalsDirty)this.#totalsDirty.add(column.id);
if(counted&&this.#running.has(column.id)){
this.#totalDeltas.push({colId:column.id,before,after});
}
}
}
#groupByColumns(){
const groupBy=this.#state('groupBy',[]);
const out=[];
for(const colId of groupBy){
const column=this.#column(colId);
if(column)out.push(column);
}
return out;
}
#groupPathOf(groupCols,data){
const path=new Array(groupCols.length);
for(let i=0;i<groupCols.length;i++){
const read=groupCols[i].getValue;
path[i]=groupPathElement(isFunction(read)?read(data):undefined);
}
return path;
}
#recordGroupDeltas(groupCols,columns,physical,previous,merged){
if(this.#totalFilteredOnly()&&!this.#inFilteredSet(physical))return;
const beforeKeys=groupPrefixKeys(this.#groupPathOf(groupCols,previous));
const afterKeys=groupPrefixKeys(this.#groupPathOf(groupCols,merged));
const moved=beforeKeys.length!==afterKeys.length
||beforeKeys.some((k,i)=>k!==afterKeys[i]);
const colDeltas=[];
for(const column of columns){
const read=column.getValue;
if(!isFunction(read))continue;
const before=read(previous);
const after=read(merged);
if(!moved&&Object.is(before,after))continue;
colDeltas.push({colId:column.id,before,after});
}
if(!colDeltas.length&&!moved)return;
this.#groupDeltas.push({beforeKeys,afterKeys,colDeltas});
}
#inFilteredSet(physical){
const entry=this.#stages.get('filter');
const indices=(entry&&entry.computed?entry.result:null);
if(!indices)return true;
let lo=0;
let hi=indices.length-1;
while(lo<=hi){
const mid=(lo+hi)>>1;
const v=indices[mid];
if(v===physical)return true;
if(v<physical)lo=mid+1;else hi=mid-1;
}
return false;
}
#totalFilteredOnly(){
const value=(this.#ctx.config||{})
.totalFilteredOnly;
return value!==false;
}
#grandLeafCount(filtered){
return this.#totalFilteredOnly()?filtered:this.#unfilteredScope().length;
}
#unfilteredScope(){
if(!this.#unfilteredCache||this.#unfilteredCache.version!==this.#queryVersion){
this.#unfilteredCache={version:this.#queryVersion,indices:this.#baseIndices()};
}
return this.#unfilteredCache.indices;
}
#unfilteredGroups(){
const groupBy=this.#state('groupBy',[]);
const key=`${this.#queryVersion}|${JSON.stringify(groupBy)}`;
if(this.#unfilteredGroupCache&&this.#unfilteredGroupCache.key===key){
return this.#unfilteredGroupCache.map;
}
const map=new Map();
const handles=[];
for(const colId of groupBy){
const handle=this.#handles.handle(colId);
if(handle)handles.push(handle);
}
if(handles.length){
const scope=this.#unfilteredScope();
const{keys,buckets}=this.#kernel('groupByColumns')(handles,scope,{
locale:(this.#ctx.config||{}).locale,
});
const collected=new Map();
for(let b=0;b<buckets.length;b++){
const tuple=keys[b];
const path=[];
for(let level=0;level<tuple.length;level++){
path.push(groupPathElement(tuple[level]));
const at=groupKey(path);
let list=collected.get(at);
if(!list){list=[];collected.set(at,list);}
for(let i=0;i<buckets[b].length;i++)list.push(buckets[b][i]);
}
}
for(const[at,list]of collected)map.set(at,Uint32Array.from(list));
}
this.#unfilteredGroupCache={key,map};
return map;
}
#onlyChangedTotals(){
return!!((this.#ctx.config||{}))
.totalOnlyChangedColumns;
}
#dropTotalState(){
this.#dropRunning();
this.#dropGroupRunning();
this.#totalsDirty=null;
this.#totalsBasis=null;
this.#lastGrand=null;
}
#dropRunning(){
this.#running.clear();
this.#totalDeltas.length=0;
this.#runningId=-1;
}
#dropGroupRunning(){
this.#groupRunning.clear();
this.#groupDeltas.length=0;
this.#groupRunningId=-1;
}
#runPivot(grouped){
const pivotBy=this.#state('pivotBy',[]);
if(!pivotBy.length){this.#pivot=null;return null;}
const kernel=(this.#ctx.compute||{}).pivot;
if(!isFunction(kernel)){
warnOnce('source.pivot.kernel','pivot requested but ctx.compute.pivot is not available; showing unpivoted rows.');
this.#pivot=null;
return null;
}
const handles=pivotBy.map((colId)=>this.#handles.handle(colId)).filter(Boolean);
const totalColumns=this.#totalColumns();
this.#pivot=kernel(handles,grouped.leaves,{
groups:grouped.keys&&grouped.buckets
?{keys:grouped.keys,buckets:grouped.buckets}
:null,
totals:totalColumns.map((c)=>c.id),
handle:(colId)=>this.#handles.handle(colId),
totalOf:(colId)=>totalFnFor(totalColumns.find((c)=>c.id===colId),'group'),
separator:((this.#ctx.config||{}).pivot||{}).separator,
maxColumns:((this.#ctx.config||{}).pivot||{}).maxColumns,
})||null;
return this.#pivot;
}
#runFlatten(grouped){
const grand=(this.#stages.get('total').result);
const showFooter=!!((this.#ctx.config||{}).groupFooter);
const detail=this.#detailInline();
if(grouped.tree)return this.#flattenTree(grouped,grand,detail);
if(!grouped.roots&&!detail){
if(grand&&grand.grand&&!this.#grandTotalPinned()){
const row=this.#factory.grandTotal(grand.grand,this.#grandLeafCount(grouped.leaves.length));
if(this.#inlineLeaves===grouped.leaves&&Array.isArray(this.#slots)){
this.#slots[this.#slots.length-1]=row;
}else{
const slots=Array.from(grouped.leaves);
slots.push(row);
this.#slots=slots;
this.#inlineLeaves=grouped.leaves;
}
}else{
this.#inlineLeaves=null;
this.#slots=grouped.leaves;
}
this.#groupHeadings=EMPTY_HEADINGS;
return this.#slots;
}
const slots=[];
const headings=[];
const pushLeaf=(physical)=>{
slots.push(physical);
if(!detail)return;
const row=this.#leafRow(physical);
if(!row||!detail.isOpen(row.key))return;
row.master=true;
row.expanded=true;
slots.push(this.#factory.detail(row,detail.heightOf(row)));
};
if(!grouped.roots){
for(let i=0;i<grouped.leaves.length;i++)pushLeaf(grouped.leaves[i]);
if(grand&&grand.grand&&!this.#grandTotalPinned()){
slots.push(this.#factory.grandTotal(grand.grand,this.#grandLeafCount(grouped.leaves.length)));
}
this.#inlineLeaves=null;
this.#groupHeadings=EMPTY_HEADINGS;
this.#slots=slots;
return slots;
}
const walk=(row,at,of)=>{
row.expanded=row.hasChildren===false?false:this.#isExpanded(row.key,{
key:row.key,
column:row.groupColumn,
value:row.groupValue,
level:row.level,
path:row.groupPath,
});
row.posinset=at+1;
row.setsize=of;
headings.push({at:slots.length,row});
slots.push(row);
if(!row.expanded)return;
if(row.children&&row.children.length){
for(let i=0;i<row.children.length;i++)walk(row.children[i],i,row.children.length);
}else{
const node=grouped.nodes.get(row.key);
const leaves=node&&node.leaves?node.leaves:null;
if(leaves)for(let i=0;i<leaves.length;i++)pushLeaf(leaves[i]);
}
if(showFooter&&row.totals)slots.push(this.#factory.footer(row));
};
for(let i=0;i<grouped.roots.length;i++)walk(grouped.roots[i],i,grouped.roots.length);
if(grand&&grand.grand&&!this.#grandTotalPinned()){
slots.push(this.#factory.grandTotal(grand.grand,this.#grandLeafCount(grouped.leaves.length)));
}
this.#groupHeadings=headings;
this.#slots=slots;
return slots;
}
#baseIndices(){
const store=(this.#ctx.store);
if(store&&isFunction(store.liveIndices))return store.liveIndices();
const out=new Uint32Array(this.#rows.length);
let n=0;
for(let i=0;i<this.#rows.length;i++)if(this.#rows[i]!==undefined)out[n++]=i;
return out.subarray(0,n);
}
#physicalCount(){
const store=(this.#ctx.store);
return store&&typeof store.physical==='number'?store.physical:this.#rows.length;
}
#dataAt(physical){
const store=(this.#ctx.store);
if(store&&isFunction(store.source))return store.source(physical);
return this.#rows[physical];
}
#materialise(index,displayIndex=index){
const slot=this.#slots[index];
if(typeof slot!=='number'){slot.index=displayIndex;return slot;}
const row=this.#leafRow(slot);
row.index=displayIndex;
return row;
}
#leafRow(physical){
let row=this.#rowCache.get(physical);
if(!row){
row=this.#factory.leaf(this.#dataAt(physical),physical);
this.#rowCache.set(physical,row);
}
const meta=this.#treeMeta?this.#treeMeta.get(physical):null;
if(meta){
row.level=meta.level;
row.posinset=meta.posinset;
row.setsize=meta.setsize;
row.hasChildren=meta.hasChildren;
row.expanded=meta.hasChildren&&!meta.lazy&&this.#isExpanded(row.key);
const rollup=this.#treeTotals?this.#treeTotals.byPhysical.get(physical):undefined;
if(rollup)row.totals=rollup;
else if(row.totals)row.totals=null;
}else if(row.hasChildren){
row.level=0;
row.hasChildren=false;
row.expanded=false;
row.posinset=undefined;
row.setsize=undefined;
}
return row;
}
#flattenTree(grouped,grand,detail){
const slots=[];
const leavesOf=(node)=>{
if(node.leafCount!==undefined)return node.leafCount;
let n=node.physical>=0?1:0;
for(const child of node.children)n+=leavesOf(child);
node.leafCount=n;
return n;
};
const walk=(node)=>{
const open=this.#isExpanded(node.key);
if(node.physical>=0){
slots.push(node.physical);
if(detail&&detail.isOpen(this.#leafRow(node.physical).key)){
const row=this.#leafRow(node.physical);
slots.push(this.#factory.detail(row,detail.heightOf(row)));
}
}else{
slots.push(this.#factory.group({
path:[node.key],
column:null,
value:node.value,
level:node.level,
parent:null,
expanded:open,
hasChildren:node.children.length>0,
leafCount:leavesOf(node),
totals:this.#treeTotals?this.#treeTotals.byKey.get(node.key):undefined,
}));
}
if(!open)return;
for(const child of node.children)walk(child);
};
for(const root of grouped.tree.roots)walk(root);
if(grand&&grand.grand&&!this.#grandTotalPinned()){
slots.push(this.#factory.grandTotal(grand.grand,this.#grandLeafCount(grouped.leaves.length)));
}
this.#inlineLeaves=null;
this.#slots=slots;
return slots;
}
#buildView(){
return lazyRowView(()=>this.count(),(index)=>this.at(index));
}
#quickPasses(physical,matches){
let text=this.#quickText.get(physical);
if(text===undefined){
const data=this.#dataAt(physical);
const columns=this.#columnList();
const parts=[];
const hidden=this.#ctx.columnHidden;
for(const column of columns){
if(column.filter&&column.filter.enabled===false)continue;
if(isFunction(hidden)&&hidden(column.id))continue;
const custom=column.value&&isFunction(column.value.quickFilterText)
?column.value.quickFilterText({value:undefined,data,row:null,column,colId:column.id,grid:null,context:this.#state('context',null)})
:null;
const value=custom!==null&&custom!==undefined
?custom
:(isFunction(column.formatValue)?column.formatValue(isFunction(column.getValue)?column.getValue(data):undefined):'');
if(value!==null&&value!==undefined&&value!=='')parts.push(String(value));
}
text=parts.join(' ').toLowerCase();
this.#quickText.set(physical,text);
}
return matches(text);
}
#totalColumns(){
const named=this.#state('totals',[]);
const wanted=named&&named.length?new Set(named):null;
return this.#columnList().filter((c)=>(
wanted?wanted.has(c.id):!!(c.total||c.groupTotal||c.grandTotal)
));
}
#totalsFor(columns,leaves,row,scope='group'){
const totals={};
const reduce=(this.#ctx.compute||{}).total;
for(const column of columns){
if(!reducibleInTotals(column))continue;
const handle=this.#handles.handle(column.id);
if(!handle)continue;
const fn=totalFnFor(column,scope);
if(!fn)continue;
if(isFunction(fn)){
const values=new Array(leaves.length);
for(let i=0;i<leaves.length;i++)values[i]=handle.get(leaves[i]);
totals[column.id]=fn(values,{
row,
column,
grid:this.#ctx.grid||null,
context:this.#state('context',null),
valueAt:(colId,i)=>{
const other=this.#handles.handle(colId);
return other&&i>=0&&i<leaves.length?other.get(leaves[i]):null;
},
});
}else if(isFunction(reduce)){
totals[column.id]=reduce(handle,leaves,fn);
}
}
return totals;
}
#resolveRelative(filters){
if(!filters)return filters;
const config=(this.#ctx.config||{});
const opts={tz:config.timeZone,weekStartsOn:config.weekStartsOn};
return mapConditions(filters,(c)=>{
const resolved=resolveRelativeCondition(c,opts);
if(resolved&&resolved.type)return resolved;
const column=this.#column(resolved&&resolved.col);
const type=column&&(column.dataType?.base||column.type);
return type?{...resolved,type}:resolved;
});
}
#isExpanded(key,info){
const expanded=this.#state('expanded',null);
if(!expanded)return true;
return isFunction(expanded.has)
?expanded.has(key,info)
:Array.prototype.includes.call(expanded,key);
}
#state(name,fallback){
if(name==='context'||!isFunction(this.#ctx.stateVersion)){
return this.#readState(name,fallback);
}
const version=this.#stateVersionFor(name);
const cached=this.#stateCache.get(name);
if(cached&&cached.version===version)return cached.value;
const value=this.#readState(name,fallback);
this.#stateCache.set(name,{version,value});
return value;
}
#readState(name,fallback){
const state=(this.#ctx.state);
if(state&&isFunction(state[name])){
const value=state[name]();
return value===undefined||value===null?fallback:value;
}
return fallback;
}
#stateVersion(){
const fn=(this.#ctx.stateVersion);
return isFunction(fn)?fn():0;
}
#totalsVersion(){
const columns=(this.#ctx.columnsVersion);
const permissions=(this.#ctx.permissionsVersion);
return`${isFunction(columns)?columns():0}|${isFunction(permissions)?permissions():0}`;
}
#stateVersionFor(name){
return name==='totals'?this.#totalsVersion():`${this.#stateVersion()}`;
}
#memoString(name,version,build){
if(!isFunction(this.#ctx.stateVersion))return build();
const cached=this.#keyMemo.get(name);
if(cached&&cached.version===version)return cached.key;
const key=build();
this.#keyMemo.set(name,{version,key});
return key;
}
#kernel(name){
const fn=(this.#ctx.compute||{})[name];
if(!isFunction(fn))fail(`ctx.compute.${name} is required by the memory source pipeline`);
return fn;
}
#columnList(){
const columns=(this.#ctx.columns);
return columns&&isFunction(columns.all)?(columns.all()):[];
}
#column(colId){
const columns=(this.#ctx.columns);
return columns&&isFunction(columns.get)?(columns.get(colId)):undefined;
}
#emit(type,payload){
const emit=(this.#ctx.emit);
if(isFunction(emit))emit(type,payload);
}
}
});
__def("packages/modules/kpi/clock.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"CLOCK_IGNORED_KEYS",{enumerable:true,get:function(){return CLOCK_IGNORED_KEYS;}});
Object.defineProperty(__exports,"resolveTimeZone",{enumerable:true,get:function(){return resolveTimeZone;}});
Object.defineProperty(__exports,"buildClockFormatters",{enumerable:true,get:function(){return buildClockFormatters;}});
Object.defineProperty(__exports,"resolveClockTile",{enumerable:true,get:function(){return resolveClockTile;}});
Object.defineProperty(__exports,"readClock",{enumerable:true,get:function(){return readClock;}});
const __m0=__req("packages/core/src/internal/util.js");
const warnOnce=__m0["warnOnce"];
const CLOCK_IGNORED_KEYS=Object.freeze([
'aggregation','field','format','thresholds','bands','target','baseline','sparkline',
]);
function warnClockIgnored(spec,id,label){
for(const key of CLOCK_IGNORED_KEYS){
if(spec[key]===undefined)continue;
warnOnce(`kpi:clock:ignored:${id}:${key}`,
`the "${label}" clock tile does not take \`${key}\`; a clock tile shows the device clock and \`${key}\` is ignored.`);
}
}
function resolveTimeZone(spec,id,label,locale){
if(spec.timeZone===undefined)return undefined;
try{
new Intl.DateTimeFormat(locale,{timeZone:spec.timeZone});
return spec.timeZone;
}catch{
warnOnce(`kpi:clock:tz:${id}`,
`the "${label}" clock tile's timeZone \`${String(spec.timeZone)}\` is not a recognised IANA zone; falling back to the local time zone.`);
return undefined;
}
}
function buildClockFormatters({
locale,timeZone,seconds,date,
}){
const dateFormatter=date
?new Intl.DateTimeFormat(locale,{
weekday:'short',day:'2-digit',month:'short',year:'numeric',timeZone,
})
:null;
const timeFormatter=new Intl.DateTimeFormat(locale,{
hour:'2-digit',
minute:'2-digit',
second:seconds?'2-digit':undefined,
timeZone,
});
return{dateFormatter,timeFormatter};
}
function resolveClockTile(spec,id,cfg){
const label=String(spec.label??spec.id??id);
warnClockIgnored(spec,id,label);
const locale=(typeof spec.locale==='string'&&spec.locale)
||(cfg&&typeof cfg.locale==='string'&&cfg.locale)
||undefined;
const timeZone=resolveTimeZone(spec,id,label,locale);
const seconds=spec.seconds!==false;
const date=spec.date!==false;
const{dateFormatter,timeFormatter}=buildClockFormatters({
locale,timeZone,seconds,date,
});
return{
id,
label,
spec,
kind:'clock',
aggregation:'clock',
isCustom:false,
customFn:null,
field:undefined,
accessor:()=>undefined,
predicate:null,
format:undefined,
target:undefined,
barBounds:null,
baseline:undefined,
spark:null,
acc:null,
members:0,
noColumn:false,
locale,
timeZone,
seconds,
date,
dateFormatter,
timeFormatter,
};
}
function readClock(tile,now=new Date()){
const dateText=tile.dateFormatter?tile.dateFormatter.format(now):null;
const timeText=tile.timeFormatter.format(now);
return{
value:now.getTime(),
formatted:dateText?`${dateText} ${timeText}`:timeText,
clock:{date:dateText,time:timeText},
};
}
});
__def("packages/modules/kpi/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"createKPI",{enumerable:true,get:function(){return createKPI;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__req("packages/core/src/internal/util.js");
const isObject=__m0["isObject"];
const isFunction=__m0["isFunction"];
const isNil=__m0["isNil"];
const warnOnce=__m0["warnOnce"];
const pathGetter=__m0["pathGetter"];
const defaultCompare=__m0["defaultCompare"];
const __m1=__req("packages/modules/kpi/aggregate.js");
const makeAccumulator=__m1["makeAccumulator"];
const accessorFor=__m1["accessorFor"];
const statusFor=__m1["statusFor"];
const formatValue=__m1["formatValue"];
const AGGREGATIONS=__m1["AGGREGATIONS"];
const barBounds=__m1["barBounds"];
const barPercent=__m1["barPercent"];
const __m2=__req("packages/modules/kpi/view.js");
const Emitter=__m2["Emitter"];
const renderKPI=__m2["renderKPI"];
const __m3=__req("packages/modules/shared/chrome.js");
const adoptGridChrome=__m3["adoptGridChrome"];
const __m4=__req("packages/core/src/source/memory.js");
const isPipelineSettle=__m4["isPipelineSettle"];
const __m5=__req("packages/modules/kpi/tree.js");
const resolveHierarchy=__m5["resolveHierarchy"];
const decorateNodes=__m5["decorateNodes"];
const branchKeys=__m5["branchKeys"];
const visibleNodes=__m5["visibleNodes"];
const __m6=__req("packages/modules/kpi/messages.js");
const resolveMessages=__m6["resolveMessages"];
const __m7=__req("packages/modules/kpi/clock.js");
const resolveClockTile=__m7["resolveClockTile"];
const readClock=__m7["readClock"];
const KPI_EVENTS=Object.freeze([
'tile:click','tile:dblclick','tile:contextmenu','node:toggle','change',
]);
const GRID_FOLLOW_EVENTS=Object.freeze([
'rows:changed','filter:changed','sort:changed','cell:changed','model:changed',
'stream:chunk','stream:evicted',
]);
const CALLBACKS=Object.freeze({
onTileClick:'tile:click',
onTileDblClick:'tile:dblclick',
onTileContextMenu:'tile:contextmenu',
onNodeToggle:'node:toggle',
onChange:'change',
});
function keyFn(rowKey){
if(isFunction(rowKey))return rowKey;
const get=pathGetter(typeof rowKey==='string'&&rowKey?rowKey:'id');
return(row)=>get(row);
}
function resolveTile(raw,i,cfg){
const spec=isObject(raw)?raw:{};
const id=String(spec.id??spec.label??`tile${i}`);
if(spec.kind==='clock')return resolveClockTile(spec,id,cfg);
const agg=spec.aggregation??spec.agg??'count';
const isCustom=isFunction(agg)||agg==='custom';
const aggregation=isCustom?'custom':agg;
if(!isCustom&&!AGGREGATIONS.includes(aggregation)){
warnOnce(`kpi:agg:${aggregation}`,
`"${aggregation}" is not a KPI aggregation; expected one of ${AGGREGATIONS.join(', ')}. Falling back to count.`);
}
const acc=isCustom?null:makeAccumulator(AGGREGATIONS.includes(aggregation)?aggregation:'count');
return{
id,
label:String(spec.label??spec.id??id),
spec,
kind:'stat',
aggregation,
isCustom,
customFn:isFunction(agg)?agg:(isFunction(spec.compute)?spec.compute:null),
field:spec.field,
accessor:accessorFor(spec.field),
predicate:isFunction(spec.filter)?spec.filter:(isFunction(spec.predicate)?spec.predicate:null),
format:spec.format,
target:spec.target,
barBounds:barBounds(spec),
baseline:spec.baseline,
spark:normaliseSpark(spec.sparkline),
acc,
members:0,
noColumn:false,
};
}
function normaliseSpark(s){
if(!s)return null;
if(typeof s==='string')return{x:undefined,y:accessorFor(s)};
if(isObject(s)&&(s.y||s.field))return{x:typeof s.x==='string'?s.x:undefined,y:accessorFor(s.y??s.field)};
return null;
}
function gridFields(tiles){
const set=new Set();
for(const t of tiles){
if(t.kind==='clock')continue;
if(typeof t.field==='string')set.add(t.field);
if(t.spark&&typeof t.spark.x==='string')set.add(t.spark.x);
if(typeof t.spec.sparkline==='string')set.add(t.spec.sparkline);
else if(isObject(t.spec.sparkline)&&typeof(t.spec.sparkline.y??t.spec.sparkline.field)==='string')set.add(t.spec.sparkline.y??t.spec.sparkline.field);
}
return[...set];
}
const KEY_FIELD='__kpiKey';
function extraFields(cfg){
const declared=cfg.fields;
if(typeof declared==='string'&&declared)return[declared];
if(Array.isArray(declared))return declared.filter((n)=>typeof n==='string'&&n);
if(declared!=null){
warnOnce('kpi:fields:type',
'config.fields must be an array of column names; ignoring it.');
}
return[];
}
function columnNames(grid){
const out=new Map();
let all=null;
if(grid&&grid.columns&&isFunction(grid.columns.all)){
try{all=grid.columns.all();}catch{all=null;}
}
for(const c of all||[]){
if(!c)continue;
if(typeof c.id==='string'&&c.id)out.set(c.id,c.id);
}
for(const c of all||[]){
if(!c)continue;
if(typeof c.field==='string'&&c.field&&!out.has(c.field)){
out.set(c.field,typeof c.id==='string'&&c.id?c.id:c.field);
}
}
return out;
}
let readingTile=null;
function matches(t,row){
readingTile=t;
try{return!!t.predicate(row);}finally{readingTile=null;}
}
function reduceWith(t,rows){
readingTile=t;
try{return t.customFn(rows,t);}finally{readingTile=null;}
}
function resolveUnprojected(grid,cache,rowKey,name,colId){
const memo=`${name}\u0000${String(rowKey)}`;
let value;
if(cache.has(memo)){
value=cache.get(memo);
}else{
try{value=grid.rows.value(rowKey,colId);}catch{value=undefined;}
cache.set(memo,value);
}
const t=readingTile;
warnOnce(`kpi:unprojected:${t?t.id:''}:${name}`,
t
?`the "${t.label}" tile's filter read \`${name}\`, which is a column on the bound grid but is not projected — add it to \`fields\`: createKPI({ grid, fields: ['${name}'], … }).`
:`\`${name}\` is a column on the bound grid but is not projected onto this KPI panel's rows — add it to \`fields\`: createKPI({ grid, fields: ['${name}'], … }).`);
return value;
}
function unprojectedProto(grid,names,projected){
const descriptors={};
const cache=new Map();
let count=0;
for(const[name,colId]of names){
if(name===KEY_FIELD||projected.has(name))continue;
count+=1;
descriptors[name]={
configurable:true,
enumerable:false,
get(){return resolveUnprojected(grid,cache,this[KEY_FIELD],name,colId);},
set(v){
Object.defineProperty(this,name,{
value:v,writable:true,enumerable:true,configurable:true,
});
},
};
}
return count?Object.defineProperties({},descriptors):null;
}
function readRows(cfg,tiles){
if(Array.isArray(cfg.rows))return cfg.rows;
const grid=cfg.grid;
if(!grid||!grid.rows||!isFunction(grid.rows.forEach))return[];
const names=columnNames(grid);
const projected=new Map();
for(const f of gridFields(tiles))projected.set(f,f);
const extra=extraFields(cfg);
for(const f of extra)if(!projected.has(f))projected.set(f,names.get(f)||f);
for(const t of tiles){
const absent=names.size>0&&typeof t.field==='string'&&t.field&&!names.has(t.field);
t.noColumn=!!absent;
if(absent){
warnOnce(`kpi:no-column:${t.id}:${t.field}`,
`the "${t.label}" tile's field \`${t.field}\` names no column on the bound grid, so the tile has nothing to measure — check it against the grid's column ids.`);
}
}
if(names.size){
for(const f of extra){
if(!names.has(f)){
warnOnce(`kpi:no-column::${f}`,
`config.fields names \`${f}\`, which is not a column on the bound grid — check it against the grid's column ids.`);
}
}
}
const proto=names.size?unprojectedProto(grid,names,projected):null;
const rows=[];
const pushLeaf=(r)=>{
const key=r.key;
const obj=proto?Object.create(proto):{};
obj[KEY_FIELD]=key;
for(const[name,colId]of projected){
try{obj[name]=grid.rows.value(key,colId);}catch{obj[name]=undefined;}
}
rows.push(obj);
};
let inGroupedGrid=false;
grid.rows.forEach((r)=>{
if(!r)return;
if(r.group){
if(!r.footer&&!r.grandTotal&&r.level===0&&isFunction(grid.rows.leavesOf)){
inGroupedGrid=true;
for(const leaf of grid.rows.leavesOf(r.key))pushLeaf(leaf);
}
return;
}
if(inGroupedGrid)return;
pushLeaf(r);
});
return rows;
}
function createKPI(el,config={}){
const cfg=isObject(config)?config:{};
const gridBound=!!cfg.grid&&!Array.isArray(cfg.rows);
const key=gridBound?keyFn('__kpiKey'):keyFn(cfg.rowKey);
const nullText=typeof cfg.nullText==='string'?cfg.nullText:'—';
const tiles=(Array.isArray(cfg.tiles)?cfg.tiles:[]).map((raw,i)=>resolveTile(raw,i,cfg));
const hierarchy=resolveHierarchy(cfg.tree,tiles);
const treeCfg=isObject(cfg.tree)?cfg.tree:{};
const expandedKeys=new Set();
if(hierarchy){
if(treeCfg.expanded===true)for(const k of branchKeys(hierarchy.roots))expandedKeys.add(k);
else if(Array.isArray(treeCfg.expanded))for(const k of treeCfg.expanded)expandedKeys.add(String(k));
}
const emitter=new Emitter();
const doc=(el&&el.ownerDocument)
||(typeof globalThis!=='undefined'&&globalThis.document)
||null;
const rowStore=new Map();
let routed=false;
let chrome=null;
let followPending=false;
const following=[];
const kpi={
el:el||null,
doc,
config:cfg,
emitter,
wired:false,
tree:!!hierarchy,
t:resolveMessages(cfg),
rowKey:cfg.rowKey??(gridBound?'__kpiKey':'id'),
model:{tiles:[]},
fire(name,payload){
emitter.emit(name,payload);
for(const[cbName,evName]of Object.entries(CALLBACKS)){
if(evName===name&&isFunction(cfg[cbName]))cfg[cbName](payload);
}
},
};
const contribute=(oldRow,newRow)=>{
for(const t of tiles){
if(t.kind==='clock')continue;
const oldIn=oldRow!=null&&(!t.predicate||matches(t,oldRow));
const newIn=newRow!=null&&(!t.predicate||matches(t,newRow));
if(oldIn)t.members-=1;
if(newIn)t.members+=1;
if(t.isCustom)continue;
if(oldIn)t.acc.remove(t.accessor(oldRow));
if(newIn)t.acc.add(t.accessor(newRow));
}
};
const resetTiles=()=>{
for(let i=0;i<tiles.length;i++){
if(tiles[i].kind==='clock')continue;
tiles[i].members=0;
if(!tiles[i].isCustom)tiles[i].acc=makeAccumulator(AGGREGATIONS.includes(tiles[i].aggregation)?tiles[i].aggregation:'count');
}
};
const seed=()=>{
rowStore.clear();
resetTiles();
for(const row of readRows(cfg,tiles)){
const k=key(row);
rowStore.set(k,row);
contribute(null,row);
}
};
const rebuildFromStore=()=>{
resetTiles();
for(const row of rowStore.values())contribute(null,row);
};
const sparkFor=(t)=>{
if(!t.spark)return null;
let rows=[...rowStore.values()];
if(t.predicate)rows=rows.filter((r)=>matches(t,r));
if(t.spark.x){
const gx=pathGetter(t.spark.x);
rows=rows.slice().sort((a,b)=>defaultCompare(gx(a),gx(b)));
}
const ys=rows.map((r)=>Number(t.spark.y(r))).filter((n)=>Number.isFinite(n));
return ys.length?ys:null;
};
const buildModel=()=>{
const storeVals=[...rowStore.values()];
const hasData=rowStore.size>0;
const out=tiles.map((t)=>{
if(t.kind==='clock'){
const reading=readClock(t);
return{
id:t.id,
label:t.label,
kind:'clock',
aggregation:t.aggregation,
field:undefined,
value:reading.value,
formatted:reading.formatted,
clock:reading.clock,
status:null,
target:undefined,
baseline:undefined,
delta:null,
deltaPercent:null,
deltaFormatted:undefined,
count:0,
sparkline:null,
bar:null,
};
}
const measured=!t.noColumn;
let value;
if(t.isCustom){
const rows=t.predicate?storeVals.filter((r)=>matches(t,r)):storeVals;
value=t.customFn?reduceWith(t,rows):null;
}else{
value=t.acc.value();
}
if(!hasData||!measured)value=null;
const status=statusFor(value,t.spec,hasData&&measured);
let delta=null;
let deltaPercent=null;
let deltaFormatted;
if(!isNil(t.baseline)&&typeof value==='number'){
delta=value-t.baseline;
deltaPercent=t.baseline!==0?delta/t.baseline:null;
deltaFormatted=formatValue(delta,t.format,nullText);
}
return{
id:t.id,
label:t.label,
kind:'stat',
aggregation:t.aggregation,
field:typeof t.field==='string'?t.field:undefined,
value:value===undefined?null:value,
formatted:formatValue(typeof value==='number'?value:(value??null),t.format,nullText),
status,
target:t.target,
baseline:t.baseline,
delta,
deltaPercent,
deltaFormatted,
count:t.members,
sparkline:sparkFor(t),
bar:t.barBounds
?{lo:t.barBounds.lo,hi:t.barBounds.hi,percent:barPercent(value,t.barBounds)}
:null,
};
});
const model={tiles:out};
if(hierarchy){
model.nodes=decorateNodes(hierarchy,tiles,new Map(out.map((t)=>[t.id,t])),expandedKeys);
}
return model;
};
const rebuildNodes=()=>{
if(!hierarchy)return;
kpi.model.nodes=decorateNodes(
hierarchy,tiles,new Map(kpi.model.tiles.map((t)=>[t.id,t])),expandedKeys,
);
};
const refreshModel=()=>{
kpi.model=buildModel();
if(kpi.el)renderKPI(kpi);
kpi.fire('change',{model:kpi.model});
return kpi;
};
const hasClockTiles=tiles.some((t)=>t.kind==='clock');
const clockDocHidden=()=>!!(kpi.doc&&kpi.doc.hidden);
let clockTimer=null;
const tickClocks=()=>{
const now=new Date();
let any=false;
for(const t of tiles){
if(t.kind!=='clock')continue;
const model=kpi.model.tiles.find((m)=>m.id===t.id);
if(!model)continue;
const reading=readClock(t,now);
model.value=reading.value;
model.formatted=reading.formatted;
model.clock=reading.clock;
any=true;
}
if(!any)return;
if(hierarchy)rebuildNodes();
if(kpi.el)renderKPI(kpi);
};
const scheduleClockTick=()=>{
if(clockDocHidden())return;
const delay=1000-(Date.now()%1000);
clockTimer=setTimeout(()=>{
clockTimer=null;
tickClocks();
scheduleClockTick();
},delay);
if(isFunction(clockTimer?.unref))clockTimer.unref();
};
const stopClockTicking=()=>{
if(clockTimer!==null){clearTimeout(clockTimer);clockTimer=null;}
};
let onClockVisibility=null;
if(hasClockTiles&&kpi.doc&&isFunction(kpi.doc.addEventListener)){
onClockVisibility=()=>{
if(clockDocHidden()){stopClockTicking();return;}
tickClocks();
if(clockTimer===null)scheduleClockTick();
};
kpi.doc.addEventListener('visibilitychange',onClockVisibility);
}
kpi.rows={
apply(change={}){
if(gridBound){
warnOnce('kpi:apply-gridbound',
'rows.apply on a grid-bound KPI panel is ignored: the panel follows the bound grid, so route the change to the grid instead and the panel will follow it.');
return;
}
routed=true;
for(const row of change.add||[]){
const k=key(row);
const prev=rowStore.get(k);
rowStore.set(k,row);
contribute(prev??null,row);
}
for(const row of change.update||[]){
const k=key(row);
const prev=rowStore.get(k);
rowStore.set(k,row);
contribute(prev??null,row);
}
for(const k of change.remove||[]){
const prev=rowStore.get(k);
if(prev===undefined)continue;
rowStore.delete(k);
contribute(prev,null);
}
refreshModel();
},
forEach(fn){for(const[k,row]of rowStore)fn(row,k);},
get count(){return rowStore.size;},
};
kpi.setRows=(rows)=>{
if(gridBound){
warnOnce('kpi:setRows-gridbound',
'setRows on a grid-bound KPI panel is ignored; the rows come from the bound grid.');
return kpi;
}
if(Array.isArray(rows))cfg.rows=rows;
routed=false;
seed();
return refreshModel();
};
kpi.refresh=()=>{
followPending=false;
if(routed)rebuildFromStore();else seed();
return refreshModel();
};
const followGrid=()=>{
if(followPending)return;
followPending=true;
queueMicrotask(()=>{
if(!followPending)return;
kpi.refresh();
});
};
if(gridBound&&isFunction(cfg.grid.on)){
const grid=cfg.grid;
for(const name of GRID_FOLLOW_EVENTS){
const handler=(payload)=>{
if(name==='model:changed'&&!isPipelineSettle(payload))return;
followGrid();
};
const bound=grid.on(name,handler);
following.push(()=>{
if(isFunction(bound))bound();
else if(isFunction(grid.off))grid.off(name,handler);
});
}
}
kpi.tiles=()=>kpi.model.tiles;
kpi.tile=(id)=>kpi.model.tiles.find((t)=>t.id===String(id));
kpi.value=(id)=>(kpi.tile(id)?.value??null);
kpi.nodes=()=>kpi.model.nodes||[];
kpi.node=(nodeKey)=>{
const wanted=String(nodeKey);
const find=(list)=>{
for(const node of list){
if(node.key===wanted)return node;
const hit=find(node.children);
if(hit)return hit;
}
return undefined;
};
return find(kpi.nodes());
};
const setExpanded=(nodeKey,open)=>{
const wanted=String(nodeKey);
const was=expandedKeys.has(wanted);
if(open===was)return kpi;
if(open)expandedKeys.add(wanted);else expandedKeys.delete(wanted);
rebuildNodes();
if(kpi.el)renderKPI(kpi);
kpi.fire('node:toggle',{key:wanted,expanded:open,node:kpi.node(wanted)});
return kpi;
};
kpi.expand=(nodeKey)=>setExpanded(nodeKey,true);
kpi.collapse=(nodeKey)=>setExpanded(nodeKey,false);
kpi.toggle=(nodeKey)=>setExpanded(nodeKey,!expandedKeys.has(String(nodeKey)));
kpi.visibleNodes=()=>(hierarchy?visibleNodes(kpi.nodes()):[]);
kpi.getState=()=>(hierarchy
?{rows:[...rowStore.values()],expanded:[...expandedKeys]}
:{rows:[...rowStore.values()]});
kpi.setState=(snapshot)=>{
const s=isObject(snapshot)?snapshot:{};
const restoring=Array.isArray(s.expanded);
if(restoring){
expandedKeys.clear();
for(const k of s.expanded)expandedKeys.add(String(k));
}
if(Array.isArray(s.rows))return kpi.setRows(s.rows);
if(restoring){
rebuildNodes();
if(kpi.el)renderKPI(kpi);
}
return kpi;
};
kpi.on=(name,fn)=>{
if(!KPI_EVENTS.includes(name)){
warnOnce(`kpi:event:${name}`,
`"${name}" is not a KPI event; expected one of ${KPI_EVENTS.join(', ')}.`);
}
return emitter.on(name,fn);
};
kpi.off=(name,fn)=>emitter.off(name,fn);
kpi.destroy=()=>{
emitter.map.clear();
followPending=false;
for(const stop of following){try{stop();}catch{}}
following.length=0;
stopClockTicking();
if(onClockVisibility&&kpi.doc&&isFunction(kpi.doc.removeEventListener)){
kpi.doc.removeEventListener('visibilitychange',onClockVisibility);
}
if(chrome){chrome.release();chrome=null;}
if(kpi.el){kpi.el.textContent='';kpi.el.classList.remove('lat-kpi');}
};
if(kpi.el&&isObject(cfg.grid))chrome=adoptGridChrome(kpi.el,cfg.grid);
seed();
refreshModel();
if(hasClockTiles)scheduleClockTick();
return kpi;
}
const __default=createKPI;
});
const __entry=__req("packages/modules/kpi/index.js");
export const
createKPI=__entry.createKPI;
export default __entry.default;