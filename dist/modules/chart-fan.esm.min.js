/*!
 * Lattice Grid 1.33.0, chart-fan module
 * Copyright (c) 2026 TOCLOCO Inc. All rights reserved.
 * https://latticegrid.dev
 */
import*as __ext_0 from"@toclocoinc/lattice-grid/modules/charts";
var __mods=Object.create(null);
var __cache=Object.create(null);
function __def(id,fn){__mods[id]=fn;}
function __req(id){
var hit=__cache[id];
if(hit)return hit;
var exports=Object.create(null);
__cache[id]=exports;
var fn=__mods[id];
if(!fn)throw new Error('[lattice] missing module: '+id);
fn(exports,__req);
return exports;
}
var __extmods=Object.create(null);
__extmods["@toclocoinc/lattice-grid/modules/charts"]=__ext_0;
function __ext(spec){
var mod=__extmods[spec];
var g=typeof globalThis!=='undefined'?globalThis:(typeof self!=='undefined'?self:null);
if(mod)return mod;
if(spec==="@toclocoinc/lattice-grid/modules/charts"&&g&&g["LatticeGrid"])return g["LatticeGrid"];
throw new Error('[lattice] this module shares the Lattice core with the page, but it was not found. Load lattice-grid (or its script build) before this module. Missing: '+spec);
}
__def("packages/modules/chart-fan/index.js",function(__exports,__req){
'use strict';
Object.defineProperty(__exports,"bindFan",{enumerable:true,get:function(){return bindFan;}});
Object.defineProperty(__exports,"drawFan",{enumerable:true,get:function(){return drawFan;}});
Object.defineProperty(__exports,"default",{enumerable:true,get:function(){return __default;}});
const __m0=__ext("@toclocoinc/lattice-grid/modules/charts");
const registerChartType=__m0["registerChartType"];
function bindFan(grid,spec){
const yCol=spec.y&&typeof spec.y==='object'?spec.y.col:spec.y;
const num=(key,col)=>(col?Number(grid.rows.value(key,col)):NaN);
const points=[];
const n=grid.rows.count();
for(let i=0;i<n;i++){
const row=grid.rows.get(i);
if(!row||row.group)continue;
const actual=num(row.key,yCol);
const forecast=num(row.key,spec.forecast);
const lo=num(row.key,spec.lower);
const hi=num(row.key,spec.upper);
const label=spec.x?grid.rows.text(row.key,spec.x):String(points.length);
points.push({
i:points.length,
label,
actual:Number.isFinite(actual)?actual:null,
forecast:Number.isFinite(forecast)?forecast:null,
lo:Number.isFinite(lo)?lo:null,
hi:Number.isFinite(hi)?hi:null,
});
}
return{points,empty:points.length===0};
}
function drawFan(ctx){
const{
plot,bound,scheme,fontSize,groups,grid,helpers,
}=ctx;
const{
measureDomain,linearScale,linearTicks,Pool,attrs,path,round,setText,
formatters,resolveScheme,NS,
}=helpers;
const sch=scheme||resolveScheme();
const colour=sch.series(0);
const points=bound.points;
const values=[];
for(const p of points)for(const v of[p.actual,p.forecast,p.lo,p.hi])if(v!==null)values.push(v);
if(!values.length)return{empty:true,legend:[]};
const yDomain=measureDomain(values,{zero:false});
const xScale=linearScale({min:0,max:Math.max(1,points.length-1)},[plot.left,plot.right]);
const yScale=linearScale(yDomain,[plot.bottom,plot.top]);
const gridPool=new Pool(groups.grid);
const axisPool=new Pool(groups.axis);
const fmt=formatters(grid,{kind:'linear',step:(yDomain.max-yDomain.min)/5||1});
for(const t of linearTicks(yDomain,5)){
const y=yScale.of(t);
attrs(gridPool.next('line',`${NS}__gridline`),{
x1:round(plot.left),y1:round(y),x2:round(plot.right),y2:round(y),
stroke:'currentColor','stroke-opacity':0.1,
});
const label=axisPool.next('text',`${NS}__tick`);
setText(label,fmt.tick(t));
attrs(label,{x:round(plot.left-4),y:round(y),'text-anchor':'end','font-size':fontSize});
}
const marks=new Pool(groups.marks);
const band=points.filter((p)=>p.lo!==null&&p.hi!==null);
if(band.length>=2){
const parts=['M'];
band.forEach((p,i)=>{
parts.push(round(xScale.of(p.i)),round(yScale.of(p.hi)));
if(i<band.length-1)parts.push('L');
});
for(let i=band.length-1;i>=0;i--){
parts.push('L',round(xScale.of(band[i].i)),round(yScale.of(band[i].lo)));
}
parts.push('Z');
attrs(marks.next('path',`${NS}__fan-band ${NS}__mark`),{
d:path(parts),fill:colour,'fill-opacity':0.18,stroke:'none',
});
}
const line=(field,cls,dash)=>{
const seq=points.filter((p)=>p[field]!==null);
if(seq.length<1)return;
const parts=['M'];
seq.forEach((p,i)=>{
parts.push(round(xScale.of(p.i)),round(yScale.of(p[field])));
if(i<seq.length-1)parts.push('L');
});
const el=marks.next('path',`${cls} ${NS}__mark`);
const a={d:path(parts),stroke:colour,'stroke-width':2,fill:'none'};
if(dash)a['stroke-dasharray']=dash;
attrs(el,a);
};
line('actual',`${NS}__fan-actual`,'');
line('forecast',`${NS}__fan-forecast`,'5 4');
gridPool.finish();
axisPool.finish();
marks.finish();
return{
points:points.length,band:band.length,xScale,yScale,legend:[],
};
}
registerChartType('fan',{draw:drawFan,bind:bindFan,labelled:false});
const __default=drawFan;
});
const __entry=__req("packages/modules/chart-fan/index.js");
export const
bindFan=__entry.bindFan,
drawFan=__entry.drawFan;
export default __entry.default;